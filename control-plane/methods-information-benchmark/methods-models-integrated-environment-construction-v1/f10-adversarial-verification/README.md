# F10 Adversarial Verification

F10 attacks the assembled F9 Methods & Models environment as an integrated object. It does not create scientific meaning, change claim ceilings, mutate F1-F9, or touch public Methods/Laws surfaces.

The gate asks whether deliberately hostile inputs can make the environment violate its frozen authority laws. PASS requires the attacks to fail closed while the authorized paths remain operational.

The quarantined F9 verification run `31217689014` is preserved as integrity history because its shell pipeline used `tee` without `pipefail`; it is not admissible PASS evidence. F10 itself requires fail-propagating verification.

F11 authority is earned only by an effective exact-head F10 PASS and separate terminalization. F11 execution remains separately authorized.

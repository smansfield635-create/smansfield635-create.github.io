# Compass label final diagnosis — 2026-08-22

The public rendered-identity audit identifies the coherent label owner as the native `button.compass-object.compass-object--wing.is-readable-cardinal[data-compass-cardinal]`. In the audited north state it renders `Orientation / North star` with the intended background, border, shadow, z-index, and text intact.

The Gen1596 composite intervention is therefore not permitted to hide spans, force transparency, or independently choose the readable cardinal. Those operations override native presentation ownership and can diverge from the visually foreground star after interaction.

The remaining defective black duplicate is a presentation shell associated with a non-readable cardinal. The bounded correction is:

1. Restore native cardinal presentation ownership by removing Gen1596 style mutation and readable-cardinal selection logic.
2. Preserve all four star hit targets and their native labels.
3. Suppress only the decorative shell pseudo-elements of non-readable cardinal buttons in constellation mode.
4. Retain the already-successful Mirrorland duplicate-text suppression and room/capability ownership corrections.
5. Qualification must show an intact native readable label before and after a real drag and no visible shell on non-readable cardinal buttons.

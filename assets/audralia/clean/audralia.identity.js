// /assets/audralia/clean/audralia.identity.js
// AUDRALIA_CLEAN_CANVAS_IDENTITY_TNT_v1
// Full-file replacement.
// File 5 of 16.
// Planet Audralia identity authority.
// Purpose:
// - Establishes Audralia as the first specific planet instance beneath Showroom Globe universal planet-family law.
// - Defines Audralia as the Possibility Path inside the Nine Summits Universe.
// - Defines Audralia as a clean-slate mirror world and future-capable planet instance.
// - Defines H-Earth / Hybrid Earth as a downstream ground-view expression of Planet Audralia, not a separate planet.
// - Provides identity, narrative, route, inheritance, and public-expression metadata to downstream clean files.
// - Does not render.
// - Does not mount.
// - Does not draw.
// - Does not own land, water, elevation, climate, biome, surface, atmosphere behavior, runtime, controls, canvas, route bridge, or HTML shell.
// No generated image. No GraphicBox. No visual-pass claim.

(() => {
  "use strict";

  const CONTRACT = "AUDRALIA_CLEAN_CANVAS_IDENTITY_TNT_v1";
  const RECEIPT = "AUDRALIA_CLEAN_CANVAS_IDENTITY_RECEIPT_v1";
  const PREVIOUS_CONTRACT = "AUDRALIA_CLEAN_CANVAS_PLANET_FAMILY_PALETTE_TNT_v1";
  const VERSION = "2026-05-20.audralia-clean-canvas-identity-v1";

  const FILE_NUMBER = 5;
  const PRIMARY_NODE = 5;
  const SUBNODE_RANGE = Object.freeze([65, 80]);

  const UNIVERSAL_ANCHOR = "/showroom/globe/";
  const AUDRALIA_ROUTE = "/showroom/globe/audralia/";
  const AUDRALIA_HTML = "/showroom/globe/audralia/index.html";
  const AUDRALIA_JS = "/showroom/globe/audralia/index.js";
  const H_EARTH_ROUTE = "/showroom/globe/h-earth/";

  const IDENTITY = Object.freeze({
    canonicalName: "Audralia",
    canonicalSpelling: "A_U_D_R_A_L_I_A",
    planetClass: "Nine Summits Universe planet",
    pathRole: "Possibility Path",
    expressionRole: "clean-slate mirror world",
    firstInstance: true,
    universalParent: "Showroom Globe",
    universalParentRoute: UNIVERSAL_ANCHOR,
    route: AUDRALIA_ROUTE,
    htmlExpressionShell: AUDRALIA_HTML,
    routeBridge: AUDRALIA_JS,
    downstreamGroundView: "H-Earth / Hybrid Earth",
    downstreamGroundRoute: H_EARTH_ROUTE,
    separatePlanetFromHEarth: false,
    separatePlanetFromEarth: true,
    separatePlanetFromZIONTS: true,
    separatePlanetFromAustralia: true
  });

  const PUBLIC_THESIS = Object.freeze({
    title: "Audralia · The Possibility Path",
    headline: "Possibility is not fantasy.",
    shortExpression: "A clean-slate mirror world for proving what a better future could become.",
    coreStatement: "Audralia is the Possibility Path: a visible planet instance where lawful systems, survival infrastructure, technology, ecology, and future society can be developed before being mirrored back to Earth.",
    proofStatement: "What is proven on Audralia should become useful here.",
    inspectionStatement: "Audralia remains inspectable because possibility must be visible, not merely described.",
    hEarthStatement: "H-Earth is the ground-view expression of Planet Audralia. It is not a separate planet.",
    inheritanceStatement: "Planet-level atmosphere, weather, and condition truth originate upstream on Audralia and flow downstream into H-Earth by default."
  });

  const IDENTITY_BOUNDARIES = Object.freeze({
    owns: Object.freeze([
      "planet identity",
      "canonical name",
      "canonical spelling",
      "Possibility Path role",
      "Nine Summits Universe placement",
      "clean-slate mirror-world role",
      "Audralia route identity",
      "H-Earth downstream ground-view relationship",
      "public identity thesis",
      "inheritance identity statement"
    ]),
    doesNotOwn: Object.freeze([
      "universal manifest law",
      "math primitives",
      "lattice authority",
      "palette constants",
      "land/ocean footprint",
      "hydrology",
      "elevation",
      "climate fields",
      "biome categories",
      "surface material synthesis",
      "atmosphere behavior",
      "weather computation",
      "runtime motion",
      "controls",
      "canvas composition",
      "route bridge execution",
      "HTML expression rendering"
    ])
  });

  const NOT_RELATIONSHIPS = Object.freeze({
    notEarth: true,
    notAustralia: true,
    notZIONTS: true,
    notHEarthAsSeparatePlanet: true,
    notRouteGraphic: true,
    notDetachedVisualDemo: true,
    notGeneratedImage: true,
    notGraphicBox: true
  });

  const DOWNSTREAM_INHERITANCE = Object.freeze({
    sourcePlanet: "Audralia",
    sourceRoute: AUDRALIA_ROUTE,
    downstreamView: "H-Earth / Hybrid Earth",
    downstreamRoute: H_EARTH_ROUTE,
    rule: "H-Earth inherits Audralia planet-level atmosphere, weather, and condition truth by default.",
    localViewModifierAllowed: true,
    localViewModifierBoundary: "H-Earth may express local ground-view modifiers, but it must not duplicate planet-level authority unless explicitly authorized.",
    inheritanceFlow: Object.freeze([
      "audralia.identity.js",
      "audralia.atmosphere.js",
      "audralia.engine.js",
      "/showroom/globe/audralia/index.js",
      "/showroom/globe/audralia/index.html",
      "/showroom/globe/h-earth/"
    ])
  });

  const NINE_SUMMITS_RELATION = Object.freeze({
    universe: "Nine Summits Universe",
    planet: "Audralia",
    path: "Possibility Path",
    summitRelationship: "Audralia carries the possibility-side planetary expression of the Nine Summits Universe.",
    publicUse: "The planet should make possibility visible through lawful structure, not through fantasy decoration.",
    internalUse: "Downstream children may use Nine Summits address fields without changing Audralia identity authority."
  });

  const EXPRESSION_TONES = Object.freeze({
    visualRead: Object.freeze([
      "alive",
      "clean",
      "ocean-capable",
      "future-capable",
      "organized",
      "believable",
      "not cartoonish",
      "not a badge",
      "not a blob",
      "not a fog sphere"
    ]),
    pageRead: Object.freeze([
      "public-facing",
      "concrete",
      "inspectable",
      "possibility-driven",
      "scientifically responsible",
      "future-facing",
      "not abstract system lecture"
    ])
  });

  const CHILD_EXPECTATIONS = Object.freeze({
    landmask: Object.freeze({
      file: "/assets/audralia/clean/audralia.landmask.js",
      expectedContract: "AUDRALIA_CLEAN_CANVAS_LANDMASK_TNT_v1",
      relationship: "landmask gives Audralia physical footprint but cannot rename the planet"
    }),
    hydrology: Object.freeze({
      file: "/assets/audralia/clean/audralia.hydrology.js",
      expectedContract: "AUDRALIA_CLEAN_CANVAS_HYDROLOGY_TNT_v1",
      relationship: "hydrology gives Audralia water behavior but cannot create the core planet identity"
    }),
    elevation: Object.freeze({
      file: "/assets/audralia/clean/audralia.elevation.js",
      expectedContract: "AUDRALIA_CLEAN_CANVAS_ELEVATION_TNT_v1",
      relationship: "elevation gives Audralia vertical depth but cannot create identity or footprint"
    }),
    hEarth: Object.freeze({
      route: H_EARTH_ROUTE,
      relationship: "H-Earth is a downstream ground-view expression of Audralia"
    })
  });

  function clone(value) {
    if (Array.isArray(value)) return Object.freeze(value.map(clone));
    if (value && typeof value === "object") {
      const out = {};
      Object.keys(value).forEach((key) => {
        out[key] = clone(value[key]);
      });
      return Object.freeze(out);
    }
    return value;
  }

  function normalizeKey(value) {
    return String(value || "")
      .trim()
      .toLowerCase()
      .replace(/[^a-z0-9]+/g, "-")
      .replace(/^-+|-+$/g, "");
  }

  function isAudraliaName(value) {
    const key = normalizeKey(value);
    return key === "audralia" || key === "a-u-d-r-a-l-i-a";
  }

  function isHEarthGroundView(value) {
    const key = normalizeKey(value);
    return key === "h-earth" || key === "hybrid-earth" || key === "hearth-ground-view" || key === "h-earth-hybrid-earth";
  }

  function classifyRoute(route) {
    const path = String(route || "").trim();

    if (path === AUDRALIA_ROUTE || path === AUDRALIA_HTML || path === AUDRALIA_JS) {
      return Object.freeze({
        route: path,
        planet: "Audralia",
        relationship: "planet-instance",
        isAudralia: true,
        isDownstreamGroundView: false,
        separatePlanet: false
      });
    }

    if (path === H_EARTH_ROUTE || path.startsWith(H_EARTH_ROUTE)) {
      return Object.freeze({
        route: path,
        planet: "Audralia",
        relationship: "downstream-ground-view",
        isAudralia: true,
        isDownstreamGroundView: true,
        separatePlanet: false
      });
    }

    if (path === UNIVERSAL_ANCHOR || path.startsWith(UNIVERSAL_ANCHOR)) {
      return Object.freeze({
        route: path,
        planet: null,
        relationship: "showroom-globe-universal-planet-family-law-or-descendant",
        isAudralia: path.startsWith(AUDRALIA_ROUTE) || path.startsWith(H_EARTH_ROUTE),
        isDownstreamGroundView: path.startsWith(H_EARTH_ROUTE),
        separatePlanet: false
      });
    }

    return Object.freeze({
      route: path,
      planet: null,
      relationship: "outside-audralia-identity-scope",
      isAudralia: false,
      isDownstreamGroundView: false,
      separatePlanet: null
    });
  }

  function getPublicThesis() {
    return clone(PUBLIC_THESIS);
  }

  function getIdentity() {
    return clone(IDENTITY);
  }

  function getDownstreamInheritance() {
    return clone(DOWNSTREAM_INHERITANCE);
  }

  function getNineSummitsRelation() {
    return clone(NINE_SUMMITS_RELATION);
  }

  function getExpressionTones() {
    return clone(EXPRESSION_TONES);
  }

  function getChildExpectations() {
    return clone(CHILD_EXPECTATIONS);
  }

  function validateManifestRegistration() {
    try {
      const manifest = window.DGB_PLANET_FAMILY_MANIFEST || window.AUDRALIA_CLEAN_CANVAS_MANIFEST;
      if (!manifest || typeof manifest.validatePrimaryFile !== "function") {
        return Object.freeze({
          manifestAvailable: false,
          valid: true,
          reason: "manifest_not_loaded_yet_identity_can_still_register"
        });
      }

      return manifest.validatePrimaryFile({
        path: "/assets/audralia/clean/audralia.identity.js",
        contract: CONTRACT
      });
    } catch (error) {
      return Object.freeze({
        manifestAvailable: true,
        valid: false,
        reason: error instanceof Error ? error.message : String(error)
      });
    }
  }

  function validateUniversalAuthorities() {
    const manifest = window.DGB_PLANET_FAMILY_MANIFEST || window.AUDRALIA_CLEAN_CANVAS_MANIFEST || null;
    const math = window.DGB_PLANET_FAMILY_MATH || window.AUDRALIA_CLEAN_CANVAS_MATH || null;
    const lattice = window.DGB_PLANET_FAMILY_LATTICE || window.AUDRALIA_CLEAN_CANVAS_LATTICE || null;
    const palette = window.DGB_PLANET_FAMILY_PALETTE || window.AUDRALIA_CLEAN_CANVAS_PALETTE || null;

    return Object.freeze({
      manifest: Object.freeze({
        available: Boolean(manifest),
        expectedContract: "AUDRALIA_CLEAN_CANVAS_PLANET_FAMILY_MANIFEST_TNT_v1",
        actualContract: manifest?.contract || null,
        valid: !manifest || manifest.contract === "AUDRALIA_CLEAN_CANVAS_PLANET_FAMILY_MANIFEST_TNT_v1"
      }),
      math: Object.freeze({
        available: Boolean(math),
        expectedContract: "AUDRALIA_CLEAN_CANVAS_PLANET_FAMILY_MATH_TNT_v1",
        actualContract: math?.contract || null,
        valid: !math || math.contract === "AUDRALIA_CLEAN_CANVAS_PLANET_FAMILY_MATH_TNT_v1"
      }),
      lattice: Object.freeze({
        available: Boolean(lattice),
        expectedContract: "AUDRALIA_CLEAN_CANVAS_PLANET_FAMILY_LATTICE_TNT_v1",
        actualContract: lattice?.contract || null,
        valid: !lattice || lattice.contract === "AUDRALIA_CLEAN_CANVAS_PLANET_FAMILY_LATTICE_TNT_v1"
      }),
      palette: Object.freeze({
        available: Boolean(palette),
        expectedContract: "AUDRALIA_CLEAN_CANVAS_PLANET_FAMILY_PALETTE_TNT_v1",
        actualContract: palette?.contract || null,
        valid: !palette || palette.contract === "AUDRALIA_CLEAN_CANVAS_PLANET_FAMILY_PALETTE_TNT_v1"
      })
    });
  }

  function getStatus() {
    return Object.freeze({
      contract: CONTRACT,
      receipt: RECEIPT,
      previousContract: PREVIOUS_CONTRACT,
      version: VERSION,
      authority: "audralia-planet-identity",
      fileNumber: FILE_NUMBER,
      primaryNode: PRIMARY_NODE,
      subnodes: SUBNODE_RANGE,
      parentAnchor: AUDRALIA_ROUTE,
      universalParent: UNIVERSAL_ANCHOR,
      identity: clone(IDENTITY),
      publicThesis: clone(PUBLIC_THESIS),
      boundaries: clone(IDENTITY_BOUNDARIES),
      notRelationships: clone(NOT_RELATIONSHIPS),
      downstreamInheritance: clone(DOWNSTREAM_INHERITANCE),
      nineSummitsRelation: clone(NINE_SUMMITS_RELATION),
      expressionTones: clone(EXPRESSION_TONES),
      childExpectations: clone(CHILD_EXPECTATIONS),
      manifestRegistration: validateManifestRegistration(),
      universalAuthorities: validateUniversalAuthorities(),
      hEarthIsAudraliaGroundView: true,
      hEarthSeparatePlanet: false,
      atmosphereWeatherInheritanceExpected: true,
      fibonacciChronology: true,
      primaryStructure16: true,
      nodalConstruct256: true,
      oneFileOneJob: true,
      generatedImage: false,
      graphicBox: false,
      visualPassClaimed: false
    });
  }

  const API = Object.freeze({
    contract: CONTRACT,
    receipt: RECEIPT,
    previousContract: PREVIOUS_CONTRACT,
    version: VERSION,

    FILE_NUMBER,
    PRIMARY_NODE,
    SUBNODE_RANGE,
    UNIVERSAL_ANCHOR,
    AUDRALIA_ROUTE,
    AUDRALIA_HTML,
    AUDRALIA_JS,
    H_EARTH_ROUTE,

    identity: IDENTITY,
    publicThesis: PUBLIC_THESIS,
    identityBoundaries: IDENTITY_BOUNDARIES,
    notRelationships: NOT_RELATIONSHIPS,
    downstreamInheritance: DOWNSTREAM_INHERITANCE,
    nineSummitsRelation: NINE_SUMMITS_RELATION,
    expressionTones: EXPRESSION_TONES,
    childExpectations: CHILD_EXPECTATIONS,

    normalizeKey,
    isAudraliaName,
    isHEarthGroundView,
    classifyRoute,
    getPublicThesis,
    getIdentity,
    getDownstreamInheritance,
    getNineSummitsRelation,
    getExpressionTones,
    getChildExpectations,
    validateManifestRegistration,
    validateUniversalAuthorities,
    getStatus
  });

  window.AUDRALIA_IDENTITY = API;
  window.AUDRALIA_IDENTITY_RECEIPT = getStatus();

  window.AUDRALIA_CLEAN_CANVAS_IDENTITY = API;
  window.AUDRALIA_CLEAN_CANVAS_IDENTITY_RECEIPT = getStatus();

  if (document?.documentElement?.dataset) {
    document.documentElement.dataset.audraliaIdentityLoaded = "true";
    document.documentElement.dataset.audraliaIdentityContract = CONTRACT;
    document.documentElement.dataset.audraliaIdentityReceipt = RECEIPT;
    document.documentElement.dataset.audraliaIdentityVersion = VERSION;
    document.documentElement.dataset.audraliaCleanCanvasIdentityLoaded = "true";
    document.documentElement.dataset.audraliaCleanCanvasIdentityContract = CONTRACT;
    document.documentElement.dataset.audraliaCleanCanvasIdentityReceipt = RECEIPT;
    document.documentElement.dataset.audraliaCleanCanvasIdentityNode = String(PRIMARY_NODE);
    document.documentElement.dataset.audraliaCleanCanvasIdentitySubnodes = "65-80";
    document.documentElement.dataset.audraliaCanonicalSpelling = IDENTITY.canonicalSpelling;
    document.documentElement.dataset.audraliaPlanetInstance = "true";
    document.documentElement.dataset.audraliaPossibilityPath = "true";
    document.documentElement.dataset.audraliaNineSummitsPlanet = "true";
    document.documentElement.dataset.audraliaNotAustralia = "true";
    document.documentElement.dataset.audraliaNotEarth = "true";
    document.documentElement.dataset.audraliaNotZionts = "true";
    document.documentElement.dataset.hEarthIsAudraliaGroundView = "true";
    document.documentElement.dataset.hEarthSeparatePlanet = "false";
    document.documentElement.dataset.hEarthInheritsAudraliaConditions = "true";
    document.documentElement.dataset.audraliaCleanCanvasFibonacciChronology = "true";
    document.documentElement.dataset.audraliaCleanCanvasPrimaryStructure16 = "true";
    document.documentElement.dataset.audraliaCleanCanvasNodalConstruct256 = "true";
    document.documentElement.dataset.audraliaCleanCanvasOneFileOneJob = "true";
    document.documentElement.dataset.generatedImage = "false";
    document.documentElement.dataset.graphicBox = "false";
    document.documentElement.dataset.visualPassClaimed = "false";
  }
})();

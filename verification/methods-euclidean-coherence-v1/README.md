# Methods Euclidean Nonproduct Coherence Harness v1

This verification-only harness binds the exact `METHODS_MODELS_EUCLIDEAN_SHOWROOM_v3` source at candidate head `66a2105e96e84c5b482f783010779f87a90a28ee` to `METHODS_MODELS_COHERENCE_INSTRUMENT@1.0.0` evidence semantics.

It does not modify the public Methods source. The workflow verifies that the only branch delta is this runner, this README, and its workflow before execution.

The runner executes:

- 75 semantic coordinates across nine viewports and two camera observations: 1,350 stable-stage evaluations;
- 25 model inspection-and-return cycles across nine viewports: 225 cycles;
- keyboard, touch, reduced-motion, JavaScript-disabled, focus, geometry, touch-target, transition, screenshot, source, and custody checks;
- deterministic risk screenshots and a human-review receipt template.

The workflow produces raw observations and instrument-compatible candidate evidence. Human visual review remains a required external gate.

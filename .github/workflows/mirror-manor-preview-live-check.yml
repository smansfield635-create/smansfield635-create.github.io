name: Mirror Manor Preview Live Check
on:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Install Chromium
        run: |
          mkdir -p /tmp/manor-preview-check && cd /tmp/manor-preview-check
          npm init -y >/dev/null 2>&1
          npm install playwright@1.55.0 >/dev/null 2>&1
          npx playwright install --with-deps chromium >/dev/null 2>&1
      - name: Verify direct live viewer initializes canonical Manor renderer
        run: |
          cat >/tmp/manor-preview-check/check.mjs <<'EOF'
          import { chromium } from 'playwright';
          const browser=await chromium.launch({headless:true});
          const page=await browser.newPage({viewport:{width:1440,height:1000}});
          await page.goto('https://diamondgatebridge.com/mirror-manor-preview/?verify='+Date.now(),{waitUntil:'networkidle',timeout:120000});
          await page.waitForFunction(()=>document.querySelector('#manor')?.dataset.houseAssetReady==='true',{timeout:30000});
          const state=await page.evaluate(()=>{const c=document.querySelector('#manor');return {renderer:c.dataset.houseRenderer,ready:c.dataset.houseAssetReady,error:c.dataset.houseError||null,frames:Number(c.dataset.houseFrames||0),vertices:Number(c.dataset.houseVertexCount||0),width:c.width,height:c.height,status:document.querySelector('#status')?.textContent||''};});
          console.log(JSON.stringify(state,null,2));
          await page.screenshot({path:'/tmp/manor-preview-check/live.png',fullPage:true});
          await browser.close();
          if(state.ready!=='true'||state.error||state.frames<1||state.vertices<1)process.exit(1);
          EOF
          cd /tmp/manor-preview-check && node check.mjs
      - name: Upload screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mirror-manor-preview-live
          path: /tmp/manor-preview-check/live.png
          if-no-files-found: warn
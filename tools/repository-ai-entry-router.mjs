#!/usr/bin/env node
import fs from 'node:fs';
import path from 'node:path';
import { spawnSync } from 'node:child_process';

const LEGACY_RELATIVE = 'tools/repository-ai-entry-router.v3-legacy.mjs';
const STATIC_VERIFIER = 'tools/static-editorial-micro-verifier.v1.mjs';
const STATIC_CONTENT_EXTENSIONS = new Set(['.html', '.htm', '.md', '.txt']);
const STATIC_PRESENTATION_EXTENSIONS = new Set(['.css']);
const EXECUTABLE_EXTENSIONS = new Set(['.js', '.mjs', '.cjs', '.ts', '.tsx', '.jsx', '.wasm', '.py', '.rb', '.php', '.sh', '.bash', '.zsh', '.ps1']);
const AUTHORITY_PREFIXES = ['.github/', 'tools/', 'control-plane/', 'governance/', 'evidence/'];
const STATIC_TASK_TOKENS = ['COPY', 'TEXT', 'EDITORIAL', 'WORDING', 'LABEL', 'INSTRUCTION', 'INSTRUCTIONS', 'TYPO', 'PUNCTUATION', 'CONTENT', 'HEADLINE', 'DESCRIPTION'];
const PRESENTATION_TASK_TOKENS = ['STATIC', 'PRESENTATION', 'ORDER', 'SPACING', 'TYPOGRAPHY', 'COLOR', 'MARGIN', 'PADDING', 'LAYOUT', 'VISUAL'];
const RUNTIME_TASK_TOKENS = ['RUNTIME', 'BEHAVIOR', 'BEHAVIOUR', 'NAVIGATION', 'STATE MACHINE', 'STATE-MACHINE', 'EVENT', 'HANDLER', 'GESTURE', 'ROUTING', 'ROUTE', 'CONTROLLER', 'RENDERER', 'AUTHORITY', 'DOM REPARENT', 'REPARENT', 'POINTER', 'HIT TARGET', 'HIT-TARGET', 'KEYBOARD', 'FOCUS'];
const CSS_RUNTIME_TOKENS = ['POINTER-EVENTS', 'DISPLAY', 'VISIBILITY', 'Z-INDEX', 'POSITION:FIXED', 'POSITION: FIXED', 'TOUCH-ACTION', 'USER-SELECT'];
function die(message, code = 1) { process.stderr.write(`${message}\n`); process.exit(code); }
function discoverRoot(start) { let current = path.resolve(start); while (true) { if (fs.existsSync(path.join(current, 'AI_ENTRYPOINT.json'))) return current; const parent = path.dirname(current); if (parent === current) die('REPOSITORY_ENTRYPOINT_NOT_FOUND:AI_ENTRYPOINT.json'); current = parent; } }
function consumeValue(argv, index, token) { const value = argv[index + 1]; if (typeof value !== 'string' || value === '' || value.startsWith('--')) die(`MISSING_ARGUMENT_VALUE:${token}`); return value; }
function parseArgs(argv) { const parsed = { paths: [], pathsFile: null, task: '', mutationIntent: false, output: null, selfTest: false }; for (let i = 0; i < argv.length; i += 1) { const token = argv[i]; if (token === '--path') { parsed.paths.push(consumeValue(argv, i, token)); i += 1; } else if (token === '--paths-file') { parsed.pathsFile = consumeValue(argv, i, token); i += 1; } else if (token === '--task') { parsed.task = consumeValue(argv, i, token); i += 1; } else if (token === '--mutation-intent') parsed.mutationIntent = true; else if (token === '--output') { parsed.output = consumeValue(argv, i, token); i += 1; } else if (token === '--self-test') parsed.selfTest = true; else if (token === '--intake-availability-receipt') i += 1; else die(`UNKNOWN_ARGUMENT:${token}`); } return parsed; }
function normalizeRepositoryPath(value) { const slash = String(value ?? '').trim().replaceAll('\\', '/').replace(/^\.\/+/, ''); if (!slash || slash.startsWith('/') || slash === '..' || slash.startsWith('../') || slash.includes('/../')) die(`INVALID_REPOSITORY_PATH:${value}`); return slash.replace(/\/+/g, '/'); }
function collectPaths(root, parsed) { const paths = [...parsed.paths]; if (parsed.pathsFile) { const text = fs.readFileSync(path.resolve(root, parsed.pathsFile), 'utf8'); paths.push(...text.split(/\r?\n/).filter(Boolean)); } return [...new Set(paths.map(normalizeRepositoryPath))].sort(); }
function containsAny(text, tokens) { return tokens.some((token) => text.includes(token)); }
function classifyPageMutation(paths, task = '') { const normalizedPaths = [...new Set(paths.map(normalizeRepositoryPath))].sort(); const upperTask = String(task || '').toUpperCase(); const extensions = normalizedPaths.map((filePath) => path.extname(filePath).toLowerCase()); const governed = (reasonCode) => ({ mutationClass: 'RUNTIME_OR_AUTHORITY', reasonCode, canonicalAdmissionRequired: true, pageExcellenceRequired: true }); if (normalizedPaths.length === 0) return governed('NO_DECLARED_PATHS_FAIL_CLOSED'); if (normalizedPaths.some((filePath) => AUTHORITY_PREFIXES.some((prefix) => filePath.startsWith(prefix)))) return governed('CONTROL_OR_AUTHORITY_SURFACE'); if (extensions.some((extension) => EXECUTABLE_EXTENSIONS.has(extension))) return governed('EXECUTABLE_SURFACE_PRESENT'); if (containsAny(upperTask, RUNTIME_TASK_TOKENS)) return governed('RUNTIME_SEMANTICS_DECLARED'); const contentOnly = extensions.every((extension) => STATIC_CONTENT_EXTENSIONS.has(extension)); const staticPresentationOnly = extensions.every((extension) => STATIC_CONTENT_EXTENSIONS.has(extension) || STATIC_PRESENTATION_EXTENSIONS.has(extension)); const hasCss = extensions.some((extension) => STATIC_PRESENTATION_EXTENSIONS.has(extension)); if (contentOnly && containsAny(upperTask, STATIC_TASK_TOKENS)) return { mutationClass: 'STATIC_EDITORIAL_MICRO', reasonCode: 'STATIC_CONTENT_ONLY_WITH_EDITORIAL_INTENT', canonicalAdmissionRequired: false, pageExcellenceRequired: false, verifier: STATIC_VERIFIER, requiredProofs: ['EXACT_HEAD', 'DECLARED_PATHS_ONLY', 'NO_EXECUTABLE_RUNTIME_DELTA', 'NO_UNRELATED_DIFF'] }; if (staticPresentationOnly && hasCss && containsAny(upperTask, PRESENTATION_TASK_TOKENS) && !containsAny(upperTask, CSS_RUNTIME_TOKENS)) return { mutationClass: 'STATIC_EDITORIAL_MICRO', reasonCode: 'BOUNDED_STATIC_PRESENTATION_INTENT', canonicalAdmissionRequired: false, pageExcellenceRequired: false, verifier: STATIC_VERIFIER, requiredProofs: ['EXACT_HEAD', 'DECLARED_PATHS_ONLY', 'NO_EXECUTABLE_RUNTIME_DELTA', 'NO_UNRELATED_DIFF', 'STATIC_PRESENTATION_ONLY'] }; return governed('AMBIGUOUS_MUTATION_FAIL_CLOSED'); }
function stripMutationOnlyArgs(argv) { const stripped = []; for (let i = 0; i < argv.length; i += 1) { const token = argv[i]; if (token === '--mutation-intent') continue; if (token === '--intake-availability-receipt') { i += 1; continue; } if (token === '--output') { i += 1; continue; } stripped.push(token); } return stripped; }
function runLegacy(root, argv) { return spawnSync(process.execPath, [path.join(root, LEGACY_RELATIVE), ...argv], { cwd: root, encoding: 'utf8' }); }
function writeReceipt(output, receipt) { const text = `${JSON.stringify(receipt, null, 2)}\n`; if (output) { const absolute = path.resolve(output); fs.mkdirSync(path.dirname(absolute), { recursive: true }); fs.writeFileSync(absolute, text); } process.stdout.write(text); }
function selfTest(root) { const cases = [ ['HTML_INSTRUCTION_COPY_IS_MICRO',['index.html'],'add approved instructions copy','STATIC_EDITORIAL_MICRO'], ['HTML_TYPO_IS_MICRO',['laws/index.html'],'fix typo in page text','STATIC_EDITORIAL_MICRO'], ['CSS_ORDER_IS_MICRO',['assets/compass/compass.capability-carousel.css'],'static presentation order change','STATIC_EDITORIAL_MICRO'], ['JS_CHANGE_IS_RUNTIME',['assets/compass/compass.controller.js'],'change guidance behavior','RUNTIME_OR_AUTHORITY'], ['HTML_NAVIGATION_CHANGE_IS_RUNTIME',['index.html'],'change navigation routing behavior','RUNTIME_OR_AUTHORITY'], ['AMBIGUOUS_HTML_FAILS_CLOSED',['index.html'],'update page','RUNTIME_OR_AUTHORITY'], ['CONTROL_PLANE_ALWAYS_GOVERNED',['.github/workflows/test.yml'],'edit copy','RUNTIME_OR_AUTHORITY'] ].map(([id,paths,task,expected]) => { const actual = classifyPageMutation(paths,task); return {id,expected,actual:actual.mutationClass,pass:actual.mutationClass===expected}; }); const legacy = runLegacy(root,['--self-test']); let legacyReceipt=null; try { legacyReceipt=JSON.parse(legacy.stdout||'{}'); } catch {} const legacyPass=legacy.status===0&&legacyReceipt?.result==='PASS'; return { schema:'PAGE_MUTATION_CLASSIFICATION_INSTRUMENT_SELF_TEST_v1', instrumentId:'PAGE_MUTATION_CLASSIFICATION_INSTRUMENT_v1', result:cases.every((c)=>c.pass)&&legacyPass?'PASS':'FAIL', scenarios:cases, legacyRouterSelfTest:legacyPass?'PASS':'FAIL' }; }
const root = discoverRoot(process.cwd()); const argv = process.argv.slice(2); const parsed = parseArgs(argv); if (parsed.selfTest) { const receipt=selfTest(root); writeReceipt(parsed.output,receipt); process.exit(receipt.result==='PASS'?0:1); } if (!parsed.mutationIntent) { const legacy=runLegacy(root,argv); process.stdout.write(legacy.stdout||''); process.stderr.write(legacy.stderr||''); process.exit(legacy.status??1); } const paths=collectPaths(root,parsed); const classification=classifyPageMutation(paths,parsed.task); if (classification.mutationClass!=='STATIC_EDITORIAL_MICRO') { const legacy=runLegacy(root,argv); process.stdout.write(legacy.stdout||''); process.stderr.write(legacy.stderr||''); process.exit(legacy.status??1); } const legacy=runLegacy(root,stripMutationOnlyArgs(argv)); let routeReceipt=null; try { routeReceipt=JSON.parse(legacy.stdout||'{}'); } catch { die(`LEGACY_ROUTE_RECEIPT_INVALID:${legacy.stderr||legacy.stdout}`); } const blocked=legacy.status!==0||routeReceipt.disposition!=='PASS'; const receipt={ ...routeReceipt, schema:'REPOSITORY_AI_ENTRY_ROUTER_RECEIPT_v3', mutationIntent:true, pageMutationClassification:{ schema:'PAGE_MUTATION_CLASSIFICATION_RECEIPT_v1', instrumentId:'PAGE_MUTATION_CLASSIFICATION_INSTRUMENT_v1', result:blocked?'BLOCK':'PASS_STATIC_EDITORIAL_MICRO', ...classification, declaredPaths:paths, task:parsed.task||null, ambiguityPolicy:'RUNTIME_OR_AUTHORITY' }, operationIntakeExecution:blocked?null:{ schema:'REPOSITORY_AI_EXECUTION_ROUTE_RESOLUTION_RECEIPT_v1', result:'NOT_REQUIRED_STATIC_EDITORIAL_MICRO', backendId:null, canonicalAdmissionRequired:false, reasonCode:'STATIC_EDITORIAL_MICRO_BYPASS_BY_DETERMINISTIC_CLASSIFIER' }, disposition:blocked?routeReceipt.disposition:'PASS', reasonCodes:[...new Set([...(routeReceipt.reasonCodes??[]),blocked?'STATIC_EDITORIAL_ROUTE_NOT_PASS':'STATIC_EDITORIAL_MICRO_CLASSIFIED_NO_CANONICAL_INTAKE_REQUIRED'])] }; writeReceipt(parsed.output,receipt); process.exit(blocked?1:0);

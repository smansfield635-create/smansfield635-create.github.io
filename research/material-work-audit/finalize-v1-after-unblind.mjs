#!/usr/bin/env node
const REPO=process.env.GITHUB_REPOSITORY||'smansfield635-create/smansfield635-create.github.io';
const [OWNER,NAME]=REPO.split('/');
const TOKEN=process.env.GITHUB_TOKEN;
const BRANCH='research/material-work-audit-v1-reclassification-ddbe792';
const V1='research/material-work-audit/material-work-audit-v1-classifications.jsonl';
const DIS='research/material-work-audit/material-work-audit-v0-v1-disagreements.jsonl';
const SUM='research/material-work-audit/material-work-audit-v1-summary.json';
const REP='research/material-work-audit/material-work-audit-v1-reproducibility.md';
const EXPECTED=906;
const V0={total:906,PARAMOUNT:143,STANDARD:253,SUPPORT:510,pre:{PARAMOUNT:42,STANDARD:84,SUPPORT:112,total:238},install:{PARAMOUNT:12,STANDARD:26,SUPPORT:169,total:207},mature:{PARAMOUNT:78,STANDARD:127,SUPPORT:193,total:398}};
if(!TOKEN)throw new Error('GITHUB_TOKEN required');
const H={Authorization:`Bearer ${TOKEN}`,Accept:'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28','User-Agent':'material-work-audit-v1-finalizer'};
async function gh(path,init={}){for(let a=0;a<5;a++){const r=await fetch(`https://api.github.com${path}`,{...init,headers:{...H,...(init.headers||{})}});if(r.ok)return r.json();const t=await r.text();if((r.status===403||r.status===429||r.status>=500)&&a<4){await new Promise(x=>setTimeout(x,1200*(a+1)));continue}throw new Error(`${r.status} ${path}: ${t.slice(0,900)}`)}}
async function branchState(){const ref=await gh(`/repos/${OWNER}/${NAME}/git/ref/heads/${BRANCH.replace(/\//g,'%2F')}`);const commit=await gh(`/repos/${OWNER}/${NAME}/git/commits/${ref.object.sha}`);return{sha:ref.object.sha,tree:commit.tree.sha}}
async function treeChild(treeSha,name){const t=await gh(`/repos/${OWNER}/${NAME}/git/trees/${treeSha}`);const e=t.tree.find(x=>x.path===name);if(!e)throw new Error(`tree child missing ${name}`);return e}
async function blobForPath(rootTree,path){let sha=rootTree;const parts=path.split('/');for(let i=0;i<parts.length;i++){const e=await treeChild(sha,parts[i]);if(i===parts.length-1){if(e.type!=='blob')throw new Error('expected blob');return e.sha}if(e.type!=='tree')throw new Error('expected tree');sha=e.sha}throw new Error('unreachable')}
async function readLargeBlob(rootTree,path){const sha=await blobForPath(rootTree,path);const b=await gh(`/repos/${OWNER}/${NAME}/git/blobs/${sha}`);return Buffer.from(b.content.replace(/\n/g,''),b.encoding||'base64').toString('utf8')}
function counts(rows){const c={PARAMOUNT:0,STANDARD:0,SUPPORT:0,total:rows.length};for(const r of rows)c[r.classification]=(c[r.classification]||0)+1;return c}
function date(r){return r.mergedAt.slice(0,10)}
function windowRows(rows,a,b){return rows.filter(r=>date(r)>=a&&date(r)<=b)}
function material(c){return c.PARAMOUNT+c.STANDARD}
function pct(n,d){return d?+(100*n/d).toFixed(4):null}
function rate(n,days){return +(n/days).toFixed(4)}
function marginalBounds(v0,v1){const maxExact=(Math.min(v0.PARAMOUNT,v1.PARAMOUNT)+Math.min(v0.STANDARD,v1.STANDARD)+Math.min(v0.SUPPORT,v1.SUPPORT))/EXPECTED;const minExact=(Math.max(0,v0.PARAMOUNT+v1.PARAMOUNT-EXPECTED)+Math.max(0,v0.STANDARD+v1.STANDARD-EXPECTED)+Math.max(0,v0.SUPPORT+v1.SUPPORT-EXPECTED))/EXPECTED;const maxParamount=(Math.min(v0.PARAMOUNT,v1.PARAMOUNT)+Math.min(EXPECTED-v0.PARAMOUNT,EXPECTED-v1.PARAMOUNT))/EXPECTED;const minParamount=(Math.max(0,v0.PARAMOUNT+v1.PARAMOUNT-EXPECTED)+Math.max(0,(EXPECTED-v0.PARAMOUNT)+(EXPECTED-v1.PARAMOUNT)-EXPECTED))/EXPECTED;return{exactThreeClass:{min:minExact,max:maxExact},paramountBinary:{min:minParamount,max:maxParamount}}}
async function issue2052Evidence(){const issue=await gh(`/repos/${OWNER}/${NAME}/issues/2052`);const comments=await gh(`/repos/${OWNER}/${NAME}/issues/2052/comments?per_page=100`);const all=[issue.body||'',...comments.map(c=>c.body||'')].join('\n\n');const explicitMissing=/remaining research boundary[\s\S]{0,300}persist the per-pr classification dataset/i.test(all)||/preserves the original 906-merge v0 aggregate result/i.test(all);const rowPatterns=(all.match(/(?:#\d+|"pr"\s*:\s*\d+)[^\n]{0,120}\b(?:PARAMOUNT|STANDARD|SUPPORT)\b/g)||[]).length;return{explicitMissing,rowPatterns,commentCount:comments.length,sourceIssue:'https://github.com/'+REPO+'/issues/2052'} }
async function researchDirectory(rootTree){let sha=rootTree;for(const part of ['research','material-work-audit'])sha=(await treeChild(sha,part)).sha;const t=await gh(`/repos/${OWNER}/${NAME}/git/trees/${sha}`);return t.tree.map(x=>x.path).sort()}
async function writeCommit(parentSha,parentTree,files){const entries=[];for(const [path,content] of Object.entries(files)){const b=await gh(`/repos/${OWNER}/${NAME}/git/blobs`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({content,encoding:'utf-8'})});entries.push({path,mode:'100644',type:'blob',sha:b.sha})}const tree=await gh(`/repos/${OWNER}/${NAME}/git/trees`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({base_tree:parentTree,tree:entries})});const commit=await gh(`/repos/${OWNER}/${NAME}/git/commits`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:'Audit v1: finalize unblinded reproducibility outputs',tree:tree.sha,parents:[parentSha]})});await gh(`/repos/${OWNER}/${NAME}/git/refs/heads/${BRANCH.replace(/\//g,'%2F')}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({sha:commit.sha,force:false})});return commit.sha}
const st=await branchState();const raw=await readLargeBlob(st.tree,V1);const rows=raw.trim().split('\n').filter(Boolean).map(JSON.parse);if(rows.length!==EXPECTED||new Set(rows.map(r=>r.pr)).size!==EXPECTED)throw new Error('v1 ledger integrity failed');
const v1=counts(rows),pre=counts(windowRows(rows,'2026-07-25','2026-08-03')),install=counts(windowRows(rows,'2026-08-04','2026-08-13')),mature=counts(windowRows(rows,'2026-08-15','2026-08-24')),aug14=counts(windowRows(rows,'2026-08-14','2026-08-14')),aug25=counts(windowRows(rows,'2026-08-25','2026-08-25'));
const dir=await researchDirectory(st.tree),e2052=await issue2052Evidence();const v0Named=dir.filter(x=>/v0|classification.*old|audit.*classification/i.test(x)&&x!==V1.split('/').pop());const rowV0Recoverable=v0Named.length>0||e2052.rowPatterns>=EXPECTED;
if(rowV0Recoverable)throw new Error(`Unexpected row-level v0 candidate found; requires explicit recovery review: files=${v0Named.join(',')} patterns=${e2052.rowPatterns}`);
const bounds=marginalBounds(V0,v1),exactTarget=.90,paramTarget=.85;const exactStatus=bounds.exactThreeClass.max<exactTarget?'FAIL_CLOSED_FROM_MARGINAL_UPPER_BOUND':'UNEVALUABLE_PAIRING_MISSING',paramStatus=bounds.paramountBinary.max<paramTarget?'FAIL_CLOSED_FROM_MARGINAL_UPPER_BOUND':'UNEVALUABLE_PAIRING_MISSING';const overall=(exactStatus.startsWith('FAIL')||paramStatus.startsWith('FAIL'))?'REPRODUCIBILITY_FAIL_CLOSED':'REPRODUCIBILITY_UNEVALUABLE_ROW_PAIRING_MISSING';
const disagreements=rows.map(r=>JSON.stringify({schema:'MATERIAL_WORK_AUDIT_V0_V1_COMPARISON_ROW_v1',pr:r.pr,title:r.title,mergedAt:r.mergedAt,v0Classification:'UNRECOVERABLE_ROW_LEVEL_V0',v1Classification:r.classification,exactThreeClassAgreement:'UNEVALUABLE',paramountBinaryAgreement:'UNEVALUABLE',reason:'V0_WAS_PERSISTED_ONLY_AS_AGGREGATES_BEFORE_V1_REAUDIT',v0Evidence:{issue2052AggregateOnly:true,rowLevelRecordFound:false},v1Evidence:{ledgerCommit:st.sha,ruleVersion:r.ruleVersion}})).join('\n')+'\n';
const summary={schema:'MATERIAL_WORK_AUDIT_V1_SUMMARY_v1',population:{count:EXPECTED,cutoff:'2026-08-25T18:25:55Z',uniquePrs:true},v1:{totals:v1,material:material(v1),materialPct:pct(material(v1),EXPECTED),windows:{pre:{dates:'2026-07-25..2026-08-03',days:10,...pre,material:material(pre),materialPerDay:rate(material(pre),10)},install:{dates:'2026-08-04..2026-08-13',days:10,...install,material:material(install),materialPerDay:rate(material(install),10)},aug14:{dates:'2026-08-14',...aug14},mature:{dates:'2026-08-15..2026-08-24',days:10,...mature,material:material(mature),materialPerDay:rate(material(mature),10)},aug25Cutoff:{dates:'2026-08-25T00:00:00Z..18:25:55Z',...aug25}}},v0:{source:'issue #2052 aggregate result + material-work-audit-v1.md',totals:{PARAMOUNT:143,STANDARD:253,SUPPORT:510,total:906},material:396,windows:{pre:V0.pre,install:V0.install,mature:V0.mature},rowLevelRecord:'NOT_PERSISTED'},aggregateDelta:{PARAMOUNT:v1.PARAMOUNT-V0.PARAMOUNT,STANDARD:v1.STANDARD-V0.STANDARD,SUPPORT:v1.SUPPORT-V0.SUPPORT,material:material(v1)-396},reproducibility:{overall,exactThreeClass:{target:exactTarget,status:exactStatus,marginalBounds:bounds.exactThreeClass},paramountBinary:{target:paramTarget,status:paramStatus,marginalBounds:bounds.paramountBinary},rowLevelPairingAvailable:false},transition:{v1InstallVsPreMaterialRatePct:+((rate(material(install),10)/rate(material(pre),10)-1)*100).toFixed(2),v1MatureVsPreMaterialRatePct:+((rate(material(mature),10)/rate(material(pre),10)-1)*100).toFixed(2)},provenance:{v1FrozenCommit:st.sha,v1LedgerPath:V1,v0Issue:2052,v0RowLevelRecoveryEvidence:e2052,researchDirectoryAtFreeze:dir,incident:'main briefly advanced through accidental placeholder cab6c3d31f200d79f66fc82878a44ed020cee9c3 and was immediately removed by 52915a8082255a319670b744eeae15e86fe7b049; net audit artifact content unchanged; ancestry event preserved.'},claimCeiling:'No world-record or causal-productivity claim is authorized by this audit.'};
const md=`# Material-work audit v1 reproducibility result\n\n## Disposition\n\n**${overall}**\n\nThe independent v1 pass completed **906/906** rows and was frozen at commit \`${st.sha}\` before any v0 inspection. After that freeze, the repository record was unblinded. Issue #2052 preserves the v0 aggregate totals but explicitly identifies persistence of the per-PR dataset as remaining work; no durable 906-row v0 classification record exists in the audit directory or issue record. Therefore row-paired agreement must not be fabricated.\n\n## v1 result\n\n| Class | v1 | v0 aggregate | Delta |\n|---|---:|---:|---:|\n| PARAMOUNT | ${v1.PARAMOUNT} | ${V0.PARAMOUNT} | ${v1.PARAMOUNT-V0.PARAMOUNT>=0?'+':''}${v1.PARAMOUNT-V0.PARAMOUNT} |\n| STANDARD | ${v1.STANDARD} | ${V0.STANDARD} | ${v1.STANDARD-V0.STANDARD>=0?'+':''}${v1.STANDARD-V0.STANDARD} |\n| SUPPORT | ${v1.SUPPORT} | ${V0.SUPPORT} | ${v1.SUPPORT-V0.SUPPORT>=0?'+':''}${v1.SUPPORT-V0.SUPPORT} |\n| Material | ${material(v1)} | 396 | ${material(v1)-396>=0?'+':''}${material(v1)-396} |\n| Total | 906 | 906 | 0 |\n\n## Reproducibility thresholds\n\nThe frozen targets were 90% exact three-class agreement and 85% PARAMOUNT/non-PARAMOUNT agreement. Exact paired values are unavailable because v0 row identities were never persisted. Marginal totals still impose rigorous bounds:\n\n- exact three-class agreement can be at most **${(100*bounds.exactThreeClass.max).toFixed(2)}%** and at least **${(100*bounds.exactThreeClass.min).toFixed(2)}%**; threshold status: **${exactStatus}**;\n- PARAMOUNT/non-PARAMOUNT agreement can be at most **${(100*bounds.paramountBinary.max).toFixed(2)}%** and at least **${(100*bounds.paramountBinary.min).toFixed(2)}%**; threshold status: **${paramStatus}**.\n\nIf an upper bound is below its frozen target, that criterion fails regardless of the missing pairing. Otherwise that criterion remains unevaluable until a genuine historical row-level v0 record is recovered. The disagreement ledger deliberately records every row as \`UNRECOVERABLE_ROW_LEVEL_V0\`; it does not reverse-engineer labels from aggregate counts.\n\n## Equal-window v1 recomputation\n\n- Pre-transition 2026-07-25..2026-08-03: ${pre.PARAMOUNT} PARAMOUNT + ${pre.STANDARD} STANDARD + ${pre.SUPPORT} SUPPORT = ${pre.total}; material ${material(pre)} (${rate(material(pre),10)}/day).\n- Installation 2026-08-04..2026-08-13: ${install.PARAMOUNT} PARAMOUNT + ${install.STANDARD} STANDARD + ${install.SUPPORT} SUPPORT = ${install.total}; material ${material(install)} (${rate(material(install),10)}/day).\n- Mature 2026-08-15..2026-08-24: ${mature.PARAMOUNT} PARAMOUNT + ${mature.STANDARD} STANDARD + ${mature.SUPPORT} SUPPORT = ${mature.total}; material ${material(mature)} (${rate(material(mature),10)}/day).\n\nInstallation vs pre material-rate change under v1: **${summary.transition.v1InstallVsPreMaterialRatePct}%**. Mature vs pre: **${summary.transition.v1MatureVsPreMaterialRatePct}%**. These are repository-description/diff-derived Layer-A results; they do not establish causal productivity or operator-attention efficiency.\n\n## Methodological consequence\n\nThe experiment successfully produced an independent, repository-evidence-based v1 ledger. The reproducibility comparison is constrained by a v0 evidence-custody failure: the first audit preserved aggregate totals but not the row-level labels needed for inter-pass pairing. This is itself a reproducibility finding and must remain visible rather than being repaired by inference.\n\nThe accidental placeholder ancestry event \`cab6c3d31f200d79f66fc82878a44ed020cee9c3\` followed by removal at \`52915a8082255a319670b744eeae15e86fe7b049\` remains recorded; net audit artifact content was unchanged.\n\n## Claim ceiling\n\nNo world-record, causal-productivity, or externally benchmarked operator-efficiency claim is authorized by this audit.\n`;
const commit=await writeCommit(st.sha,st.tree,{[DIS]:disagreements,[SUM]:JSON.stringify(summary,null,2)+'\n',[REP]:md});console.log(`FINALIZED_OUTPUTS commit=${commit}`);console.log(`V1_COUNTS P=${v1.PARAMOUNT} S=${v1.STANDARD} U=${v1.SUPPORT} MATERIAL=${material(v1)}`);console.log(`REPRODUCIBILITY ${overall} exactMax=${(100*bounds.exactThreeClass.max).toFixed(2)} paramountMax=${(100*bounds.paramountBinary.max).toFixed(2)}`);

(()=>{'use strict';const root=document.documentElement;const buttons=[...document.querySelectorAll('[data-lens-button]')];const panels=[...document.querySelectorAll('[data-lens-panel]')];const key='dgb-battery-lens';function setLens(lens,focus=false){if(!['platform','engineering','evidence'].includes(lens))lens='platform';root.dataset.activeLens=lens;buttons.forEach((button)=>{const active=button.dataset.lensButton===lens;button.setAttribute('aria-selected',String(active));button.tabIndex=active?0:-1;if(active&&focus)button.focus();});panels.forEach((panel)=>{panel.hidden=panel.dataset.lensPanel!==lens;});try{sessionStorage.setItem(key,lens);}catch{}}buttons.forEach((button,index)=>{button.addEventListener('click',()=>setLens(button.dataset.lensButton));button.addEventListener('keydown',(event)=>{if(!['ArrowLeft','ArrowRight','Home','End'].includes(event.key))return;event.preventDefault();let next=index;if(event.key==='ArrowRight')next=(index+1)%buttons.length;if(event.key==='ArrowLeft')next=(index-1+buttons.length)%buttons.length;if(event.key==='Home')next=0;if(event.key==='End')next=buttons.length-1;setLens(buttons[next].dataset.lensButton,true);});});let initial='platform';try{initial=sessionStorage.getItem(key)||initial;}catch{}setLens(initial);root.dataset.workflowReady='true';})();
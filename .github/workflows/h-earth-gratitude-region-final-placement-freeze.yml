name: H-Earth Gratitude Region final-placement repository admission
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary workflow disabled until bounded admission execution is armed."

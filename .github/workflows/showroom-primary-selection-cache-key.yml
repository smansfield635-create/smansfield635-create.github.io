name: Showroom primary selection cache key

on:
  push:
    paths:
      - ".github/workflows/showroom-primary-selection-cache-key.yml"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Advance bounded Showroom crystal cache key
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          html = Path("showroom/index.html")
          old = "/showroom/index.crystals.js?v=SHOWROOM_SPHERICAL_DEPTH_LABELS_GLOW_v1"
          new = "/showroom/index.crystals.js?v=SHOWROOM_PRIMARY_SELECTION_CLUSTER_SPACING_v2"

          text = html.read_text(encoding="utf-8")
          count = text.count(old)
          if count != 1:
              raise SystemExit(f"Expected exactly one cache-key target; found {count}.")

          html.write_text(text.replace(old, new), encoding="utf-8")
          Path(".github/workflows/showroom-primary-selection-cache-key.yml").unlink()
          PY

          node --check showroom/index.crystals.js
          node --check showroom/index.selection.js

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add showroom/index.html .github/workflows/showroom-primary-selection-cache-key.yml
          git commit -m "Advance Showroom primary selection cache key"
          git push origin HEAD:main

# Compass–Tests Architecture Binding v1

Operation: `COMPASS_TESTS_ARCHITECTURE_BINDING_v1`

Status: `FROZEN_ADMISSION_BINDING_CANDIDATE`

This package is a nonpublic admission candidate. It does **not** construct an integrated Tests manifestation and does not grant construction, public-route, scientific-state, or claim-upgrade authority.

## Governing separation

`CURRENTNESS != MECHANICAL_SUITABILITY != ADMISSION`

A donor crosses the boundary only as the smallest practical mechanical transfer unit. Donor-specific semantics are stripped before destination binding:

`T_donor : (M_mechanical, S_donor) -> (M_mechanical, EMPTY)`

`M_mechanical + S_Tests -> P_Tests`

The Tests destination owns object identity, class, standing, relation meaning, containment, and operation meaning. Compass mechanics own navigation/presentation state only.

## Binding

`C(D,R,N) -> P`

- `D`: authorized object identities, classes, standing.
- `R`: authorized relation structure.
- `N`: Compass navigation state.
- `P`: spatial manifestation.

Navigation or presentation changes may not mutate `D`, `R`, or scientific authority. Presentation geometry may not manufacture semantic structure.

## Admission result

All nine required capability records are resolved as `ADMIT_WITH_ADAPTER`. This closes the donor map **only if this candidate is separately reviewed/promoted**. It does not authorize construction.

Primary admitted mechanical donors are the current Main Compass controller, Main Compass crystal interaction/projection mechanics, the page-agnostic upstream Compass renderer, and one bounded Laws Compass depth-transition/state-machine unit. Universal Compass remains historical-reference-only and is withheld. Showroom, Archcoin, and H-Earth are withheld from v1 because no unique required capability remains.

The current Laws `TEST`, `RESEARCH`, `METHODS & MODELS`, and `RESULTS & RECORDS` identities are not reclassified into the new Tests ontology by label similarity. Laws `TEST` remains an existing Laws route/epistemic-law object unless a later authority explicitly binds otherwise.

## Authorized by this candidate after promotion

Only L-1 orientation, L0, L1, FOCUS, Compass entry-context preservation, and Compass return-context restoration are within the binding scope.

L2 INSPECT, L3 FOLLOW/provenance traversal, L4 ENTER, M1R2 entry, broad selector population, public promotion, and any scientific mutation remain withheld.

## Stop boundary

`NO INTEGRATED MANIFESTATION CONSTRUCTION UNTIL DONOR MAP + BINDING CONTRACT + PER-DONOR DO-NOT-INHERIT LEDGER ARE CLOSED AND A SEPARATE INTEGRATED-M1 CONSTRUCTION AUTHORITY EXISTS.`

#!/usr/bin/env node
const REPO=process.env.GITHUB_REPOSITORY||'smansfield635-create/smansfield635-create.github.io';
const [OWNER,NAME]=REPO.split('/');
const TOKEN=process.env.GITHUB_TOKEN;
const OUTPUT_BRANCH='research/material-work-audit-v1-reclassification-ddbe792';
const OUTPUT_PATH='research/material-work-audit/material-work-audit-v1-classifications.jsonl';
const BASE_SHA='ddbe7924907d275bb7858a581da0d88f371021fe';
const FROZEN_QUERY=`repo:${REPO} is:pr is:merged merged:2026-07-25T00:00:00Z..2026-08-25T18:25:55Z`;
const EXPECTED=906;
const RULE_VERSION='MATERIAL_WORK_AUDIT_V1_DETERMINISTIC_CLASSIFIER_20260825_2';
if(!TOKEN) throw new Error('GITHUB_TOKEN required');
const headers={Authorization:`Bearer ${TOKEN}`,Accept:'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28','User-Agent':'material-work-audit-v1-carrier'};
async function gh(path,init={}){for(let a=0;a<6;a++){const r=await fetch(`https://api.github.com${path}`,{...init,headers:{...headers,...(init.headers||{})}});if(r.ok)return r.json();const t=await r.text();if((r.status===403||r.status===429||r.status>=500)&&a<5){await new Promise(x=>setTimeout(x,1800*(a+1)));continue;}throw new Error(`${r.status} ${path}: ${t.slice(0,800)}`)}}
const uniq=xs=>[...new Set(xs.filter(Boolean))], lc=x=>(x||'').toLowerCase();
function surfaceFor(path){const p=path.replace(/^\//,'');if(p==='index.html'||p.startsWith('assets/compass/'))return'COMPASS_ROOT';if(p.startsWith('laws/'))return'LAWS';if(p.startsWith('evidence/'))return'EVIDENCE';if(p.startsWith('developer/'))return'DEVELOPER';if(p.startsWith('governance/'))return'GOVERNANCE';if(p.startsWith('products/'))return'PRODUCTS';if(p.startsWith('campaigns/'))return'CAMPAIGNS';if(p.startsWith('showroom/globe/audralia/'))return'AUDRALIA';if(p.startsWith('showroom/globe/h-earth/'))return'H_EARTH';if(p.startsWith('showroom/globe/hearth/'))return'HEARTH';if(p.startsWith('showroom/'))return'SHOWROOM';if(p.startsWith('instruments/'))return'INSTRUMENTS';if(p.startsWith('inspection/'))return'INSPECTION';if(p.startsWith('preview/'))return'PREVIEW';return null}
function isEvidencePath(p){const s=lc(p);return s.startsWith('.github/')||s.startsWith('verification/')||s.startsWith('scripts/')||s.startsWith('docs/')||s.startsWith('research/')||s.includes('/validation/')||s.includes('/registry/')||s.includes('receipt')||s.includes('manifest')||s.includes('evidence-artifact')||s.includes('publication-surfaces/')||s.endsWith('.md')||s.includes('benchmark')||s.includes('verifier')||s.includes('self-test')}
function isControlPath(p){const s=lc(p);return s.startsWith('.github/')||s.startsWith('tools/')||s.startsWith('verification/')||s.startsWith('scripts/')||s.startsWith('infrastructure/')||s.includes('/control-plane/')||s.includes('/registry/')||s.includes('/validation/')||s.startsWith('docs/')||s.startsWith('research/')}
function isPureSupportTitle(title){return /^(revert|verification-only|verify\b|register\b|router:\s*register|release:\s*register|document\b|record\b|persist\b|expose exact|publish exact|publish immutable|deploy exact|update .*publish|repair .*manifest|.*verification manifest|.*verifier.*repair|fix .*workflow|repair .*workflow|trigger\b)/.test(lc(title).trim())}
function reusableCapability(text){return /(install|introduc|establish|create|add|generalize|enforce|adopt|compile|build)/.test(text)&&/(reusable|shared|universal|compiler|engine|protocol|builder|gateway|capabilit|framework|kernel|control plane|control-plane|system|pipeline|execution packet|working set|sparse-index|publication preflight|entitlement)/.test(text)&&!/(only.*register|registration only|manifest repair only|verifier-only|verification-only|receipt only|documentation-only)/.test(text)}
function explicitQualified(text){return /(qualification|benchmark|validation|preflight|harness|browser|matrix|workflow)[\s\S]{0,160}\b(pass|success|green|qualified|pass_closed)\b/.test(text)||/\b(pass_closed|qualified candidate|exact[- ]candidate qualification.*pass|assertions.*pass)\b/.test(text)}
function explicitLive(text){return /live_exact_head_verified/.test(text)||/(deployed|public|live)[\s\S]{0,100}(exact (identity|head)|byte identity|runtime verification|browser proof)[\s\S]{0,100}\b(pass|verified|success)\b/.test(text)||/\blive[- ]browser[^.\n]{0,120}\bpass\b/.test(text)}
function verificationBreadth(text){const gs=[/desktop|wide|1280|1440/,/tablet|ipad|1024|820|694/,/phone|mobile|samsung|390|412|430|360/,/landscape/,/portrait/,/keyboard/,/touch|pointer/,/no-js|no js|reduced-motion/,/cold[- ]cache|reload|refresh/,/day|dawn|night/];let n=0;for(const r of gs)if(r.test(text))n++;const m=text.match(/(?:profiles|fixtures|configurations|devices|states|viewports)\s*[:=]?\s*`?(\d+)/);if(m)n=Math.max(n,+m[1]);if(/phone[^\n.]{0,80}tablet[^\n.]{0,80}desktop|desktop[^\n.]{0,80}tablet[^\n.]{0,80}phone/.test(text))n=Math.max(n,3);return n}
function classify(pr,files){const title=pr.title||'',body=pr.body||'',text=lc(`${title}\n${body}`),paths=files.map(f=>f.filename),changedPaths=paths.length||pr.changed_files||0,materialPaths=paths.filter(p=>!isEvidencePath(p)),surfaces=uniq(materialPaths.map(surfaceFor).filter(s=>s&&!['INSPECTION','PREVIEW'].includes(s))),hasPublicMaterial=surfaces.length>0||materialPaths.some(p=>/^(assets\/|index\.html$)/.test(p)),allControlOrEvidence=paths.length>0&&paths.every(p=>isControlPath(p)||isEvidencePath(p)||['INSPECTION','PREVIEW'].includes(surfaceFor(p)));
const revert=/^revert\b/.test(lc(title))||/emergency rollback|exact inverse scope|fully reverted|restoration-only rollback/.test(text),registrationOnly=/(registration only|registers? .* only|routing-only prerequisite|registry-only prerequisite|scope is .*router|exact-path registration|publication surface registration)/.test(text)&&!hasPublicMaterial,verifierOnly=/(verifier-only|verification-only|diagnostic-only boundary|instrumentation repair|changes only .*verifier|qualification.*only)/.test(text)&&!hasPublicMaterial,manifestOnly=/(manifest repair|verification-manifest|publication manifest|cache identity|cache-key|version identity|fingerprint stamper)/.test(text)&&materialPaths.length===0,publicationTransport=/(publication|deployment|pages|publish)/.test(text)&&/(transport|carrier|registration only|inspection route|snapshot|no product|product.*unchanged|no .*product.*bytes)/.test(text)&&!hasPublicMaterial,exactBindingOnly=/(exact identity|identity correction|binding correction|contract identit|cache identity|request identity)/.test(text)&&/(no material behavior|semantics.*unchanged|behavior.*unchanged|product.*unchanged|runtime.*unchanged)/.test(text),pureSupport=revert||registrationOnly||verifierOnly||manifestOnly||publicationTransport||exactBindingOnly||(isPureSupportTitle(title)&&allControlOrEvidence),reusable=reusableCapability(text),materialBehavior=hasPublicMaterial&&/(add|replace|restore|repair|rebuild|recompose|integrat|change|correct|mount|render|interaction|runtime|geometry|cloud|weather|carousel|navigation|presentation|environment|content|startup|loader|condition|brain|terrain|camera|gesture)/.test(text),material=materialBehavior||reusable||(!pureSupport&&materialPaths.length>0);
let klass,trace=[];if(pureSupport&&!reusable){klass='SUPPORT';trace.push('SUPPORT_EXCLUSION_GATE')}else if(!material){klass='SUPPORT';trace.push('NO_POSITIVE_MATERIALITY_PREDICATE')}else{klass='STANDARD';trace.push(reusable&&!hasPublicMaterial?'REUSABLE_ENGINEERING_EXCEPTION_TO_SUPPORT':'STANDARD_MATERIALITY_GATE')}
const qualified=explicitQualified(text),live=explicitLive(text),breadth=verificationBreadth(text),multiSurface=surfaces.length>=2||(/(multiple public surfaces|across .*developer.*evidence.*governance|whole-site|site-level|four-domain|multi-route|multiple routes)/.test(text)&&material),namedArchitecture=/(\b[A-Z][A-Z0-9_]{5,}_v\d\b|reusable architecture|shared .*runtime|shared .*engine|universal .*authority|system-level|compiler|kernel|protocol|entitlement engine|composite authority|execution packet)/.test(`${title}\n${body}`),integrationBoundary=multiSurface||/(material integration|integrat(?:e|es|ed|ion) .*authority|independent authority domains|system-level|major multi-stage|whole-site|site-level|shared implementation|canonical .*integration|composite|end-to-end)/.test(text);let broad=0;const br=[];if(multiSurface){broad++;br.push('MULTI_SURFACE')}if(materialPaths.length>=5){broad++;br.push('MATERIAL_PATHS_GE_5')}if(breadth>=3&&qualified){broad++;br.push('VERIFICATION_BREADTH_GE_3')}if(live){broad++;br.push('INDEPENDENT_LIVE_EVIDENCE')}if(namedArchitecture){broad++;br.push('NAMED_REUSABLE_ARCHITECTURE')}if(klass==='STANDARD'&&integrationBoundary&&broad>=2){klass='PARAMOUNT';trace.push('PARAMOUNT_BOUNDARY_CROSSING_AND_BROAD_CONSEQUENCE')}
const autonomous=/(automatic|autonomous|auto-release|on merge|push-triggered)[\s\S]{0,180}(qualification|merge|deploy|publication|live)/.test(text)&&live?true:'UNRESOLVED';return{schema:'MATERIAL_WORK_AUDIT_V1_CLASSIFICATION_ROW_v1',ruleVersion:RULE_VERSION,pr:pr.number,title,mergedAt:pr.merged_at,mergeCommitSha:pr.merge_commit_sha,classification:klass,flags:{qualified,live,multiSurface,autonomousEndToEnd:autonomous,changedPaths,materialChangedPaths:materialPaths.length,affectedSurfaces:surfaces.length||'UNRESOLVED',surfaces,verificationBreadth:{distinctSignals:breadth}},evidence:{repositoryUrl:pr.html_url,changedPathSample:paths.slice(0,12),additions:pr.additions,deletions:pr.deletions,supportPredicates:{revert,registrationOnly,verifierOnly,manifestOnly,publicationTransport,exactBindingOnly},reusableCapability:reusable,materialBehavior,paramount:{integrationBoundary,broadConsequenceCount:broad,broadReasons:br},decisionTrace:trace}}}
async function mapLimit(items,limit,fn){const out=new Array(items.length);let next=0;async function worker(){for(;;){const i=next++;if(i>=items.length)return;out[i]=await fn(items[i],i)}}await Promise.all(Array.from({length:limit},worker));return out}
async function listPopulation(){const items=[];for(let page=1;page<=10;page++){const d=await gh(`/search/issues?q=${encodeURIComponent(FROZEN_QUERY)}&sort=created&order=asc&per_page=100&page=${page}`);if(page===1&&d.total_count!==EXPECTED)throw new Error(`Population drift ${d.total_count}`);items.push(...d.items);if(d.items.length<100)break}if(items.length!==EXPECTED||new Set(items.map(x=>x.number)).size!==EXPECTED)throw new Error(`population integrity ${items.length}`);return items.map(x=>x.number)}
async function fetchPR(number){const pr=await gh(`/repos/${OWNER}/${NAME}/pulls/${number}`),files=[];for(let page=1;page<=30;page++){const part=await gh(`/repos/${OWNER}/${NAME}/pulls/${number}/files?per_page=100&page=${page}`);files.push(...part);if(part.length<100)break}return{pr,files}}
async function writeGitData(content){const ref=await gh(`/repos/${OWNER}/${NAME}/git/ref/heads/${OUTPUT_BRANCH.replace(/\//g,'%2F')}`);if(ref.object.sha!==BASE_SHA)throw new Error(`Official audit branch moved before v1 freeze: ${ref.object.sha}`);const parent=await gh(`/repos/${OWNER}/${NAME}/git/commits/${ref.object.sha}`);const blob=await gh(`/repos/${OWNER}/${NAME}/git/blobs`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({content,encoding:'utf-8'})});const tree=await gh(`/repos/${OWNER}/${NAME}/git/trees`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({base_tree:parent.tree.sha,tree:[{path:OUTPUT_PATH,mode:'100644',type:'blob',sha:blob.sha}]})});const commit=await gh(`/repos/${OWNER}/${NAME}/git/commits`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:`Audit v1: freeze blinded ${EXPECTED}-row classification ledger`,tree:tree.sha,parents:[ref.object.sha]})});await gh(`/repos/${OWNER}/${NAME}/git/refs/heads/${OUTPUT_BRANCH.replace(/\//g,'%2F')}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({sha:commit.sha,force:false})});return{commit:commit.sha,blob:blob.sha}}
const nums=await listPopulation();console.log(`Frozen population reconciled: ${nums.length}`);const fetched=await mapLimit(nums,6,async(n,i)=>{if(i%50===0)console.log(`fetch ${i}/${EXPECTED}`);return fetchPR(n)});const rows=fetched.map(x=>classify(x.pr,x.files)).sort((a,b)=>Date.parse(a.mergedAt)-Date.parse(b.mergedAt)||a.pr-b.pr);if(rows.length!==EXPECTED||new Set(rows.map(r=>r.pr)).size!==EXPECTED)throw new Error('ledger integrity');const jsonl=rows.map(r=>JSON.stringify(r)).join('\n')+'\n';console.log(`v1 row integrity PASS bytes=${Buffer.byteLength(jsonl)} rows=${rows.length}`);const wr=await writeGitData(jsonl);console.log(`FROZEN_V1_DATASET commit=${wr.commit} blob=${wr.blob}`);

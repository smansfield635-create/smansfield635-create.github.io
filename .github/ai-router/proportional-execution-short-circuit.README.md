# Proportional execution short-circuit

This repair reconnects existing proportional-routing precedents instead of adding another execution layer.

For `STATIC_EDITORIAL_MICRO`, the blocking route is exactly:

`PROJECT_ROUTE -> EXACT_DIFF -> STATIC_EDITORIAL_VERIFIER`

Canonical intake, Canonical Packet Compiler, resume reconstruction, Page Excellence, agent orchestration, and transport research are not applicable project phases for this class.

Write transport remains an internal execution concern. Use the first already-authorized capability that safely supports the declared path and exact-head semantics. Whole-file replacement is allowed when complete materialization is safe; a patch primitive is not mandatory. If a preferred writer is incapable, try the next already-authorized writer without changing scope or authority. Exhaustion of safe writers blocks execution only; it does not create a product failure, successor generation, readmission requirement, or new transport implementation mandate.

Governed runtime/authority work remains on canonical intake and its selected qualification route. Ambiguity remains fail-closed.

name: Laws CP4 Live Projection Priority PR Applicator Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Live projection priority correction is committed."

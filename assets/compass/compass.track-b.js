(()=>{
'use strict';
const CONTRACT='COMPASS_TRACK_B_UPPER_PRESENTATION_v7';
if(globalThis.__DGB_TRACK_B?.contract===CONTRACT)return;
const qs=(s,r=document)=>r.querySelector(s);
const textNodes=(root=document)=>[...root.querySelectorAll('h1,h2,h3,h4,p,strong,span,summary,a')];
const exactText=(text,root=document)=>textNodes(root).find(n=>n.textContent.trim()===text);
const includesText=(text,root=document)=>textNodes(root).find(n=>n.textContent.includes(text));
function majorBlock(node){if(!node)return null;let cur=node;while(cur&&cur!==document.body){if(/^(SECTION|ARTICLE|ASIDE)$/.test(cur.tagName))return cur;cur=cur.parentElement}cur=node;while(cur&&cur!==document.body){if(cur.tagName==='DIV')return cur;cur=cur.parentElement}return node.parentElement}
function proseBlock(node){if(!node)return null;let cur=node;while(cur&&cur!==document.body){const text=cur.textContent||'';if(/^(DETAILS|ARTICLE|ASIDE|DIV)$/.test(cur.tagName)&&text.includes('Why Diamond Gate exists')&&cur.querySelectorAll('p').length>=2&&!cur.querySelector('[data-compass-scene]')&&!cur.querySelector('[data-compass-root]'))return cur;cur=cur.parentElement}return node.parentElement}
function ensureCss(){if(qs('link[data-track-b-style]'))return;const l=document.createElement('link');l.rel='stylesheet';l.href='/assets/compass/compass.track-b.css?v=track-b-v7';l.dataset.trackBStyle='true';document.head.append(l)}
function brandHeader(){const title=exactText('The Compass');const header=qs('.compass-estate__header')||qs('header')||majorBlock(title);if(!header)return null;if(!qs('.compass-track-b-brand',header)){const b=document.createElement('div');b.className='compass-track-b-brand';b.textContent='DiamondGateBridge.com';header.prepend(b)}return header}
function ensureIntroShell(header){let outer=qs('.compass-track-b-intro');if(outer||!header)return outer;outer=document.createElement('details');outer.className='compass-track-b-intro';outer.dataset.trackBIntroduction='true';const summary=document.createElement('summary');summary.textContent='New here? Open the introduction.';const body=document.createElement('div');body.className='compass-track-b-intro__body';const film=document.createElement('div');film.className='compass-track-b-film';const video=document.createElement('video');video.controls=true;video.preload='metadata';video.playsInline=true;video.setAttribute('aria-label','Diamond Gate Bridge — Chapter One');const source=document.createElement('source');source.src='/showroom/globe/h-earth/awards/media/diamond-gate-compass-mirrorland-36s.mp4';source.type='video/mp4';video.append(source);film.append(video);const about=document.createElement('details');about.className='compass-track-b-about';const aboutSummary=document.createElement('summary');aboutSummary.textContent='What is Diamond Gate Bridge?';const copy=document.createElement('div');copy.className='compass-track-b-about-copy';copy.dataset.trackBAboutCopy='true';copy.innerHTML=`<p>Diamond Gate exists because useful ideas are often separated by the boundaries between disciplines, products, institutions, and stories. The estate gives those different kinds of work one place to meet without pretending they are all the same thing.</p><p>It connects research, software, worlds, evidence, governance, construction, and human questions so you can move between them while keeping their differences visible.</p><p>For thousands of years, people have searched for better ways to understand themselves, each other, and the systems they inhabit. Philosophers questioned first principles. Each discipline illuminated part of the landscape.</p><p>Perhaps the next step is not abandoning what came before, but making those pieces easier to see together. The Earth did not become round when we discovered it. What if the next frontier is not outside the box, but outside the cube? Diamond Gate Bridge begins with that question.</p><p>The Compass is how you orient yourself inside that larger purpose. North gives context. East opens worlds. South measures and governs. West builds and tests.</p><p>Bring a question. Move through the estate until the right kind of room becomes clear.</p>`;about.append(aboutSummary,copy);body.append(film,about);outer.append(summary,body);header.after(outer);return outer}
function removeLegacyWhy(){const heading=exactText('Why Diamond Gate exists')||includesText('Why Diamond Gate exists');if(!heading||qs('.compass-track-b-intro')?.contains(heading))return true;const block=proseBlock(heading);if(block&&block!==document.body&&!block.querySelector('[data-compass-scene]')&&!block.querySelector('[data-compass-root]'))block.remove();else heading.remove();return true}
function findBuilt(){return majorBlock(exactText('Built as one connected system.')||includesText('Built as one connected system.')||exactText('Software TRL 7')||includesText('Software TRL 7'))}
function findBuildCta(){return majorBlock(exactText('Build Your Own Custom Site')||includesText('Build Your Own Custom Site')||exactText('Build something of your own')||includesText('Build something of your own'))}
function relocateCarousel(){const orbit=qs('[data-capability-orbit]');if(!orbit)return false;const built=findBuilt(),build=findBuildCta(),cue=exactText('Three ways to engage.')||includesText('Three ways to engage.');if(!built||orbit===built||built.contains(orbit)||orbit.contains(built))return false;orbit.dataset.trackBCapabilityBlock='true';orbit.classList.add('compass-track-b-relocated-orbit');built.dataset.trackBBuiltEvidence='true';if(build)build.dataset.trackBBuildCta='true';if(build&&build!==built&&!built.contains(build)&&build!==orbit&&!orbit.contains(build)){built.after(build);if(cue&&cue!==orbit&&!orbit.contains(cue)){build.after(cue);cue.after(orbit)}else build.after(orbit)}else if(cue&&cue!==orbit&&!orbit.contains(cue)){built.after(cue);cue.after(orbit)}else built.after(orbit);return true}
function annotateScene(){const scene=qs('[data-compass-scene]');if(scene)scene.dataset.trackBPresentation='context-dominant'}
let attempts=0,timer=0,observer;
function reconcile(){attempts++;ensureCss();const header=brandHeader();ensureIntroShell(header);const introDone=removeLegacyWhy();const orderDone=relocateCarousel();annotateScene();document.documentElement.dataset.trackB='constructed-v7';if(introDone&&orderDone){clearTimeout(timer);observer?.disconnect();return}if(attempts<12){clearTimeout(timer);timer=setTimeout(reconcile,Math.min(160*attempts,800))}}
function init(){globalThis.__DGB_TRACK_B=Object.freeze({contract:CONTRACT,chapterOne:'owner-approved-live-master',trackAFrozen:true});observer=new MutationObserver(()=>{if(attempts<12)reconcile()});observer.observe(document.documentElement,{childList:true,subtree:true});reconcile()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();

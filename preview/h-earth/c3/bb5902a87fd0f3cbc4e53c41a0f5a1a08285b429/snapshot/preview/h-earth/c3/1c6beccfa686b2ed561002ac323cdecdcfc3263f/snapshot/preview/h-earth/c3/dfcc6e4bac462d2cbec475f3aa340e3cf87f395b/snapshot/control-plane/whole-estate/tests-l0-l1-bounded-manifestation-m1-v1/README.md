# Tests L0/L1 bounded manifestation M1

## Status

`BOUNDED_NONPUBLIC_L0_L1_MANIFESTATION_CANDIDATE`

This package is the first nonpublic perceptual manifestation of the promoted Tests L0/L1 navigation authority and promoted Object-Projection Registry.

It does not create ontology, scientific standing, evidence, research custody, a public route, or deep-entry authority.

## Governing identities

- construction base / promoted registry merge: `64d0ea7673d1ecb61372a0dd8b127b889157da1d`
- authority contract blob: `0bfab791c005ca71cee771a5d14d27f8dfab3177`
- registry contract blob: `ed969c87359c42444c477a752f332f07f0059a30`
- promoted registry blob: `b801a11e342051a6b00e6f7098b1d691685ad0b6`

## Candidate identity

`M1_TESTS_METHODS_OPERATIONAL_FIELD`

The candidate manifests only:

- L0 selector controls;
- the admitted `METHODS` projection;
- the three admitted L1 objects;
- the two admitted directional relations;
- bounded `FOCUS` movement as attention movement only.

It deliberately does **not** implement L2 inspection, L3 FOLLOW traversal, L4 ENTER, or M1R2 deep entry.

## Fixed semantic population

Projection:

`METHODS`

Objects:

1. `METHODS` — `METHOD`
2. `ROUTE_OPERATOR_PLATFORM` — `METHOD`
3. `PROSPECTIVE_FINAL_REPORT_PORTFOLIO` — `TEST_INSTANCE`

Relations:

1. `METHODS -> GOVERNS_PROCEDURE_FOR -> PROSPECTIVE_FINAL_REPORT_PORTFOLIO`
2. `ROUTE_OPERATOR_PLATFORM -> EXECUTES -> PROSPECTIVE_FINAL_REPORT_PORTFOLIO`

The manifestation runtime reads those labels, classes, and relations from the promoted registry and fails closed if the registry does not match the frozen bounded population.

## L0 rule

The four L0 projection controls remain visible as stable navigation controls:

- METHODS — active and admitted;
- MODELS — unavailable in this candidate;
- EXPERIMENTS — unavailable in this candidate;
- EVIDENCE — unavailable in this candidate.

The disabled controls do not instantiate objects or populations.

## L1 rule

The three admitted objects remain simultaneously present in one spatial field. One object has perceptual primacy as the active focus while the other two remain spatially retained.

`FOCUS` changes attention only. It does not alter:

- object identity;
- object class;
- standing;
- projection membership;
- declared relations;
- relation direction;
- authority source;
- claim ceiling;
- deep-entry authority.

## Nonsemantic spatiality

The following are presentation-only and must not be interpreted scientifically:

- position;
- adjacency;
- screen distance;
- object scale;
- connector curvature;
- responsive reflow;
- animation or transition timing;
- focus order.

Only the two source-bound directional connectors encode declared scientific/operational relations.

## M1R2 boundary

`M1R2_METHOD_PROCEDURE_FIELD` remains `VALID_DEEP_PROCEDURAL_INSTRUMENT`.

`ENTER` is absent because the promoted registry withholds parent-method binding authority. This candidate does not infer that parent from labels, proximity, prior manifestations, or thread context.

## Mutation boundary

- public mutation: `FALSE`
- public route creation: `FALSE`
- research-content duplication: `FALSE`
- M1R2 mutation: `FALSE`
- T* mutation: `FALSE`
- B* mutation: `FALSE`
- scientific claim upgrade: `FALSE`
- broader MODELS population: `FALSE`
- broader EXPERIMENTS population: `FALSE`
- broader EVIDENCE population: `FALSE`

## Evidence boundary

This package may establish construction/static-conformance evidence for this bounded candidate. It does not establish runtime, accessibility, perceptual, user-differential, public-promotion, or scientific evidence.

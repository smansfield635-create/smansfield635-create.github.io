name: Temporary Awards Edit Recovery Hash
on:
  push:
    branches:
      - agent/awards-h-earth-edited-crop-recovery
jobs:
  hash:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
      - name: Hash awards edit recovery page
        run: sha256sum showroom/globe/h-earth/awards/index.html

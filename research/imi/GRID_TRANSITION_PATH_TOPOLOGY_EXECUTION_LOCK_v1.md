# Execution Lock

Scientific protocol frozen at commit lineage before any generated-state outcome. Deterministic implementation repairs, if required, may only make the frozen procedure executable and may not alter source, seed, state count, switching set size, path score, recovery rules, emergency allowance, challenger, validation split, or PASS thresholds.
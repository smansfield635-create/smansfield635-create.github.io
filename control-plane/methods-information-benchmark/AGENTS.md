# Methods Information Benchmark — pre-role genesis

No permanent Role 1–6 authority is active. The committed user-origin seed in PR #551 is the sole canonical genesis candidate. PR #550 is a noncanonical implementation donor only and may not become a second seed lineage.

Room names, conversation memory, screenshots, summaries, and private working state are nonauthoritative.

The Bootstrap Builder may construct the genesis instruments and materialize inactive role contracts. It may not approve, independently verify, ratify, or activate its own output. Its workflow and command-emitted receipt must prove `EXPECTED_HEAD = EXECUTED_HEAD` and bind the exact changed-path blob map, seed identity, authority files, tools, schemas, benchmark-discovery state, command, and normalized execution class.

The Bootstrap Verifier must execute from a clean exact-head checkout, consume an authentic Builder receipt and declared changed-path blob map, use a distinct execution holder, reproduce the fingerprint, and perform no repair. Missing Builder custody must fail closed; the verifier may not substitute its own fingerprint.

Use `AI_ENTRYPOINT.json`, the repository AI router, this project entrypoint, the fresh-room locator, and the shared bootstrap executable. Stop on noncandidate execution, missing authority, ambiguous benchmark identity, self-activation, incompatible function assignment, private-state dependence, holder collision, seed mismatch, changed-path mismatch, or fingerprint mismatch.

Public Methods, Laws, showroom, renderer, preview, and live product paths are protected.

name: Laws Six-Node CP4 Browser Evidence v2 Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by the two-commit corrected candidate verifier."

# H-Earth Live Experience Maturity Convergence agent entrypoint

This directory is a nonproduct H-Earth control package. It makes the live-experience convergence strategy recoverable and tool-recognizable; it grants no product, renderer, terrain, geography, weather, merge, deployment, release, canonicalization, or registry-activation authority.

Before any successor work:

1. Read `/AI_ENTRYPOINT.json` and `/AGENTS.md`.
2. Read `/h-earth-3d/AGENTS.md`.
3. Read `program.locator.v1.json`, then follow its read order.
4. Re-read current repository `main`; current main outranks this package's historical starting head.
5. Run the repository AI router and H-Earth automatic preflight against every proposed affected path.
6. For any `showroom/` or interface mutation, use the Mandatory Page Excellence entry gate and honor `page-excellence-binding.v1.json`.
7. Resume only from `current-state.v1.json` or from a later exact repository-resident supersession.
8. Never use room-private conversation state as implementation authority.

The package's core law is:

`UNBOX THE EXPERIENCE != REBUILD THE EXPERIENCE`

and:

`RUNTIME PRESERVATION != PRESENTATION PRESERVATION`

No product mutation is authorized by this control package.

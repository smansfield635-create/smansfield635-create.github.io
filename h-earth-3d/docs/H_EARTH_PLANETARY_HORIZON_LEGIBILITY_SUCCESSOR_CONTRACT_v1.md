# H-Earth Planetary Horizon Legibility Successor Contract v1

Operation: `H_EARTH_PLANETARY_HORIZON_LEGIBILITY_SUCCESSOR_20260819_001`

Protected parent: `10d046396d58c7cf7c38c7dad5121acdb8cd197d`

Failure evidence: `23895.mp4`, SHA-256 `4c43943fc0f6ef8f035cdea936704327cabfdf41c0a380da43afaa950dbf976c`.

## Governing diagnosis

Generation 327 established a true full-XYZ spherical continuation and fixed planetary anchor, but bounded geographic representation ends at the lawful 6000-world-unit manifold envelope. From elevated terrain viewpoints the true geometric horizon can lie beyond that distance. The persistent renderer therefore has no physical planetary surface between the bounded representation edge and the geometric horizon and clears that region directly to the sky-horizon color. The mathematical curvature exists, but the visible planet body does not reach the horizon.

## Successor law

The successor adds one fixed spherical **planet-body horizon shell** beneath the bounded land/ocean representation.

The shell:

- begins at the existing 6000-unit world-manifold edge;
- is generated only from the protected Gen327 planetary world frame;
- is slightly below the bounded geographic surface so existing land/ocean remains authoritative where present;
- extends far enough to contain the geometric horizon across the current elevated observer envelope;
- is fixed in world/planet space and cannot recenter around the camera;
- carries no land identity, ocean identity, regional identity, topology classification, navigation address, collision authority, or accessible-region authority;
- exists solely for planetary depth, occlusion, atmospheric-distance blending, and the physical horizon silhouette.

This is not a screen-space curve and does not manufacture geography beyond the canonical manifold.

## Protected architecture

The following remain immutable in this operation:

- Gen311 terrain and regional geography;
- Generation 324/325 terrain-supported locomotion and camera floor;
- Generation 327 `planetary-world-frame.js` and world-representation plan;
- R3A planet-relative camera framing;
- persistent WebGL renderer;
- pointer/touch intake and GPU binding;
- public route and deployment surfaces.

## Acceptance

The successor may pass machine qualification only if:

1. the Gen327 planetary-frame identity and scale remain unchanged;
2. exactly one non-geographic planet-body shell is materialized;
3. shell coordinates are identical across different camera positions;
4. shell inner radius equals the 6000-unit bounded-manifold edge;
5. shell outer radius contains the geometric horizon for observer heights 60, 90, 120, 180, and 240 world units;
6. a low observer whose geometric horizon is inside 6000 remains covered by bounded representation rather than depending on the shell;
7. spherical falloff is materially present and a perspective-curvature signal is nonzero;
8. the shell creates no geography, topology, navigation, collision, or accessible-region authority;
9. the existing Run8E neutral package remains valid and contains the shell;
10. the canonical immutable live GPU package contains the shell in an actual draw range;
11. the one-land/one-ocean bounded continuation contract remains intact;
12. qualification leaves the exact candidate workspace unchanged.

Browser/owner acceptance remains required after machine PASS. The decisive visual criterion is that elevated 360-degree viewing reads as a planetary body falling away into a natural curved horizon while preserving the materially improved Gen327 presentation continuity.

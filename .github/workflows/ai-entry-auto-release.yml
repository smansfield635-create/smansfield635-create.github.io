name: AI Entry — Automatic Exact-Head Release (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Record explicit release architecture
        run: |
          echo 'AI_ENTRY_AUTOMATIC_EXACT_HEAD_RELEASE=RETIRED'
          echo 'Automatic all-surfaces-on-every-push publication is no longer authoritative.'
          echo 'Production publication authority is .github/workflows/pages-exact-head-deploy-v3.yml.'
          echo 'Exact-head publication is explicitly dispatched for one registered surface and its declared dependencies.'
          echo 'This compatibility workflow performs no deployment, no live verification, and creates no publication authority.'

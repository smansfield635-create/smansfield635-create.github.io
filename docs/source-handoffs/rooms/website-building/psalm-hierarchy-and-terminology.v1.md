# PSALM hierarchy and terminology source handoff v1

Status: source handoff only.

## Canonical terminology

Canonical term: `PSALM`.

Do not substitute `SONG`, `SOM`, `SUN`, or `SON` for `PSALM` in this structure.

The use of `songs` in conversation is treated as a rejected or mistaken term unless a later owner-authorized source explicitly reopens the terminology.

## Preserved hierarchy

The preserved hierarchy is:

```text
MK -> MPK -> MP[1-4] -> CP[1-16] -> BS[1-64] -> ST[1-256] -> ISK[1-256]
```

Known expansion:

```text
MK  = Master Key
MPK = Master PSALM Key
MP  = Master PSALM
CP  = Center PSALM
BS  = Branch Seat
ST  = Seat Trace
ISK = Inner Seat Key
```

## Master PSALM meaning

`PSALM` expands as:

```text
Perception
Structure
Alignment
Law
Motion
```

The Master PSALM stands above kernel, runtime, renderer, gauge, route, child file, and implementation. A kernel may execute beneath PSALM authority; a kernel does not replace PSALM.

## 256 field

The retained field is a 256 PSALM field. Fibonacci may be used as lawful progression rhythm inside the preserved 256 field, but Fibonacci does not replace the 256 field and does not replace PSALM.

## Traversal caution

The recovered traversal language includes forward/reverse ladder behavior and anchored triads. Exact original ladder detail was not fully recoverable in this room. Any repository consumer should keep partially recovered traversal content marked as partial until an exact source is recovered.

## Non-authority statement

This file preserves source terminology and hierarchy. It is not executable router code and does not itself authorize implementation changes.

const fixture={job:{id:'v4-test-kitchen',demo:0,status:'Active',customer:'Test Kitchen',project:'Kitchen remodel',insuranceJob:false,approved:0,invoiced:0,paid:0,items:{contract:'wait',changes:'wait',invoice:'wait',payment:'wait',subs:'wait',insurance:'wait'},issues:[]},records:[{type:'Contract',amount:40000,state:'signed'},{type:'Change Order',amount:4500,state:'signed'},{type:'Change Order',amount:2100,state:'open'},{type:'Customer Invoice',amount:25000,state:'open'},{type:'Customer Invoice',amount:12000,state:'open'},{type:'Customer Payment',amount:20000,state:'paid'},{type:'Customer Payment',amount:10000,state:'paid'},{type:'Subcontractor Bill',amount:4800,state:'open',party:'ABC Electric'}],expected:{approved:44500,invoiced:37000,paid:30000,customerBalance:7000,unsignedChanges:2100,tradePayables:4800}};function calculate(records){const sum=f=>records.filter(f).reduce((s,r)=>s+r.amount,0);let approved=sum(r=>r.type==='Contract'&&r.state==='signed')+sum(r=>r.type==='Change Order'&&r.state==='signed'),invoiced=sum(r=>r.type==='Customer Invoice'),paid=sum(r=>r.type==='Customer Payment'),unsignedChanges=sum(r=>r.type==='Change Order'&&r.state!=='signed'),tradePayables=sum(r=>['Subcontractor Bill','Supplier Bill'].includes(r.type)&&r.state!=='paid');return{approved,invoiced,paid,customerBalance:Math.max(0,invoiced-paid),unsignedChanges,tradePayables}}let actual=calculate(fixture.records);let pass=Object.entries(fixture.expected).every(([k,v])=>actual[k]===v);if(!pass)throw new Error('V4_TEST_KITCHEN_FAIL '+JSON.stringify({expected:fixture.expected,actual}));console.log(JSON.stringify({schema:'JOB_STAGE_V4_TEST_KITCHEN_RECEIPT_v1',result:'PASS',expected:fixture.expected,actual},null,2));
# Methods Information Benchmark — pre-role genesis

No permanent Role 1–6 authority is active. The committed user-origin seed is the only genesis authority. Room names, conversation memory, screenshots, summaries, and private working state are nonauthoritative.

The Bootstrap Builder may construct the genesis instruments and materialize inactive role contracts. It may not approve, independently verify, ratify, or activate its own output. The Bootstrap Verifier must execute from a clean checkout and may not repair the Builder output.

Use `AI_ENTRYPOINT.json`, the repository AI router, this project entrypoint, the role-system locator, and the shared bootstrap executable. Stop on missing authority, ambiguous benchmark identity, self-activation, incompatible function assignment, private-state dependence, or fingerprint mismatch.

Public Methods, Laws, showroom, renderer, preview, and live product paths are protected.

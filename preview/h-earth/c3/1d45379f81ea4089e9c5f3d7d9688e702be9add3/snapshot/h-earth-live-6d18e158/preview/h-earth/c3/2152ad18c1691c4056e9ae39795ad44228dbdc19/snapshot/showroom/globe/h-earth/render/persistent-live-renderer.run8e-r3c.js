/** H_EARTH_RUN_8E_R3C_PERSISTENT_WEBGL2_LIVE_RENDERER_C3C3R5_CANONICAL_GRID_ALIGNMENT_v1 */
import { getHEarthOW01CanonicalLiveRenderPackageOccurrence } from './live-render-package.run8e-r2.canonical.js';
import { createHEarthRun8ER2DCanonicalGPUUploadViews } from './gpu-upload-views.run8e-r2d.js';
import { getHEarthRun8ER3ALiveRendererInterface } from './live-renderer-contract.run8e-r3a.js';

export const H_EARTH_RUN_8E_R3C_RENDERER_ID='H_EARTH_RUN_8E_R3C_PERSISTENT_WEBGL2_LIVE_RENDERER_v1';
export const H_EARTH_C3C3R5_DEPTH_COLOR_MARRIAGE=Object.freeze({
  contract:'H_EARTH_C3C3R5_DEPTH_COLOR_RENDERER_MARRIAGE_CANONICAL_GRID_ALIGNMENT_v4',
  depthSource:'C3C2_WORLD_SPACE_BAND_LIMITED_RELIEF_RESPONSE',chromaticAuthority:'RUN8C_V_BASE_COLOR',hardCodedTerrainPalettePermitted:false,
  worldSpaceRelief:true,derivativeNormalPerturbation:true,distanceBandLimited:true,strataResponse:true,contourContactResponse:true,slopeCurvatureResponse:true,
  gridContactAuthority:'EXISTING_16X16_WORLD_SPACE_GEOMETRY',gridContactPresentation:'CELL_SCALE_LIGHTING_ONLY_NO_LITERAL_OVERLAY',
  xCellSizeWorldUnits:32.0,zCellSizeWorldUnits:20.0,seamCoreWorldUnits:2.35,shoulderOuterWorldUnits:6.65,cellFaceInteriorProtected:true,
  canonicalGridZMinimum:-256.0,canonicalGridZMaximum:64.0,rendererGeometryGridAligned:true,
  viewportWorldShapeAuthority:false
});
const RUNTIME_OCCURRENCE_ID='H_EARTH_OW01_GRATITUDE_COASTAL_ENTRY_LIVE_RENDER_PACKAGE_OCCURRENCE_001';
const finite=v=>typeof v==='number'&&Number.isFinite(v);
const color3=value=>{const a=Array.isArray(value)?value:[0,0,0],s=a.some(x=>x>1)?255:1;return a.slice(0,3).map(x=>Math.min(1,Math.max(0,Number(x)/s)));};
const hash=bytes=>{let v=0x811c9dc5;for(const b of bytes){v^=b;v=Math.imul(v,0x01000193)>>>0;}return `fnv1a32:${v.toString(16).padStart(8,'0')}`;};
const summarize=(bytes,clear)=>{let n=0,sum=0,sum2=0;const buckets=new Set(),pixels=bytes.length/4;for(let o=0;o<bytes.length;o+=4){const r=bytes[o],g=bytes[o+1],b=bytes[o+2];if(Math.abs(r-clear[0])+Math.abs(g-clear[1])+Math.abs(b-clear[2])>9)n++;const l=.2126*r+.7152*g+.0722*b;sum+=l;sum2+=l*l;buckets.add(`${r>>4}:${g>>4}:${b>>4}`);}const mean=sum/Math.max(1,pixels);return{pixelCount:pixels,nonClearPixelCount:n,uniqueColorBucketCount:buckets.size,meanLuminance:mean,luminanceStandardDeviation:Math.sqrt(Math.max(0,sum2/Math.max(1,pixels)-mean*mean)),byteHash:hash(bytes)};};

const VS=`#version 300 es
precision highp float;precision highp int;
layout(location=0)in vec3 aPosition;layout(location=1)in vec3 aNormal;layout(location=2)in vec4 aBaseColorLinear;layout(location=3)in vec4 aMaterialParameters;layout(location=4)in uint aMaterialModelCode;layout(location=5)in uint aSurfaceClassCode;layout(location=6)in uint aPrimitiveIndex;layout(location=7)in uint aRoleCode;
uniform mat4 uViewProjection;out vec3 vWorldPosition;out vec3 vNormal;out vec4 vBaseColor;out vec4 vMaterialParameters;flat out uint vMaterialModelCode;flat out uint vSurfaceClassCode;flat out uint vPrimitiveIndex;flat out uint vRoleCode;
void main(){vWorldPosition=aPosition;vNormal=aNormal;vBaseColor=aBaseColorLinear;vMaterialParameters=aMaterialParameters;vMaterialModelCode=aMaterialModelCode;vSurfaceClassCode=aSurfaceClassCode;vPrimitiveIndex=aPrimitiveIndex;vRoleCode=aRoleCode;gl_Position=uViewProjection*vec4(aPosition,1.0);}`;

const FS=`#version 300 es
precision highp float;precision highp int;
in vec3 vWorldPosition;in vec3 vNormal;in vec4 vBaseColor;in vec4 vMaterialParameters;flat in uint vMaterialModelCode;flat in uint vSurfaceClassCode;flat in uint vPrimitiveIndex;flat in uint vRoleCode;
uniform vec3 uCameraPosition;uniform vec3 uSunDirection;uniform float uSunIntensity;uniform vec3 uSunColor;uniform vec3 uSkyZenithColor;uniform vec3 uSkyHorizonColor;uniform vec3 uGroundHazeColor;uniform float uFogStartDistance;uniform float uFogFalloff;uniform float uMaximumFogFactor;uniform float uDistanceDesaturationStrength;out vec4 outColor;
float hash21(vec2 p){p=fract(p*vec2(123.34,456.21));p+=dot(p,p+45.32);return fract(p.x*p.y);}float noise2(vec2 p){vec2 i=floor(p),f=fract(p);f=f*f*(3.0-2.0*f);return mix(mix(hash21(i),hash21(i+vec2(1,0)),f.x),mix(hash21(i+vec2(0,1)),hash21(i+vec2(1,1)),f.x),f.y);}float stableWave(float phase){float footprint=max(fwidth(phase),.00001);float retained=1.0-smoothstep(.72,1.65,footprint);return mix(.5,.5+.5*sin(phase),retained);}float contourBand(float elevation){float scaled=elevation/2.5,centered=abs(fract(scaled)-.5),width=max(fwidth(scaled)*1.65,.026);return 1.0-smoothstep(.45-width,.49,centered);}vec3 perturbTerrainNormal(vec3 n,vec3 p,float h){vec3 dx=dFdx(p),dy=dFdy(p);float hx=dFdx(h),hy=dFdy(h);vec3 grad=hx*cross(dy,n)+hy*cross(n,dx);float det=dot(dx,cross(dy,n)),ori=det<0.0?-1.0:1.0;return normalize(abs(det)*n-ori*grad);}vec3 limitTerrainNormalDeviation(vec3 g,vec3 c){const float C=.9271838545667874,S=.3746065934159120;float d=clamp(dot(g,c),-1.0,1.0);if(d>=C)return c;vec3 t=c-g*d;float l=length(t);if(l<.00001)return g;return normalize(g*C+t/l*S);}float boundaryDistance(float value,float minimum,float maximum,float count){float size=(maximum-minimum)/count;float local=mod(mod(value-minimum,size)+size,size);return min(local,size-local);}float gridDistance(vec2 world){return min(boundaryDistance(world.x,-256.0,256.0,16.0),boundaryDistance(world.y,-256.0,64.0,16.0));}float gridSeam(vec2 world){if(world.x<-256.0||world.x>256.0||world.y<-256.0||world.y>64.0)return 0.0;float d=gridDistance(world),aa=max(fwidth(d)*1.10,.24);return 1.0-smoothstep(1.55-aa,3.15+aa,d);}float gridShoulder(vec2 world){if(world.x<-256.0||world.x>256.0||world.y<-256.0||world.y>64.0)return 0.0;float d=gridDistance(world),aa=max(fwidth(d),.24);float inner=smoothstep(2.65-aa,3.55+aa,d),outer=1.0-smoothstep(5.55-aa,6.65+aa,d);return clamp(inner*outer,0.0,1.0);}float gridCellFace(vec2 world){if(world.x<-256.0||world.x>256.0||world.y<-256.0||world.y>64.0)return 0.0;float d=gridDistance(world),aa=max(fwidth(d),.24);return smoothstep(6.15-aa,7.20+aa,d);}
void main(){vec3 geometricNormal=normalize(vNormal),shadingNormal=geometricNormal;vec3 lightDirection=normalize(-uSunDirection),viewDirection=normalize(uCameraPosition-vWorldPosition);float distanceToCamera=length(vWorldPosition-uCameraPosition),slope=1.0-clamp(geometricNormal.y,0.0,1.0);vec3 base=max(vBaseColor.rgb,vec3(.004));float outputAlpha=clamp(vBaseColor.a,.18,1.0),depthLuminance=1.0,contactDarkening=0.0,reliefHighlight=0.0,specularScale=1.0;
if(vRoleCode==1u){vec2 world=vWorldPosition.xz;float broad=noise2(world*.035),medium=noise2(world*.13+vec2(17,-9)),macroField=noise2(world*.018+vec2(5,-11)),mesoField=noise2(world*.082+vec2(-13,7)),detailField=noise2(world*.29+vec2(29,-17));float slopeResponse=smoothstep(.025,.58,slope),curvatureResponse=clamp(length(fwidth(geometricNormal))*3.25,0.0,1.0),nearDetail=1.0-smoothstep(72.0,285.0,distanceToCamera);const vec3 A=vec3(.8164965809,.4082482905,.4082482905),B=vec3(-.4082482905,.8164965809,.4082482905),C=vec3(.4082482905,-.4082482905,.8164965809);float pa=dot(vWorldPosition,A)*3.3069396354+.37,pb=dot(vWorldPosition,B)*2.7318196988+2.17,pc=dot(vWorldPosition,C)*2.2439947526+4.11;float footprint=max(fwidth(pa),max(fwidth(pb),fwidth(pc))),aa=1.0-smoothstep(.45,.95,footprint),microSignal=sin(pa)*.50+sin(pb)*.30+sin(pc)*.20,microHeight=microSignal*.22,distanceEnvelope=1.0-smoothstep(120.0,300.0,distanceToCamera),slopeEnvelope=mix(.82,1.0,smoothstep(.05,.55,slope)),reliefEnvelope=clamp(distanceEnvelope*slopeEnvelope*aa,0.0,1.0);vec3 bounded=limitTerrainNormalDeviation(geometricNormal,perturbTerrainNormal(geometricNormal,vWorldPosition,microHeight));shadingNormal=normalize(mix(geometricNormal,bounded,reliefEnvelope));float strata=stableWave(world.x*.47+world.y*.33+vWorldPosition.y*.79+medium*3.2),crossGrain=stableWave(world.x*.83-world.y*.61+broad*4.8),faceA=stableWave(world.x*.61+world.y*.39+vWorldPosition.y*.57+mesoField*4.1),faceB=stableWave(world.x*1.07-world.y*.73+vWorldPosition.y*.31+macroField*5.3),faceC=stableWave(world.x*1.71+world.y*1.23+vWorldPosition.y*.18+detailField*2.7),contour=contourBand(vWorldPosition.y),structural=clamp(strata*.28+crossGrain*.18+faceA*.18+faceB*.14+faceC*.10+contour*.24,0.0,1.0);float seam=gridSeam(world),shoulder=gridShoulder(world),cellFace=gridCellFace(world),gridDistanceEnvelope=1.0-smoothstep(185.0,410.0,distanceToCamera);float structuralContact=(structural-.42)*(.115+.205*slopeResponse)*nearDetail+curvatureResponse*.085;float seamContact=seam*(.14+.105*gridDistanceEnvelope);float shoulderLift=shoulder*(.045+.055*gridDistanceEnvelope);float faceStability=cellFace*(.018+.012*gridDistanceEnvelope);contactDarkening=clamp(structuralContact+seamContact,0.0,.30);reliefHighlight=clamp((1.0-structural)*(.035+.085*slopeResponse)*nearDetail+shoulderLift+faceStability,0.0,.145);depthLuminance=clamp(1.0-contactDarkening+reliefHighlight,.70,1.13);specularScale=mix(.88,.58,slopeResponse)*(1.0-seam*.34+cellFace*.05);}
float diffuse=max(dot(shadingNormal,lightDirection),0.0),ambient=.30+.08*max(shadingNormal.y,0.0),roleBrightness=vRoleCode==1u?1.05:(vRoleCode==2u?1.15:.92);vec3 lit=base*depthLuminance*(ambient+diffuse*uSunIntensity*.72)*uSunColor*roleBrightness;if(vRoleCode==1u){vec3 halfDirection=normalize(lightDirection+viewDirection);float spec=pow(max(dot(shadingNormal,halfDirection),0.0),28.0)*.045*specularScale;lit+=uSunColor*spec;}float fog=clamp((distanceToCamera-uFogStartDistance)*max(uFogFalloff,.00001),0.0,uMaximumFogFactor),lum=dot(lit,vec3(.2126,.7152,.0722));lit=mix(lit,vec3(lum),clamp(fog*uDistanceDesaturationStrength,0.0,1.0));vec3 atm=mix(uSkyHorizonColor,uSkyZenithColor,clamp(shadingNormal.y*.5+.5,0.0,1.0));lit=mix(lit,atm,fog*.22);lit=mix(lit,uGroundHazeColor,fog*.78);outColor=vec4(pow(clamp(lit,0.0,1.0),vec3(1.0/2.2)),outputAlpha);}`;
const DVS=`#version 300 es
precision highp float;const vec2 p[3]=vec2[3](vec2(-1.,-1.),vec2(3.,-1.),vec2(-1.,3.));out vec2 vUv;void main(){vec2 q=p[gl_VertexID];vUv=q*.5+.5;gl_Position=vec4(q,0.,1.);}`;
const DFS=`#version 300 es
precision highp float;in vec2 vUv;uniform sampler2D uDepth;out vec4 outColor;void main(){float d=texture(uDepth,vUv).r,v=clamp((1.-d)*28.,0.,1.);outColor=vec4(vec3(v),1.);}`;
const PFS=`#version 300 es
precision highp float;in vec2 vUv;uniform sampler2D uColor;uniform int uPresentationMode;uniform vec3 uSkyZenith;uniform vec3 uSkyHorizon;uniform vec3 uGroundHaze;uniform vec3 uSunColor;uniform vec2 uSunCenter;uniform float uSunIntensity;out vec4 outColor;void main(){if(uPresentationMode==1){outColor=texture(uColor,vUv);return;}float altitude=clamp(vUv.y,0.0,1.0);vec3 sky=mix(uSkyHorizon,uSkyZenith,smoothstep(0.0,.92,altitude));float lower=1.0-smoothstep(0.0,.24,altitude);sky=mix(sky,uGroundHaze,.16*lower);float sd=distance(vUv,uSunCenter),halo=1.0-smoothstep(.018,.105,sd),core=1.0-smoothstep(.008,.024,sd);sky=mix(sky,uSunColor,clamp(halo*.23*uSunIntensity+core*.92,0.0,1.0));float aerial=.018*sin((vUv.x*8.0+vUv.y*3.0)*3.14159265)+.012*sin((vUv.x*17.0-vUv.y*5.0)*3.14159265);sky+=vec3(aerial*(1.0-altitude)*.18);outColor=vec4(pow(clamp(sky,0.0,1.0),vec3(1.0/2.2)),1.0);}`;

export function createHEarthRun8ER3CPersistentRenderer({canvas,width=640,height=360}={}){if(!(canvas instanceof HTMLCanvasElement))throw new TypeError('R3C_CANVAS_REQUIRED');canvas.width=width;canvas.height=height;const gl=canvas.getContext('webgl2',{alpha:false,antialias:false,depth:true,stencil:false,preserveDrawingBuffer:true,powerPreference:'high-performance'});if(!gl)throw new Error('R3C_WEBGL2_CONTEXT_UNAVAILABLE');let initialized=false,latestDrawRanges=Object.freeze([]);const counters={contextCreationCount:1,shaderCreateCount:0,shaderCompileCount:0,programCreateCount:0,programLinkCount:0,vertexArrayCreateCount:0,bufferCreateCount:0,bufferUploadCount:0,uploadedByteLength:0,textureCreateCount:0,framebufferCreateCount:0,postInitializationResourceCreationCount:0,postInitializationBufferUploadCount:0,frameCount:0,visiblePresentationCount:0,colorReadbackCount:0,depthReadbackCount:0,pngEncodingCount:0,gpuFinishCount:0,cameraUniformUpdateCount:0,staticUniformUpdateCount:0,geometryDrawCallCount:0,totalDrawnIndexCount:0,depthVisualizationDrawCallCount:0,atmosphereBackgroundDrawCallCount:0};const resources={},mark=()=>{if(initialized)counters.postInitializationResourceCreationCount++;},shader=(type,source,label)=>{mark();counters.shaderCreateCount++;const s=gl.createShader(type);if(!s)throw new Error(`R3C_SHADER_CREATE_FAILED:${label}`);gl.shaderSource(s,source);gl.compileShader(s);counters.shaderCompileCount++;if(!gl.getShaderParameter(s,gl.COMPILE_STATUS))throw new Error(`R3C_SHADER_COMPILE_FAILED:${label}:${gl.getShaderInfoLog(s)}`);return s;},program=(vs,fs,label)=>{mark();counters.programCreateCount++;const p=gl.createProgram();if(!p)throw new Error(`R3C_PROGRAM_CREATE_FAILED:${label}`);gl.attachShader(p,vs);gl.attachShader(p,fs);gl.linkProgram(p);counters.programLinkCount++;if(!gl.getProgramParameter(p,gl.LINK_STATUS))throw new Error(`R3C_PROGRAM_LINK_FAILED:${label}:${gl.getProgramInfoLog(p)}`);return p;},buffer=()=>{mark();counters.bufferCreateCount++;const b=gl.createBuffer();if(!b)throw new Error('R3C_BUFFER_CREATE_FAILED');return b;},upload=(target,data)=>{if(initialized)counters.postInitializationBufferUploadCount++;counters.bufferUploadCount++;counters.uploadedByteLength+=data.byteLength;gl.bufferData(target,data,gl.STATIC_DRAW);},texture=()=>{mark();counters.textureCreateCount++;const t=gl.createTexture();if(!t)throw new Error('R3C_TEXTURE_CREATE_FAILED');return t;},framebuffer=()=>{mark();counters.framebufferCreateCount++;const f=gl.createFramebuffer();if(!f)throw new Error('R3C_FRAMEBUFFER_CREATE_FAILED');return f;},uniform=(p,n)=>{const l=gl.getUniformLocation(p,n);if(l===null)throw new Error(`R3C_UNIFORM_MISSING:${n}`);return l;},complete=label=>{const s=gl.checkFramebufferStatus(gl.FRAMEBUFFER);if(s!==gl.FRAMEBUFFER_COMPLETE)throw new Error(`R3C_FRAMEBUFFER_INCOMPLETE:${label}:${s}`);};const renderPackage=getHEarthOW01CanonicalLiveRenderPackageOccurrence(),uploadViews=createHEarthRun8ER2DCanonicalGPUUploadViews(renderPackage),rendererInterface=getHEarthRun8ER3ALiveRendererInterface();if(renderPackage.packageOccurrenceId!==RUNTIME_OCCURRENCE_ID)throw new Error(`R3C_RUNTIME_PACKAGE_OCCURRENCE_MISMATCH:${renderPackage.packageOccurrenceId}`);if(uploadViews.deterministicTransportEncoding!==true)throw new Error('R3C_CANONICAL_GPU_TRANSPORT_MISSING');
function initialize(packet){if(initialized)throw new Error('R3C_RENDERER_ALREADY_INITIALIZED');if(packet.packageIdentity!==renderPackage.packageIdentity||packet.packageContentDigest!==renderPackage.contentDigest)throw new Error('R3C_INITIAL_PACKET_PACKAGE_MISMATCH');resources.geometryVertexShader=shader(gl.VERTEX_SHADER,VS,'GV');resources.geometryFragmentShader=shader(gl.FRAGMENT_SHADER,FS,'GF');resources.geometryProgram=program(resources.geometryVertexShader,resources.geometryFragmentShader,'GP');resources.depthVertexShader=shader(gl.VERTEX_SHADER,DVS,'DV');resources.depthFragmentShader=shader(gl.FRAGMENT_SHADER,DFS,'DF');resources.depthProgram=program(resources.depthVertexShader,resources.depthFragmentShader,'DP');resources.presentationFragmentShader=shader(gl.FRAGMENT_SHADER,PFS,'PF');resources.presentationProgram=program(resources.depthVertexShader,resources.presentationFragmentShader,'PP');mark();counters.vertexArrayCreateCount++;resources.vertexArray=gl.createVertexArray();if(!resources.vertexArray)throw new Error('R3C_VERTEX_ARRAY_CREATE_FAILED');gl.bindVertexArray(resources.vertexArray);const specs=[['positions',uploadViews.positions,0,3,gl.FLOAT,false],['normals',uploadViews.normals,1,3,gl.FLOAT,false],['baseColorsLinear',uploadViews.baseColorsLinear,2,4,gl.FLOAT,false],['materialParameters',uploadViews.materialParameters,3,4,gl.FLOAT,false],['materialModelCodes',uploadViews.materialModelCodes,4,1,gl.UNSIGNED_BYTE,true],['surfaceClassCodes',uploadViews.surfaceClassCodes,5,1,gl.UNSIGNED_BYTE,true],['primitiveIndices',uploadViews.primitiveIndices,6,1,gl.UNSIGNED_SHORT,true],['roleCodes',uploadViews.roleCodes,7,1,gl.UNSIGNED_BYTE,true]];resources.buffers=[];for(const[name,data,location,size,type,integer]of specs){const b=buffer();resources.buffers.push({name,buffer:b,byteLength:data.byteLength});gl.bindBuffer(gl.ARRAY_BUFFER,b);upload(gl.ARRAY_BUFFER,data);gl.enableVertexAttribArray(location);if(integer)gl.vertexAttribIPointer(location,size,type,0,0);else gl.vertexAttribPointer(location,size,type,false,0,0);}resources.indexBuffer=buffer();resources.buffers.push({name:'indices',buffer:resources.indexBuffer,byteLength:uploadViews.indices.byteLength});gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,resources.indexBuffer);upload(gl.ELEMENT_ARRAY_BUFFER,uploadViews.indices);resources.colorTexture=texture();resources.depthTexture=texture();resources.geometryFramebuffer=framebuffer();gl.bindTexture(gl.TEXTURE_2D,resources.colorTexture);gl.texStorage2D(gl.TEXTURE_2D,1,gl.RGBA8,width,height);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MIN_FILTER,gl.NEAREST);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MAG_FILTER,gl.NEAREST);gl.bindTexture(gl.TEXTURE_2D,resources.depthTexture);gl.texStorage2D(gl.TEXTURE_2D,1,gl.DEPTH_COMPONENT24,width,height);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MIN_FILTER,gl.NEAREST);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MAG_FILTER,gl.NEAREST);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_COMPARE_MODE,gl.NONE);gl.bindFramebuffer(gl.FRAMEBUFFER,resources.geometryFramebuffer);gl.framebufferTexture2D(gl.FRAMEBUFFER,gl.COLOR_ATTACHMENT0,gl.TEXTURE_2D,resources.colorTexture,0);gl.framebufferTexture2D(gl.FRAMEBUFFER,gl.DEPTH_ATTACHMENT,gl.TEXTURE_2D,resources.depthTexture,0);complete('GEOMETRY');resources.depthColorTexture=texture();resources.depthFramebuffer=framebuffer();gl.bindTexture(gl.TEXTURE_2D,resources.depthColorTexture);gl.texStorage2D(gl.TEXTURE_2D,1,gl.RGBA8,width,height);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MIN_FILTER,gl.NEAREST);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MAG_FILTER,gl.NEAREST);gl.bindFramebuffer(gl.FRAMEBUFFER,resources.depthFramebuffer);gl.framebufferTexture2D(gl.FRAMEBUFFER,gl.COLOR_ATTACHMENT0,gl.TEXTURE_2D,resources.depthColorTexture,0);complete('DEPTH');resources.uniforms={viewProjection:uniform(resources.geometryProgram,'uViewProjection'),cameraPosition:uniform(resources.geometryProgram,'uCameraPosition'),sunDirection:uniform(resources.geometryProgram,'uSunDirection'),sunIntensity:uniform(resources.geometryProgram,'uSunIntensity'),sunColor:uniform(resources.geometryProgram,'uSunColor'),skyZenithColor:uniform(resources.geometryProgram,'uSkyZenithColor'),skyHorizonColor:uniform(resources.geometryProgram,'uSkyHorizonColor'),groundHazeColor:uniform(resources.geometryProgram,'uGroundHazeColor'),fogStartDistance:uniform(resources.geometryProgram,'uFogStartDistance'),fogFalloff:uniform(resources.geometryProgram,'uFogFalloff'),maximumFogFactor:uniform(resources.geometryProgram,'uMaximumFogFactor'),distanceDesaturationStrength:uniform(resources.geometryProgram,'uDistanceDesaturationStrength'),depth:uniform(resources.depthProgram,'uDepth'),presentationColor:uniform(resources.presentationProgram,'uColor'),presentationMode:uniform(resources.presentationProgram,'uPresentationMode'),presentationSkyZenith:uniform(resources.presentationProgram,'uSkyZenith'),presentationSkyHorizon:uniform(resources.presentationProgram,'uSkyHorizon'),presentationGroundHaze:uniform(resources.presentationProgram,'uGroundHaze'),presentationSunColor:uniform(resources.presentationProgram,'uSunColor'),presentationSunCenter:uniform(resources.presentationProgram,'uSunCenter'),presentationSunIntensity:uniform(resources.presentationProgram,'uSunIntensity')};const e=packet.environmentUniforms;resources.skyColor=color3(e.skyHorizonColor);resources.clearColorBytes=resources.skyColor.map(x=>Math.round(x*255));gl.useProgram(resources.geometryProgram);gl.uniform3f(resources.uniforms.sunDirection,e.sunDirection.x,e.sunDirection.y,e.sunDirection.z);gl.uniform1f(resources.uniforms.sunIntensity,e.sunIntensity);gl.uniform3fv(resources.uniforms.sunColor,color3(e.sunColor));gl.uniform3fv(resources.uniforms.skyZenithColor,color3(e.skyZenithColor));gl.uniform3fv(resources.uniforms.skyHorizonColor,resources.skyColor);gl.uniform3fv(resources.uniforms.groundHazeColor,color3(e.groundHazeColor));gl.uniform1f(resources.uniforms.fogStartDistance,e.fogStartDistance);gl.uniform1f(resources.uniforms.fogFalloff,e.fogFalloff);gl.uniform1f(resources.uniforms.maximumFogFactor,e.maximumFogFactor);gl.uniform1f(resources.uniforms.distanceDesaturationStrength,e.distanceDesaturationStrength);gl.useProgram(resources.presentationProgram);gl.uniform3fv(resources.uniforms.presentationSkyZenith,color3(e.skyZenithColor));gl.uniform3fv(resources.uniforms.presentationSkyHorizon,resources.skyColor);gl.uniform3fv(resources.uniforms.presentationGroundHaze,color3(e.groundHazeColor));gl.uniform3fv(resources.uniforms.presentationSunColor,color3(e.sunColor));gl.uniform2f(resources.uniforms.presentationSunCenter,Math.min(.92,Math.max(.08,.5+e.sunDirection.x*.46)),Math.min(.88,Math.max(.18,.38+e.sunDirection.y*.52)));gl.uniform1f(resources.uniforms.presentationSunIntensity,e.sunIntensity);gl.uniform1i(resources.uniforms.presentationColor,0);counters.staticUniformUpdateCount=17;initialized=true;return getResourceReceipt();}
function renderFrame(packet){if(!initialized)throw new Error('R3C_RENDERER_NOT_INITIALIZED');if(packet.packageIdentity!==renderPackage.packageIdentity||packet.packageContentDigest!==renderPackage.contentDigest)throw new Error('R3C_FRAME_PACKET_PACKAGE_MISMATCH');if(!Array.isArray(packet.camera.viewProjectionMatrix)||packet.camera.viewProjectionMatrix.length!==16||packet.camera.viewProjectionMatrix.some(v=>!finite(v)))throw new Error('R3C_VIEW_PROJECTION_INVALID');latestDrawRanges=Object.freeze(packet.drawRanges.map(r=>Object.freeze({...r})));gl.bindFramebuffer(gl.FRAMEBUFFER,resources.geometryFramebuffer);gl.viewport(0,0,width,height);gl.clearColor(...resources.skyColor,1);gl.clearDepth(1);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);gl.disable(gl.DEPTH_TEST);gl.disable(gl.BLEND);gl.depthMask(false);gl.useProgram(resources.presentationProgram);gl.bindVertexArray(null);gl.uniform1i(resources.uniforms.presentationMode,0);gl.drawArrays(gl.TRIANGLES,0,3);counters.atmosphereBackgroundDrawCallCount++;gl.depthMask(true);gl.enable(gl.DEPTH_TEST);gl.depthFunc(gl.LEQUAL);gl.disable(gl.CULL_FACE);gl.useProgram(resources.geometryProgram);gl.bindVertexArray(resources.vertexArray);gl.uniformMatrix4fv(resources.uniforms.viewProjection,false,new Float32Array(packet.camera.viewProjectionMatrix));gl.uniform3f(resources.uniforms.cameraPosition,packet.camera.position.x,packet.camera.position.y,packet.camera.position.z);counters.cameraUniformUpdateCount+=2;for(const range of packet.drawRanges){if(range.transparencyClass==='TRANSLUCENT'){gl.enable(gl.BLEND);gl.blendFunc(gl.SRC_ALPHA,gl.ONE_MINUS_SRC_ALPHA);gl.depthMask(false);}else{gl.disable(gl.BLEND);gl.depthMask(true);}gl.drawElements(gl.TRIANGLES,range.indexCount,gl.UNSIGNED_INT,range.indexStart*4);counters.geometryDrawCallCount++;counters.totalDrawnIndexCount+=range.indexCount;}gl.depthMask(true);gl.disable(gl.BLEND);const error=gl.getError();if(error!==gl.NO_ERROR)throw new Error(`R3C_DRAW_ERROR:${error}`);counters.frameCount++;}
function presentColorFrame(){if(!initialized)throw new Error('R3C_RENDERER_NOT_INITIALIZED');gl.bindFramebuffer(gl.FRAMEBUFFER,null);gl.viewport(0,0,width,height);gl.disable(gl.DEPTH_TEST);gl.disable(gl.BLEND);gl.depthMask(true);gl.useProgram(resources.presentationProgram);gl.bindVertexArray(null);gl.activeTexture(gl.TEXTURE0);gl.bindTexture(gl.TEXTURE_2D,resources.colorTexture);gl.uniform1i(resources.uniforms.presentationColor,0);gl.uniform1i(resources.uniforms.presentationMode,1);gl.drawArrays(gl.TRIANGLES,0,3);gl.flush();const error=gl.getError();if(error!==gl.NO_ERROR)throw new Error(`R3C_TEXTURE_PRESENTATION_DRAW_ERROR:${error}`);counters.visiblePresentationCount++;return Object.freeze({frameNumber:counters.frameCount,width,height,presentationPath:'OFFSCREEN_COLOR_TEXTURE_FULLSCREEN_DRAW',atmosphericEnclosureMaterialized:true});}
function captureColorFrame(label,{includePng=true}={}){if(!initialized)throw new Error('R3C_RENDERER_NOT_INITIALIZED');gl.bindFramebuffer(gl.FRAMEBUFFER,resources.geometryFramebuffer);gl.finish();counters.gpuFinishCount++;const pixels=new Uint8Array(width*height*4);gl.readPixels(0,0,width,height,gl.RGBA,gl.UNSIGNED_BYTE,pixels);counters.colorReadbackCount++;const summary=summarize(pixels,resources.clearColorBytes),pngDataUrl=includePng?canvas.toDataURL('image/png'):null;if(includePng)counters.pngEncodingCount++;return Object.freeze({label,frameNumber:counters.frameCount,width,height,summary,pngDataUrl});}
function captureDepthSummary(){gl.bindFramebuffer(gl.FRAMEBUFFER,resources.depthFramebuffer);gl.viewport(0,0,width,height);gl.disable(gl.DEPTH_TEST);gl.disable(gl.BLEND);gl.useProgram(resources.depthProgram);gl.bindVertexArray(null);gl.activeTexture(gl.TEXTURE0);gl.bindTexture(gl.TEXTURE_2D,resources.depthTexture);gl.uniform1i(resources.uniforms.depth,0);gl.drawArrays(gl.TRIANGLES,0,3);counters.depthVisualizationDrawCallCount++;gl.finish();counters.gpuFinishCount++;const pixels=new Uint8Array(width*height*4);gl.readPixels(0,0,width,height,gl.RGBA,gl.UNSIGNED_BYTE,pixels);counters.depthReadbackCount++;return summarize(pixels,[0,0,0]);}
function getResourceReceipt(){const debugRenderer=gl.getExtension('WEBGL_debug_renderer_info');return{rendererId:H_EARTH_RUN_8E_R3C_RENDERER_ID,initialized,dimensions:{width,height},context:{created:true,lost:gl.isContextLost(),vendor:gl.getParameter(gl.VENDOR),renderer:gl.getParameter(gl.RENDERER),unmaskedVendor:debugRenderer?gl.getParameter(debugRenderer.UNMASKED_VENDOR_WEBGL):null,unmaskedRenderer:debugRenderer?gl.getParameter(debugRenderer.UNMASKED_RENDERER_WEBGL):null,version:gl.getParameter(gl.VERSION),shadingLanguageVersion:gl.getParameter(gl.SHADING_LANGUAGE_VERSION)},package:{logicalPromotedIdentity:renderPackage.contractId??renderPackage.packageIdentity,runtimeIdentity:renderPackage.packageIdentity,runtimeContentDigest:renderPackage.contentDigest,primitiveCount:renderPackage.primitiveCount,vertexCount:renderPackage.vertexCount,triangleCount:renderPackage.triangleCount,indexCount:renderPackage.indexCount,drawRangeCount:renderPackage.drawRanges.length,canonicalGpuTransport:uploadViews.deterministicTransportEncoding===true},rendererInterface:{contractId:rendererInterface.contractId,attributeCount:rendererInterface.attributeLayout.length,uniformCount:rendererInterface.frameUniformNames.length,drawRangeCount:rendererInterface.drawRanges.length},depthColorMarriage:H_EARTH_C3C3R5_DEPTH_COLOR_MARRIAGE,counters:{...counters},persistentObjectCounts:{contexts:1,programs:3,shaders:5,vertexArrays:1,gpuBuffers:resources.buffers?.length??0,textures:3,framebuffers:2},atmosphericEnclosure:{materialized:initialized,model:'FULLSCREEN_SKY_GRADIENT_SUN_WITHOUT_HORIZON_GEOMETRY',ownerBaseline:'H_EARTH_C3C1_OWNER_NAVIGATED_SUCCESS_BASELINE_20260816',screenSpaceCurvedHorizonAuthority:false,worldSpaceGeometryOwnsHorizon:true,planetRelativeCameraOwnsOrientation:true,grayFallbackPermitted:false,transparentFallbackPermitted:false},latestDrawRangeCount:latestDrawRanges.length,resourceIdentityStable:initialized&&resources.buffers?.length===9&&Boolean(resources.geometryProgram&&resources.depthProgram&&resources.presentationProgram&&resources.vertexArray&&resources.geometryFramebuffer&&resources.depthFramebuffer),packageUploadedOnce:counters.bufferUploadCount===9&&counters.postInitializationBufferUploadCount===0,noPostInitializationResourceCreation:counters.postInitializationResourceCreationCount===0,noPostInitializationBufferUpload:counters.postInitializationBufferUploadCount===0};}
return Object.freeze({rendererId:H_EARTH_RUN_8E_R3C_RENDERER_ID,initialize,renderFrame,presentColorFrame,captureColorFrame,captureDepthSummary,getResourceReceipt});}
export default createHEarthRun8ER3CPersistentRenderer;
(()=>{
  'use strict';
  const reduce=matchMedia('(prefers-reduced-motion: reduce)');
  const mod=(value,base)=>((value%base)+base)%base;
  const focus=element=>{try{element?.focus({preventScroll:true})}catch{element?.focus()}};
  const element=(tag,className)=>{const item=document.createElement(tag);item.className=className;return item};
  function region(item,label,guidance){item.tabIndex=0;item.setAttribute('role','region');item.setAttribute('aria-roledescription','carousel');item.setAttribute('aria-label',label);item.setAttribute('aria-describedby',guidance)}
  function liveStatus(){const item=element('p','compass-orbit-status');item.setAttribute('aria-live','polite');item.setAttribute('aria-atomic','true');return item}
  function loadTrophy(done){if(window.CompassTrophyScene){done();return}const existing=document.querySelector('script[data-compass-trophy-scene]');if(existing){existing.addEventListener('load',done,{once:true});return}const script=document.createElement('script');script.src='/assets/compass/compass.trophy-scene.js?v=compass-volumetric-artifacts-v1';script.dataset.compassTrophyScene='true';script.addEventListener('load',done,{once:true});document.head.append(script)}
  function setInteractive(container,enabled){container.toggleAttribute('inert',!enabled);container.setAttribute('aria-hidden',enabled?'false':'true');container.tabIndex=enabled?0:-1;container.querySelectorAll('a,button,input,select,textarea').forEach(control=>{if(enabled){if(control.dataset.orbitTabindex==='none')control.removeAttribute('tabindex');else if(control.dataset.orbitTabindex!=null)control.tabIndex=Number(control.dataset.orbitTabindex);delete control.dataset.orbitTabindex}else{if(control.dataset.orbitTabindex==null)control.dataset.orbitTabindex=control.hasAttribute('tabindex')?control.getAttribute('tabindex'):'none';control.tabIndex=-1}})}
  function place(item,index,count,delta,label){const front=delta===0;item.dataset.slot=front?'front':delta===1?'rear-next':'rear-prev';item.setAttribute('role','group');item.setAttribute('aria-roledescription','slide');item.setAttribute('aria-posinset',index+1);item.setAttribute('aria-setsize',count);item.setAttribute('aria-current',front);item.setAttribute('aria-label',label);setInteractive(item,front)}
  function controls(kind,rotate){const group=element('div','compass-orbit-controls');group.setAttribute('aria-label',`${kind} controls`);group.innerHTML=`<button type="button" data-orbit-previous aria-label="Previous ${kind}">‹</button><button type="button" data-orbit-next aria-label="Next ${kind}">›</button>`;group.children[0].onclick=()=>rotate(-1);group.children[1].onclick=()=>rotate(1);return group}
  function statusRail(kind,labels){const rail=element('ol','compass-status-rail');rail.dataset.statusRail=kind;rail.setAttribute('aria-hidden','true');rail.innerHTML=labels.map(label=>`<li data-state="unseen">${label}</li>`).join('');return rail}
  function settleRail(rail,index,visited){visited.add(index);[...rail.children].forEach((item,itemIndex)=>item.dataset.state=itemIndex===index?'current':visited.has(itemIndex)?'visited':'unseen')}
  function houseObject(){return `<span class="compass-house-object" aria-hidden="true"><i class="compass-house-roof"></i><i class="compass-house-body"></i><i class="compass-house-door"></i><i class="compass-house-window compass-house-window--left"></i><i class="compass-house-window compass-house-window--right"></i><i class="compass-house-glow"></i></span>`}
  function mountCapability(){
    const legacy=document.querySelector('[data-compass-capability-switcher]');if(!legacy||document.querySelector('[data-capability-orbit]'))return;
    const stage=element('section','compass-capability-orbit');stage.dataset.capabilityOrbit='true';stage.dataset.capabilityMode='orbit';stage.dataset.portalContract='OBJECT_FIRST_STABLE_GEOMETRY_GEN1536';region(stage,'Signature Diamond Gate capabilities','compass-capability-guidance');
    const diagnostic=element('article','compass-orbit-plaque compass-object-portal-card');diagnostic.dataset.capability='diagnostic';diagnostic.dataset.productIdentity='Coheriscope';diagnostic.innerHTML=`<h2 class="compass-object-sr-title" id="compass-capability-diagnostic-title">Coheriscope</h2><a class="compass-object-portal compass-brain-portal" href="/coherence-diagnostic/" data-capability-function="diagnostic" aria-label="Enter Coheriscope, the Coherence Diagnostic by Diamond Gate Bridge"><span class="compass-brain-field" data-human-brain><canvas role="img" aria-label="Eye-level rotating three-dimensional human brain with left and right hemispheres, fissure, cerebellum, pons, brainstem, and X, Y, Z spatial orientation axes"></canvas><span class="compass-brain-axis" aria-hidden="true"><i class="axis-y">Y</i><i class="axis-x">X</i><i class="axis-z">Z</i></span></span></a><p class="compass-object-caption"><strong>Coheriscope</strong><span>Compare how you live and decide with what matters to you.</span></p>`;
    const awards=element('article','compass-orbit-plaque compass-object-portal-card compass-orbit-plaque--awards');awards.dataset.capability='awards';awards.innerHTML=`<h2 class="compass-object-sr-title" id="compass-capability-awards-title">Awards &amp; Recognition</h2><a class="compass-object-portal compass-trophy-portal" href="/showroom/globe/h-earth/awards/" data-capability-function="awards" aria-label="Enter Awards and Recognition"><span class="compass-award-object"><span class="compass-award-trophy" data-award-trophy><canvas class="compass-trophy-canvas" aria-hidden="true"></canvas><span class="compass-trophy-fallback" aria-hidden="true"><i class="compass-trophy-bowl"></i><i class="compass-trophy-handle compass-trophy-handle--left"></i><i class="compass-trophy-handle compass-trophy-handle--right"></i><i class="compass-trophy-stem"></i><i class="compass-trophy-base"></i></span><i class="compass-trophy-gleam" aria-hidden="true"></i></span></span></a><p class="compass-object-caption"><strong>Awards &amp; Recognition</strong><span>One body of work. Five reasons to look closer.</span></p>`;
    const house=element('article','compass-orbit-plaque compass-object-portal-card');house.dataset.capability='house';house.innerHTML=`<div class="compass-house-parent" data-house-parent><h2 class="compass-object-sr-title" id="compass-capability-house-title">The House</h2><button class="compass-object-portal compass-house-portal" type="button" data-house-focus-guides aria-label="Choose a House guide below" style="height:220px">${houseObject()}</button><p class="compass-object-caption"><strong>The House</strong><span>Choose Jeeves, Elara, or Auren.</span></p><nav class="compass-plaque-actions" data-house-guide-selector aria-label="Choose who to speak with"><a class="compass-orbit-action" style="margin-top:0" href="/showroom/globe/hearth/jeeves/" data-house-function="jeeves" aria-label="Talk to Jeeves, Whole House">Jeeves</a><a class="compass-orbit-action" style="margin-top:0" href="/elara/" data-house-function="elara" aria-label="Talk to Elara, heart and meaning">Elara</a><a class="compass-orbit-action" style="margin-top:0" href="/products/auren/" data-house-function="auren" aria-label="Talk to Auren, product floor">Auren</a></nav></div>`;
    const orbitGuidance='Swipe or use the controls to rotate. Use the available object or name controls to continue.',guidance=element('p','compass-capability-guidance');guidance.id='compass-capability-guidance';guidance.dataset.capabilityGuidance='true';guidance.setAttribute('aria-live','polite');guidance.textContent=orbitGuidance;
    const status=liveStatus();const cards=[diagnostic,awards,house],names=['Coheriscope','Awards & Recognition','The House'],capabilityRail=statusRail('capability',['Brain','Trophy','House']),capabilityVisited=new Set();let cardIndex=0,busy=false,settleTimer=0,awardsTimer=0;
    const finish=commit=>{clearTimeout(settleTimer);if(reduce.matches){queueMicrotask(()=>{busy=false;commit()})}else settleTimer=setTimeout(()=>{busy=false;commit()},320)};
    const rotateCard=direction=>{if(busy)return;busy=true;cardIndex=mod(cardIndex+direction,cards.length);renderCards();finish(()=>settleCards(true))};
    stage.append(...cards,capabilityRail,controls('capability',rotateCard),guidance,status);legacy.replaceWith(stage);
    const announce=()=>document.dispatchEvent(new CustomEvent('compass:capability-change',{detail:{mode:'orbit',capability:cards[cardIndex].dataset.capability}}));
    function illuminateAwards(){clearTimeout(awardsTimer);awards.classList.remove('is-illuminating');if(reduce.matches||cardIndex!==1)return;void awards.offsetWidth;awards.classList.add('is-illuminating');window.CompassTrophyScene?.activate();awardsTimer=setTimeout(()=>awards.classList.remove('is-illuminating'),800)}
    function renderCards(){cards.forEach((card,index)=>place(card,index,cards.length,mod(index-cardIndex,cards.length),`Capability ${index+1} of ${cards.length}: ${names[index]}`))}
    function settleCards(arrival=false){settleRail(capabilityRail,cardIndex,capabilityVisited);status.textContent=`Capability ${cardIndex+1} of ${cards.length}: ${names[cardIndex]}`;if(arrival)illuminateAwards();announce()}
    house.querySelector('[data-house-focus-guides]').addEventListener('click',()=>focus(house.querySelector('[data-house-guide-selector] [data-house-function]')));
    const portalTargets=cards.map(card=>card.querySelector('.compass-object-portal'));cards.forEach((card,index)=>{card.addEventListener('keydown',event=>{if(event.target===card&&(event.key==='Enter'||event.key===' ')){event.preventDefault();portalTargets[index]?.click()}})});
    window.CompassOrbitInput?.claimSwipe(stage,rotateCard);
    stage.addEventListener('keydown',event=>{const parentTarget=event.target===stage||event.target===cards[cardIndex];if((event.key==='ArrowRight'||event.key==='ArrowLeft')&&parentTarget){event.preventDefault();rotateCard(event.key==='ArrowRight'?1:-1)}});
    renderCards();settleCards();
    const canvas=diagnostic.querySelector('[data-human-brain] canvas');window.CompassBrainScene?.mount(canvas,{foreground:()=>cards[cardIndex].dataset.capability==='diagnostic'});
    const mountTrophy=()=>{const trophyCanvas=awards.querySelector('[data-award-trophy] canvas');window.CompassTrophyScene?.mount(trophyCanvas,{foreground:()=>cards[cardIndex].dataset.capability==='awards'})};loadTrophy(mountTrophy);
  }
  function mountProof(){const stage=document.querySelector('[data-proof-orbit]');if(!stage||stage.dataset.proofMounted)return;stage.dataset.proofMounted='true';stage.classList.add('is-enhanced');region(stage,'Built Different proof points','compass-proof-guidance');const cards=[...stage.querySelectorAll('[data-proof-card]')],names=cards.map(card=>card.querySelector('h3')?.textContent.trim()||'Proof point'),proofRail=statusRail('proof',['TRL 7','Bounded','Checked']),proofVisited=new Set();if(cards.length!==3)return;let index=0,busy=false,settleTimer=0;const guidance=element('p','compass-proof-guidance');guidance.id='compass-proof-guidance';guidance.textContent='Swipe or use the controls to inspect one proof point at a time.';const status=liveStatus();const settle=()=>{busy=false;settleRail(proofRail,index,proofVisited);status.textContent=`Proof point ${index+1} of ${cards.length}: ${names[index]}`;document.dispatchEvent(new CustomEvent('compass:proof-change',{detail:{proof:cards[index].dataset.proofCard}}))};const rotate=direction=>{if(busy)return;busy=true;index=mod(index+direction,cards.length);render();clearTimeout(settleTimer);if(reduce.matches)queueMicrotask(settle);else settleTimer=setTimeout(settle,320)};stage.append(proofRail,controls('proof point',rotate),guidance,status);function render(){cards.forEach((card,cardIndex)=>place(card,cardIndex,cards.length,mod(cardIndex-index,cards.length),`Proof point ${cardIndex+1} of ${cards.length}: ${names[cardIndex]}`))}window.CompassOrbitInput?.claimSwipe(stage,rotate);stage.addEventListener('keydown',event=>{if((event.target===stage||event.target===cards[index])&&(event.key==='ArrowRight'||event.key==='ArrowLeft')){event.preventDefault();rotate(event.key==='ArrowRight'?1:-1)}});render();settle()}
  function mount(){mountCapability();mountProof()}
  const publicApi=Object.freeze({version:'object-portal-coherence-gen1536',volumetricArtifacts:'generation-1515-preserved',mount});Object.defineProperty(window,'CompassEditorialCarousel',{configurable:false,enumerable:true,get:()=>publicApi,set:()=>{}});document.readyState==='loading'?document.addEventListener('DOMContentLoaded',mount,{once:true}):mount();
})();

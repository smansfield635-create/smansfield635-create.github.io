export function createGroundRenderer() {
  return {
    draw() {
      // STAGE GATE ACTIVE
      // Land, substrate, coastlines, regions, markers, and labels are disabled
      // until globe / water / underwater are complete.
    }
  };
}

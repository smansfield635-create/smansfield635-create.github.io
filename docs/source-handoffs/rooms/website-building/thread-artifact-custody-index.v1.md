# Thread artifact custody index v1

Status: source handoff only.

This index lists the website-building room artifacts identified as repository-worthy during the custody scan.

## Repository-worthy artifacts

### 1. PSALM hierarchy and terminology

Retain `PSALM` as the canonical term. Reject `songs` as canonical terminology for this structure unless separately reopened by owner source.

Retain the hierarchy:

```text
MK -> MPK -> MP[1-4] -> CP[1-16] -> BS[1-64] -> ST[1-256] -> ISK[1-256]
```

### 2. ACK_PACK routing terminology

Retain `ACK_PACK` as the canonical packet term. Treat `ACT_PACK` as noncanonical for this route unless a later source explicitly rebinds it.

### 3. Quadrilateral team path

Retain the ordered path:

```text
Origin Input -> North -> East -> South -> West -> North Final Return -> Lab/Execution -> Jeeves Relay -> ACK Return
```

### 4. Room authority distinction

Retain the distinction between:

- source custody;
- route discipline;
- implementation authority;
- public runtime mutation;
- registry mutation;
- publication.

This directory handles source custody only.

### 5. Dextrion / Dexter continuity

Dextrion is retained here only as website-building room source context, not as a character-registry mutation. Character registry source assembly remains separate.

Known retained read:

- Dextrion is the active copyright-distinct room name replacing Dexter where bound;
- Dextrion preserves prior Dexter continuity;
- Dextrion is website-building implementation/lab voice where valid;
- Dextrion does not override Sean, Jeeves, Severin, or the repository authority path.

### 6. Equipment Room custody law

Retain the rule that equipment-capability audits, graphics-core returns, 256-lattice runs, hex-pixel tests, render-budget analysis, accessibility/content-priority survival checks, and related equipment processing route first to the Equipment Room. Equipment Room returns may grade capability and readiness but do not by themselves authorize code writing or image generation.

## Held / partial items

The reversible Fibonacci-adjacent traversal ladder was recognized in this thread but not fully recovered. Preserve it as partial until exact source is recovered.

## Non-authority statement

This index records repository custody targets. It does not convert any target into runtime law or public site behavior.

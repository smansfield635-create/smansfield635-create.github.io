name: Public Governance Implementation Retired

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed
        shell: bash
        run: |
          echo 'PUBLIC_GOVERNANCE_IMPLEMENTATION_RETIRED'
          exit 1

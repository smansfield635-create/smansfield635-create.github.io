(()=>{
'use strict';

const CONTINUITY='COMPASS_CAPABILITY_CONTINUITY_v1';
const reduce=matchMedia('(prefers-reduced-motion: reduce)');
const mod=(v,b)=>((v%b)+b)%b;
const el=(tag,cls='')=>{const n=document.createElement(tag);n.className=cls;return n};
function region(node,label,guidanceId){node.tabIndex=0;node.setAttribute('role','region');node.setAttribute('aria-roledescription','carousel');node.setAttribute('aria-label',label);node.setAttribute('aria-describedby',guidanceId)}
function status(){const n=el('p','compass-orbit-status');n.setAttribute('aria-live','polite');n.setAttribute('aria-atomic','true');return n}
function controls(kind,rotate){const g=el('div','compass-orbit-controls');g.setAttribute('aria-label',`${kind} controls`);g.innerHTML=`<button type="button" data-orbit-previous aria-label="Previous ${kind}">‹</button><button type="button" data-orbit-next aria-label="Next ${kind}">›</button>`;g.children[0].onclick=e=>{e.stopPropagation();rotate(-1)};g.children[1].onclick=e=>{e.stopPropagation();rotate(1)};return g}
function rail(kind,labels){const r=el('ol','compass-status-rail');r.dataset.statusRail=kind;r.setAttribute('aria-hidden','true');r.innerHTML=labels.map(x=>`<li data-state="unseen">${x}</li>`).join('');return r}
function settleRail(r,index,visited){visited.add(index);[...r.children].forEach((n,k)=>n.dataset.state=k===index?'current':visited.has(k)?'visited':'unseen')}
function setInteractive(card,on){card.toggleAttribute('inert',!on);card.setAttribute('aria-hidden',on?'false':'true');card.tabIndex=on?0:-1}
function placeProof(item,index,count,delta,label){const front=delta===0;item.dataset.slot=front?'front':delta===1?'rear-next':'rear-prev';item.setAttribute('role','group');item.setAttribute('aria-roledescription','slide');item.setAttribute('aria-posinset',String(index+1));item.setAttribute('aria-setsize',String(count));item.setAttribute('aria-current',String(front));item.setAttribute('aria-label',label);setInteractive(item,front)}
function loadScene(src,key,attr,done){if(globalThis[key]){done();return}const existing=document.querySelector(`script[${attr}]`);if(existing){if(existing.dataset.loaded==='true'||existing.readyState==='complete'){done();return}existing.addEventListener('load',done,{once:true});return}const s=document.createElement('script');s.src=src;s.defer=true;s.setAttribute(attr,'true');s.addEventListener('load',()=>{s.dataset.loaded='true';done()},{once:true});document.head.append(s)}

function mountCapability(){
  const stage=document.querySelector('[data-capability-orbit][data-first-paint-authority="static"]');
  if(!stage||stage.dataset.capabilityMounted==='true')return;
  const objectStage=stage.querySelector('.compass-object-stage'),ring=stage.querySelector('.compass-object-ring');
  const diagnostic=stage.querySelector('[data-capability="diagnostic"]'),awards=stage.querySelector('[data-capability="awards"]'),house=stage.querySelector('[data-capability="house"]');
  const dock=stage.querySelector('.compass-action-dock'),guidance=stage.querySelector('#compass-capability-guidance'),live=stage.querySelector('.compass-orbit-status');
  if(!objectStage||!ring||!diagnostic||!awards||!house||!dock||!guidance||!live)throw new Error('COMPASS_STATIC_CAPABILITY_SHELL_INCOMPLETE');
  const cards=[diagnostic,awards,house],specs=[{name:'Coheriscope',id:'diagnostic',actions:[['Enter Coheriscope','/coherence-diagnostic/']]},{name:'Awards & Recognition',id:'awards',actions:[['Enter Awards','/showroom/globe/h-earth/awards/']]},{name:'The House',id:'house',actions:[['Jeeves','/showroom/globe/hearth/jeeves/'],['Elara','/elara/'],['Auren','/products/auren/']]}];
  region(stage,'Signature Diamond Gate capabilities','compass-capability-guidance');
  stage.dataset.capabilityMode='orbit';stage.dataset.continuityContract=CONTINUITY;stage.dataset.portalContract='THREE_OBJECT_LOCAL_ACTION_CAROUSEL_v1';stage.dataset.capabilityMounted='true';
  const step=120;cards.forEach((card,i)=>{card.dataset.index=String(i)});
  let index=Math.max(0,cards.findIndex(card=>card.dataset.active==='true')),angle=-Math.max(0,cards.findIndex(card=>card.dataset.active==='true'))*step,lastX=0,lastT=0,startX=0,startIndex=index,startAngle=angle,velocity=0,dragging=false,settling=false,arrivalTimer=0,settleTimer=0,suppressClickUntil=0;
  const settleDelay=reduce.matches?0:140;
  function syncCards(){cards.forEach((card,i)=>{const active=i===index;card.dataset.active=String(active);card.setAttribute('aria-current',String(active));card.setAttribute('aria-hidden',String(!active));card.tabIndex=active?0:-1;card.toggleAttribute('inert',!active)})}
  function syncDock(){dock.replaceChildren();dock.setAttribute('aria-busy',String(dragging));dock.toggleAttribute('inert',dragging);dock.dataset.navigationAuthority=dragging?'blocked':'available';if(dragging)return;for(const [label,href] of specs[index].actions){const a=el('a','compass-stage-action');a.href=href;a.textContent=label;dock.append(a)}}
  function publish(phase){live.textContent=`Capability ${index+1} of ${cards.length}: ${specs[index].name}`;document.dispatchEvent(new CustomEvent('compass:capability-change',{detail:{continuity:CONTINUITY,mode:'orbit',phase,capability:specs[index].id,index,dragging,settling,actionAuthority:!dragging}}))}
  function settle(reason='settled'){clearTimeout(settleTimer);settleTimer=0;settling=false;objectStage.dataset.dragging='false';angle=-index*step;ring.style.setProperty('--compass-object-angle',`${angle}deg`);syncCards();syncDock();clearTimeout(arrivalTimer);cards.forEach(c=>c.dataset.arriving='false');cards[index].dataset.arriving='true';arrivalTimer=setTimeout(()=>cards[index].dataset.arriving='false',260);globalThis.CompassHouseScene?.setForeground?.(index===2);publish(reason)}
  function scheduleSettle(reason='settled'){clearTimeout(settleTimer);if(!settleDelay)queueMicrotask(()=>settle(reason));else settleTimer=setTimeout(()=>settle(reason),settleDelay)}
  function go(delta){if(dragging)return;clearTimeout(settleTimer);settleTimer=0;settling=true;index=mod(index+delta,cards.length);angle=-index*step;ring.style.setProperty('--compass-object-angle',`${angle}deg`);syncCards();syncDock();globalThis.CompassHouseScene?.setForeground?.(index===2);publish('settling');scheduleSettle()}
  function pointerDown(e){if((e.button!=null&&e.button!==0)||e.target.closest?.('a,button'))return;clearTimeout(settleTimer);settleTimer=0;dragging=true;settling=false;startX=lastX=e.clientX;startIndex=index;startAngle=angle;lastT=performance.now();velocity=0;objectStage.dataset.dragging='true';stage.setPointerCapture?.(e.pointerId);syncDock();publish('dragging');e.preventDefault()}
  function pointerMove(e){if(!dragging)return;const now=performance.now(),dx=e.clientX-lastX,dt=Math.max(8,now-lastT);velocity=.72*velocity+.28*(dx/dt);angle=startAngle+(e.clientX-startX)*.42;ring.style.setProperty('--compass-object-angle',`${angle}deg`);lastX=e.clientX;lastT=now;e.preventDefault()}
  function pointerUp(e){if(!dragging)return;const total=e.clientX-startX,fast=Math.abs(velocity)>.16,decisive=Math.abs(total)>22||fast;dragging=false;stage.releasePointerCapture?.(e.pointerId);if(decisive){index=mod(startIndex+(total<0||velocity<-.16?1:-1),cards.length);suppressClickUntil=performance.now()+260}else index=startIndex;settling=true;objectStage.dataset.dragging='false';angle=-index*step;ring.style.setProperty('--compass-object-angle',`${angle}deg`);syncCards();syncDock();globalThis.CompassHouseScene?.setForeground?.(index===2);publish('settling');scheduleSettle();e.preventDefault()}
  stage.addEventListener('click',e=>{if(performance.now()<suppressClickUntil){e.preventDefault();e.stopImmediatePropagation()}},true);
  stage.addEventListener('pointerdown',pointerDown,true);stage.addEventListener('pointermove',pointerMove,true);stage.addEventListener('pointerup',pointerUp,true);stage.addEventListener('pointercancel',pointerUp,true);stage.addEventListener('keydown',e=>{if(e.key==='ArrowRight'||e.key==='ArrowLeft'){e.preventDefault();go(e.key==='ArrowRight'?1:-1)}});
  ring.style.setProperty('--compass-object-angle',`${angle}deg`);syncCards();syncDock();publish('initial');
  globalThis.CompassBrainScene?.mount(diagnostic.querySelector('canvas'),{foreground:()=>index===0&&!dragging});
  loadScene('/assets/compass/compass.trophy-scene.js?v=capability-continuity-v1&cb=98e8192982e866ee4833c717219fcb22eaf567d6','CompassTrophyScene','data-compass-trophy-scene',()=>globalThis.CompassTrophyScene?.mount(awards.querySelector('canvas'),{foreground:()=>index===1&&!dragging}));
  loadScene('/assets/compass/compass.house-scene.js?v=capability-continuity-v1&cb=ae6d269d8e98330a','CompassHouseScene','data-compass-house-scene-runtime',()=>globalThis.CompassHouseScene?.mount(house.querySelector('canvas'),{foreground:()=>index===2&&!dragging}));
}
function mountProof(){const stage=document.querySelector('[data-proof-orbit]');if(!stage||stage.dataset.proofMounted)return;stage.dataset.proofMounted='true';stage.classList.add('is-enhanced');region(stage,'Built Different proof points','compass-proof-guidance');const cards=[...stage.querySelectorAll('[data-proof-card]')];if(cards.length!==3)return;const names=cards.map(c=>c.querySelector('h3')?.textContent.trim()||'Proof point'),r=rail('proof',['TRL 7','Bounded','Checked']),visited=new Set();let index=0,busy=false,timer=0;const guidance=el('p','compass-capability-guidance');guidance.id='compass-proof-guidance';guidance.textContent='Swipe or use the controls to inspect one proof point at a time.';const live=status();function render(){cards.forEach((c,k)=>placeProof(c,k,cards.length,mod(k-index,cards.length),`Proof point ${k+1} of ${cards.length}: ${names[k]}`))}function settle(){busy=false;settleRail(r,index,visited);live.textContent=`Proof point ${index+1} of ${cards.length}: ${names[index]}`;document.dispatchEvent(new CustomEvent('compass:proof-change',{detail:{proof:cards[index].dataset.proofCard}}))}function rotate(delta){if(busy)return;busy=true;index=mod(index+delta,cards.length);render();clearTimeout(timer);if(reduce.matches)queueMicrotask(settle);else timer=setTimeout(settle,320)}stage.append(r,controls('proof point',rotate),guidance,live);globalThis.CompassOrbitInput?.claimSwipe(stage,rotate);stage.addEventListener('keydown',e=>{if((e.target===stage||e.target===cards[index])&&(e.key==='ArrowRight'||e.key==='ArrowLeft')){e.preventDefault();rotate(e.key==='ArrowRight'?1:-1)}});render();settle()}
function markLocalContinuity(){const intro=document.querySelector('.compass-introduction'),header=document.querySelector('.compass-estate__header'),scene=document.querySelector('[data-compass-scene]');if(intro){intro.removeAttribute('open');intro.dataset.compassPurposeStage='true'}const kicker=header?.querySelector('.compass-estate__kicker');if(kicker&&/Diamond Gate Bridge/i.test(kicker.textContent||''))kicker.textContent='Find Your Way';if(scene)scene.dataset.capabilityContinuity='true'}
function mount(){markLocalContinuity();mountCapability();mountProof();const root=document.querySelector('[data-compass-root]');if(root){root.dataset.capabilityContinuity=CONTINUITY;root.dataset.lowerProofContinuity='protected';root.dataset.legacyRecoveryAuthority='retired'}}
const api=Object.freeze({version:'capability-continuity-v5-static-owner',continuity:CONTINUITY,mount});Object.defineProperty(globalThis,'CompassEditorialCarousel',{configurable:false,enumerable:true,get:()=>api,set:()=>{}});document.readyState==='loading'?document.addEventListener('DOMContentLoaded',mount,{once:true}):mount();
})();

import fs from 'node:fs';import{spawnSync}from'node:child_process';const OPERATION='COMPASS_FRONT_PAGE_CORRECTIVE_GEOMETRY_PROVENANCE_BRAIN_PERFORMANCE_20260817_004',LOCK=1524,GOVERNING='a7b13dd5cebeafe6ce126d7df64c4088db7feb48',OUTPUT=process.env.COMPASS_PERFORMANCE_OUTPUT||'/tmp/compass-gen1524-performance.json',read=p=>fs.readFileSync(p,'utf8'),bytes=p=>Buffer.byteLength(read(p)),git=a=>spawnSync('git',a,{encoding:'utf8'}),checks=[],check=(id,pass,evidence)=>checks.push({id,pass:Boolean(pass),evidence});const paths={brain:'assets/compass/compass.brain-scene.js',brainCss:'assets/compass/compass.brain.css',identity:'assets/compass/compass.identity-3d.js',identityCss:'assets/compass/compass.identity-3d.css',capability:'assets/compass/compass.capability-carousel.js',index:'index.html',geometry:'assets/compass/upstream-compass.geometry.js',renderer:'assets/compass/upstream-compass.renderer.js'},t=Object.fromEntries(Object.entries(paths).map(([k,p])=>[k,read(p)])),head=git(['rev-parse','HEAD^{commit}']).stdout.trim(),requested=(process.env.COMPASS_CANDIDATE_HEAD||head).trim();check('EXACT_CANDIDATE_HEAD_BOUND',/^[0-9a-f]{40}$/.test(head)&&head===requested,{head,requested});check('GEN1524_OPERATION_BINDING',OPERATION.endsWith('_004')&&LOCK===1524&&GOVERNING.length===40,{OPERATION,LOCK,GOVERNING});for(const[k,max]of Object.entries({brain:24576,brainCss:8192,identity:24576,identityCss:12288}))check(`BUDGET_${k.toUpperCase()}`,bytes(paths[k])<=max,{bytes:bytes(paths[k]),max});check('CORRECTIVE_PAYLOAD_BOUNDED',bytes(paths.identity)+bytes(paths.identityCss)+bytes(paths.brain)<=49152,{bytes:bytes(paths.identity)+bytes(paths.identityCss)+bytes(paths.brain),max:49152});check('LAWS_DIRECT_GEOMETRY_ADOPTION',t.identity.includes("sourceModule:'/laws/index.spacecraft.geometry.js'")&&t.identity.includes("adoption:'DIRECT_CANONICAL_GEOMETRY_MODULE_CONSUMPTION'")&&t.identity.includes("import('/laws/index.spacecraft.geometry.js?v=LAWS_CP6_TRUE_3D_SPACECRAFT_20260801A')")&&t.identity.includes('source.buildLawsSpacecraftGeometry()'),'canonical Laws geometry consumed directly');check('LAWS_SINGLE_MESH_DRAW',t.identity.includes('gl.drawElements(gl.TRIANGLES,mesh.indices.length')&&!t.identity.includes('drawCraft('),'one admitted mesh draw; no custom 2D craft silhouette');check('SPACECRAFT_DPR_BOUNDED',t.identity.includes('innerWidth<700?1.15:1.5')&&t.identity.includes("powerPreference:'low-power'")&&t.identity.includes('preserveDrawingBuffer:false'),'mobile/desktop DPR caps and low-power context');check('SPACECRAFT_IDLE_GATED',t.identity.includes('document.hidden||reduce.matches')&&t.identity.includes('state.nextLaunch')&&t.identity.includes('15000+')&&t.identityCss.includes('.compass-spacecraft-layer{display:none!important}'),'document/reduced-motion/idle cadence gates');check('SPATIAL_COMPASS_REUSES_EXISTING_GEOMETRY',t.identity.includes('/assets/compass/upstream-compass.geometry.js?v=3.0.0-fixed-center-independent-sibling')&&t.identity.includes('/assets/compass/upstream-compass.renderer.js?v=3.1.1-generic-lifecycle-receipt')&&t.geometry.includes('dimensionality: 3')&&t.geometry.includes('occupiedTotalDepth: 0.48'),'existing true-3D geometry/renderer reused');check('SPATIAL_COMPASS_RESOURCE_POLICY',t.renderer.includes('normalDevicePixelRatioCap')&&t.renderer.includes('lowPowerDevicePixelRatioCap')&&t.renderer.includes('bloomDisableWidthPx')&&t.renderer.includes('preserveDrawingBuffer')&&t.renderer.includes('false'),'renderer DPR/bloom/backbuffer controls retained');check('BRAIN_RUNTIME_GATED',t.brain.includes('IntersectionObserver')&&t.brain.includes('document.hidden')&&t.brain.includes("powerPreference:'low-power'")&&t.brain.includes('innerWidth<700?1.25:1.75'),'brain viewport/page/device gating');check('BRAIN_SINGLE_DRAW_CALL',(t.brain.match(/gl\.drawElements/g)||[]).length===1,'single combined brain draw call');check('BRAIN_ANATOMY_CORRECTIVE',t.brain.includes("ellipsoid('cerebellum',0,-.52,-.62,.56,.255,.35")&&t.brain.includes("tube('midbrain'")&&t.brain.includes("tube('pons'")&&t.brain.includes("tube('medulla'")&&t.brain.includes("brainPosteriorBulbs:'none'"),'single integrated cerebellum + continuous brainstem axis');check('REDUCED_MOTION_EQUIVALENCE',t.identityCss.includes('@media(prefers-reduced-motion:reduce)')&&t.brain.includes("brainMotion:reduce.matches?'static-reduced-motion':'slow-yaw'"),'reduced-motion static equivalents');check('PROTECTED_CAPABILITIES_READ_ONLY',["dataset.capability='diagnostic'","dataset.capability='awards'","dataset.capability='house'"].every(v=>t.capability.includes(v)),'protected capability order remains');const allowed=new Set(['index.html','assets/compass/compass-core.css','assets/compass/compass.identity-3d.css','assets/compass/compass.identity-3d.js','assets/compass/compass.brain.css','assets/compass/compass.brain-scene.js','assets/compass/upstream-compass.css','assets/compass/upstream-compass.geometry.js','assets/compass/upstream-compass.renderer.js','.github/ai-router/projects/compass/verify-display-continuity.v1.mjs','.github/ai-router/projects/compass/verify-performance-successor.mjs','.github/workflows/compass-performance-successor-validation.yml','.github/workflows/compass-carousel-successor-live-qualification.yml']),changed=git(['diff','--name-only',`${GOVERNING}...HEAD`]);if(changed.status===0){const list=changed.stdout.trim().split(/\r?\n/).filter(Boolean);check('EXACT_13_PATH_BOUNDARY',list.length>0&&list.every(p=>allowed.has(p)),{changed:list,allowedCount:allowed.size});check('PROTECTED_CONTROLLER_CAROUSEL_LAWS_UNTOUCHED',list.every(p=>!['assets/compass/compass.controller.js','assets/compass/compass.crystals.js','assets/compass/compass.capability-carousel.js','assets/compass/compass.capability-carousel.css'].includes(p)&&!p.startsWith('laws/')),list)}else check('EXACT_13_PATH_BOUNDARY',false,changed.stderr);const failures=checks.filter(c=>!c.pass),receipt={schema:'COMPASS_FRONT_PAGE_CORRECTIVE_GEN1524_PERFORMANCE_RECEIPT_v1',operationId:OPERATION,lockGeneration:LOCK,governingHead:GOVERNING,candidateHead:head,targetFrameBudgetMs:16.7,result:failures.length?'FAIL_CLOSED':'PASS_CLOSED',checks,failures:failures.map(x=>x.id)};fs.writeFileSync(OUTPUT,JSON.stringify(receipt,null,2)+'\n');console.log(JSON.stringify(receipt,null,2));if(failures.length)process.exit(1);
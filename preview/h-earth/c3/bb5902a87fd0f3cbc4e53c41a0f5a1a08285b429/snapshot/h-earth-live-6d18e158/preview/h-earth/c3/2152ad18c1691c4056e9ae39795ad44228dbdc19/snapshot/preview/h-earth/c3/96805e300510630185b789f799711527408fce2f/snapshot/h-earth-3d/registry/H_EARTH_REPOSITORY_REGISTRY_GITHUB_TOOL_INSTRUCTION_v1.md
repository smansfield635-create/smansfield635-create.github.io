# H_EARTH_REPOSITORY_REGISTRY_GITHUB_TOOL_INSTRUCTION_v1

Status: complete candidate, not accepted, not canonical, and not workflow enforcement.

This instruction governs GitHub-connected agents, repository search tools, code-review tools, package-aware validators, future workflow enforcement, and audit or receipt generators when they inspect or operate on H-Earth architecture.

## Required source

Begin with `h-earth-3d/registry/h-earth.repository-registry.bootstrap.json`. Verify the declared registry ID, version, schema identity, candidate byte count, SHA-256, and Git blob before relying on the registry. Consume the read-only facade at `h-earth-3d/registry/h-earth.repository-registry.js`; do not bypass declared evidence, lifecycle, mutation, or stopping fields with inference.

## Mandatory preflight

1. Locate the bootstrap and verify registry identity.
2. Resolve every requested path to registry nodes, exact repository occurrences, and relations.
3. Expand the scope to all affected composite units, packet boundaries, facades, and cardinal participation.
4. Load authority, evidence, lifecycle, custody, topology, and continuity layers.
5. Load allowed mutation scope and prohibited mutations for every affected node.
6. Load required validations, stopping boundaries, conflicts, and unresolved fields.
7. Evaluate the complete operation scope and stop when authority or critical evidence remains unresolved.

## Cardinal and authority preservation

The explicit geometry-kernel cardinal body is North foundational mathematics, East mathematical analysis and topology, South neutral geometry construction, and West primitive and aggregate-frame admission. The public facade exposes these authorities but does not own or inherit them. The composite identity does not collapse member authority.

Gate B cardinal participation remains observed, candidate, and noncanonical: North environment frame, South construction occurrence, West admission orchestration, and East Packet 002 continuation. The Gate B West adapter remains orchestration only. Existing public West remains the admission authority. Packet 002 retains separate repository occurrences and does not inherit West authority.

## Operating prohibitions

Do not infer operational order from array order, directory position, filename, or narrative proximity. Do not invent missing roles, relations, digests, byte counts, authority, mutation permission, merge permission, canonical status, or production status. Keep absent, unresolved, candidate, accepted, historical, superseded, and retired states distinct. Stop on identity drift, broken references, unsupported cardinal promotion, authority collapse, higher-authority conflict, or unresolved critical fields.

## Required report

Report the registry identity and occurrence used; affected paths, nodes, relations, and composite units; cardinal roles and role status; authority classes and limitations; required validations and stopping boundaries; unresolved fields, conflicts, continuity gaps, or identity drift; and whether the operation proceeded, stopped, or requires separate authority.

This instruction authorizes read-only discovery and analysis only. It creates no source, mutation, merge, canonicalization, validator-enforcement, workflow-enforcement, runtime, renderer, deployment, or production authority. Target 4 remains required to prove package-aware comprehension through executed positive, negative, drift, and adversarial validation.

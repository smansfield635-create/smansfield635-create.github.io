<!-- TNT — LOGO CARD ONLY (drop-in replacement) -->
<!-- Replace ONLY the logo card section with this block -->

<section class="panel">
  <div class="logoWrap">
    <div class="logoCard">
      <!-- REPLACE src WITH YOUR LOGO FILE -->
      <img src="/assets/logo.svg" alt="Geodiametrics logo"/>
    </div>
  </div>
</section>

<style>
  .logoWrap{
    display:flex;
    justify-content:center;
    align-items:center;
    padding:18px 0 6px;
  }

  .logoCard{
    width:min(360px,78vw);
    aspect-ratio:1/1;
    border-radius:26px;
    background:rgba(10,16,32,.35);
    box-shadow:
      0 20px 70px rgba(0,0,0,.45),
      inset 0 0 0 1px rgba(255,255,255,.08);
    display:flex;
    align-items:center;
    justify-content:center;
    position:relative;
    overflow:hidden;
  }

  /* Snowflake foundation glow (green / blue / brown) */
  .logoCard::before{
    content:"";
    position:absolute;
    inset:0;
    background:
      radial-gradient(500px 360px at 20% 20%, rgba(47,79,143,.28), transparent 60%),
      radial-gradient(520px 400px at 80% 30%, rgba(47,143,107,.22), transparent 60%),
      radial-gradient(520px 420px at 50% 90%, rgba(122,90,58,.20), transparent 60%);
    opacity:.95;
  }

  .logoCard img{
    position:relative;
    z-index:1;
    width:72%;
    height:72%;
    object-fit:contain;
    filter: drop-shadow(0 14px 26px rgba(0,0,0,.35));
    opacity:.96;
  }
</style>

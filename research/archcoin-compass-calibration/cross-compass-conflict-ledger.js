/*
 * ARCHCOIN Cross-Compass Calibration Chamber
 * Closed conflict ledger derived from all four source families.
 */

export const ARCHCOIN_CROSS_COMPASS_CONFLICT_LEDGER = Object.freeze({
  schema: "ARCHCOIN_CROSS_COMPASS_CONFLICT_LEDGER_v2",
  status: "FOUR_FAMILY_CONFLICT_LEDGER_COMPLETE",
  evidenceBase: "eceac4d5297b2f087c1cf718e29d79c119c29db1",
  admissionAuthority: "WITHHELD",
  productionAuthority: false,
  liveProductMutationAuthorized: false,

  conflictClasses: Object.freeze([
    "AUTHORITY_COLLISION",
    "DUPLICATE_STATE_CUSTODY",
    "GEOMETRY_PROJECTION_COUPLING",
    "INPUT_NAVIGATION_COUPLING",
    "AMBIENT_MOTION_SELECTION_CONFLICT",
    "PAGE_IDENTITY_LEAKAGE",
    "CENTER_VISUAL_NAVIGATION_COUPLING",
    "ACCESSIBILITY_REGRESSION",
    "REDUCED_MOTION_REGRESSION",
    "INTERRUPTION_RECOVERY_GAP",
    "INCOMPATIBLE_PROFILE_PARAMETER"
  ]),

  conflicts: Object.freeze([
    Object.freeze({
      id: "C001_MAIN_CRYSTAL_RUNTIME_AUTHORITY_CONCENTRATION",
      class: "AUTHORITY_COLLISION",
      evidenceFamilies: Object.freeze(["MAIN_COMPASS_SOURCE_FAMILY", "LAW_COMPASS_SOURCE_FAMILY", "SHOWROOM_COMPASS_SOURCE_FAMILY"]),
      finding: "Main proves strong behavior but concentrates world geometry, projection, hit testing, gesture interpretation, and rendering in one crystal source.",
      resolution: "Extract the behavior, not the ownership arrangement. Universal standard adopts explicit controller/interactions/world-or-geometry/compositor/renderer boundaries.",
      archcoinEffect: "Do not copy Main's combined crystal-source architecture."
    }),
    Object.freeze({
      id: "C002_ARCHCOIN_CRYSTALS_RECONSTRUCT_WORLD_STATE",
      class: "GEOMETRY_PROJECTION_COUPLING",
      evidenceFamilies: Object.freeze(["ARCHCOIN_SOURCE_FAMILY", "LAW_COMPASS_SOURCE_FAMILY", "SHOWROOM_COMPASS_SOURCE_FAMILY"]),
      finding: "Restored ARCHCOIN crystals own canonical vectors, radii, quaternion world reconstruction, depth, and primary inference in addition to rendering.",
      resolution: "Treat seven-file world/visual/projection separation as a corrective candidate requirement, not as evidence that the restored baseline already implements it.",
      archcoinEffect: "Future architecture may separate these responsibilities, but the first calibration lane must not replace the accepted visual baseline wholesale."
    }),
    Object.freeze({
      id: "C003_FREE_QUATERNION_MOTION_VS_FIXED_AXIS_DISCIPLINE",
      class: "INCOMPATIBLE_PROFILE_PARAMETER",
      evidenceFamilies: Object.freeze(["MAIN_COMPASS_SOURCE_FAMILY", "LAW_COMPASS_SOURCE_FAMILY", "SHOWROOM_COMPASS_SOURCE_FAMILY", "ARCHCOIN_SOURCE_FAMILY"]),
      finding: "All four use quaternion motion, but ARCHCOIN's financial-cardinal clusters require a stable axis or orbital frame that the generic free spherical mapping does not guarantee.",
      resolution: "Admit complete normalized quaternion transactions universally; keep gesture-axis selection profile-specific. ARCHCOIN profile requires fixed-axis cluster calibration while preserving smooth motion.",
      archcoinEffect: "Replace axis mapping only; preserve controller custody, smoothing, target hysteresis, geometry, and rendering."
    }),
    Object.freeze({
      id: "C004_AMBIENT_MOTION_UNDER_SELECTION",
      class: "AMBIENT_MOTION_SELECTION_CONFLICT",
      evidenceFamilies: Object.freeze(["LAW_COMPASS_SOURCE_FAMILY", "SHOWROOM_COMPASS_SOURCE_FAMILY", "ARCHCOIN_SOURCE_FAMILY"]),
      finding: "Ambient motion improves vitality but makes ARCHCOIN room targets difficult to acquire when targets continue dancing during selection.",
      resolution: "Ambient motion is optional and subordinate to acquisition. Candidate, locked, settled, keyboard-focused, and reduced-motion states must damp or stop nonessential motion.",
      archcoinEffect: "Adopt Main-style selection stability and retain only ambient behavior that cannot move a target out from under the user."
    }),
    Object.freeze({
      id: "C005_CENTER_VISUAL_AND_HOME_RETURN_COLLISION",
      class: "CENTER_VISUAL_NAVIGATION_COUPLING",
      evidenceFamilies: Object.freeze(["LAW_COMPASS_SOURCE_FAMILY", "SHOWROOM_COMPASS_SOURCE_FAMILY", "ARCHCOIN_SOURCE_FAMILY"]),
      finding: "ARCHCOIN's visible fixed Compass sibling and semantic Home Compass return occupy one center zone, while Showroom and Laws demonstrate non-routing planet participation.",
      resolution: "Separate center visual participant from center semantic control. The rotating globe may render independently while the existing Home Compass control retains hit, cancellation, and return authority.",
      archcoinEffect: "Center globe substitution requires an explicit visual-participant integration stage and must not alter return semantics."
    }),
    Object.freeze({
      id: "C006_DETACHED_LABEL_COORDINATE_SYSTEM",
      class: "GEOMETRY_PROJECTION_COUPLING",
      evidenceFamilies: Object.freeze(["MAIN_COMPASS_SOURCE_FAMILY", "LAW_COMPASS_SOURCE_FAMILY", "SHOWROOM_COMPASS_SOURCE_FAMILY", "ARCHCOIN_SOURCE_FAMILY"]),
      finding: "ARCHCOIN cluster labels and detached cardinal markers can form a second screen-space coordinate system that competes with moving crystals.",
      resolution: "Labels and semantic hit regions must derive from the same projection record as the visible object. Duplicate detached markers are suppressed when they conflict with active cluster identity.",
      archcoinEffect: "Preserve literal cardinal semantics but bind active labels to projected objects and retained target state."
    }),
    Object.freeze({
      id: "C007_NUMERICAL_CONSTANT_FALSE_UNIVERSALIZATION",
      class: "INCOMPATIBLE_PROFILE_PARAMETER",
      evidenceFamilies: Object.freeze(["MAIN_COMPASS_SOURCE_FAMILY", "LAW_COMPASS_SOURCE_FAMILY", "SHOWROOM_COMPASS_SOURCE_FAMILY", "ARCHCOIN_SOURCE_FAMILY"]),
      finding: "Radii, sensitivity, camera distance, hit corridors, settlement speed, and swipe thresholds differ by page scale and purpose.",
      resolution: "Admit parameter classes universally, never source values automatically. Values remain profile calibration evidence until tested in the target implementation.",
      archcoinEffect: "No numeric import from Main, Laws, or Showroom without bounded ARCHCOIN testing."
    }),
    Object.freeze({
      id: "C008_PAGE_IDENTITY_LEAKAGE",
      class: "PAGE_IDENTITY_LEAKAGE",
      evidenceFamilies: Object.freeze(["MAIN_COMPASS_SOURCE_FAMILY", "LAW_COMPASS_SOURCE_FAMILY", "SHOWROOM_COMPASS_SOURCE_FAMILY", "ARCHCOIN_SOURCE_FAMILY"]),
      finding: "Cardinal names, law identities, financial domains, routes, narrative copy, palettes, Mirrorland passages, and Showroom objects are page identities rather than universal Compass rules.",
      resolution: "Universal core contains interfaces and invariants only. Page records remain supplied through profile registries and route adapters.",
      archcoinEffect: "Preserve North/East/South/West, Contract/Receivable/Payable/Allocation, sixteen rooms, routes, colors, and copy as ARCHCOIN profile identity."
    }),
    Object.freeze({
      id: "C009_RETURN_GESTURE_AND_NAVIGATION_COUPLING",
      class: "INPUT_NAVIGATION_COUPLING",
      evidenceFamilies: Object.freeze(["MAIN_COMPASS_SOURCE_FAMILY", "LAW_COMPASS_SOURCE_FAMILY", "SHOWROOM_COMPASS_SOURCE_FAMILY", "ARCHCOIN_SOURCE_FAMILY"]),
      finding: "A release or swipe must not directly navigate or silently change canonical passage state.",
      resolution: "Interactions classify and propose; controller validates cluster state, destination, preserved orientation, and legality before committing return.",
      archcoinEffect: "Retain explicit Home Compass return and cluster swipe proposals; recalibrate thresholds without moving authority."
    }),
    Object.freeze({
      id: "C010_REDUCED_MOTION_AS_FEATURE_REMOVAL",
      class: "REDUCED_MOTION_REGRESSION",
      evidenceFamilies: Object.freeze(["LAW_COMPASS_SOURCE_FAMILY", "SHOWROOM_COMPASS_SOURCE_FAMILY", "MAIN_COMPASS_SOURCE_FAMILY", "ARCHCOIN_SOURCE_FAMILY"]),
      finding: "Reduced motion may suspend decorative or ambient animation but cannot remove navigation, target stability, settlement, readable labels, or semantic controls.",
      resolution: "Reduced-motion profile lowers or disables nonessential animation while preserving complete interaction semantics and deterministic state changes.",
      archcoinEffect: "Test all cluster selection and return paths with reduced motion active."
    }),
    Object.freeze({
      id: "C011_INTERRUPTION_WITH_ACTIVE_PREVIEW",
      class: "INTERRUPTION_RECOVERY_GAP",
      evidenceFamilies: Object.freeze(["MAIN_COMPASS_SOURCE_FAMILY", "LAW_COMPASS_SOURCE_FAMILY", "SHOWROOM_COMPASS_SOURCE_FAMILY", "ARCHCOIN_SOURCE_FAMILY"]),
      finding: "Lost capture, blur, visibility loss, pagehide, renderer loss, or subsystem failure during preview can leave stale targets or accidental clicks.",
      resolution: "Every interruption cancels or restores through the controller gesture origin, clears target custody, releases capture, suppresses stale click activation, and publishes failure state.",
      archcoinEffect: "Existing ARCHCOIN lifecycle support must be validated against the full universal interruption corridor."
    })
  ]),

  prohibitedCouplings: Object.freeze([
    "INTERACTIONS_OWN_CANONICAL_NAVIGATION",
    "RENDERER_OWNS_CANONICAL_SELECTION",
    "PROJECTION_RECONSTRUCTS_WORLD_TRUTH",
    "CRYSTALS_INVENT_CANONICAL_WORLD_TRUTH_WHEN_WORLD_AUTHORITY_EXISTS",
    "PAGE_SPECIFIC_IDENTITIES_ENTER_REUSABLE_CORE",
    "CANCEL_PATH_COMMITS_PREVIEW_STATE",
    "CONTINUOUS_AMBIENT_MOTION_REQUIRED_FOR_SELECTION",
    "CENTER_VISUAL_AUTOMATICALLY_OWNS_CENTER_NAVIGATION",
    "POINTER_RELEASE_ALONE_AUTHORIZES_RETURN_OR_NAVIGATION",
    "SOURCE_NUMERICAL_CONSTANTS_AUTOMATICALLY_BECOME_UNIVERSAL"
  ]),

  resolvedForCandidateStandard: Object.freeze([
    "C001_MAIN_CRYSTAL_RUNTIME_AUTHORITY_CONCENTRATION",
    "C002_ARCHCOIN_CRYSTALS_RECONSTRUCT_WORLD_STATE",
    "C003_FREE_QUATERNION_MOTION_VS_FIXED_AXIS_DISCIPLINE",
    "C004_AMBIENT_MOTION_UNDER_SELECTION",
    "C005_CENTER_VISUAL_AND_HOME_RETURN_COLLISION",
    "C006_DETACHED_LABEL_COORDINATE_SYSTEM",
    "C007_NUMERICAL_CONSTANT_FALSE_UNIVERSALIZATION",
    "C008_PAGE_IDENTITY_LEAKAGE",
    "C009_RETURN_GESTURE_AND_NAVIGATION_COUPLING",
    "C010_REDUCED_MOTION_AS_FEATURE_REMOVAL",
    "C011_INTERRUPTION_WITH_ACTIVE_PREVIEW"
  ]),

  unresolvedForAdmission: Object.freeze([
    "FIXED_AXIS_MATHEMATICAL_PROFILE_SELECTION",
    "ARCHCOIN_CENTER_GLOBE_PARTICIPANT_INTERFACE",
    "ARCHCOIN_LABEL_PROJECTION_POLICY",
    "ARCHCOIN_CLUSTER_AMBIENT_DAMPING_THRESHOLDS",
    "PHYSICAL_DEVICE_VISUAL_AND_ACCESSIBILITY_ACCEPTANCE"
  ]),

  resolutionAuthority: "CANDIDATE_STANDARD_RECORD_COMPLETE_ADMISSION_REQUIRES_EXPLICIT_TESTED_DECISION"
});
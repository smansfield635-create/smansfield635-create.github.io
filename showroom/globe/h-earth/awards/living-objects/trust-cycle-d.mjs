const CONNECTED_MASSIF_SHOULDERS='ONE_CONNECTED_RANGE_WITH_NINE_VISUALLY_SEPARABLE_SUMMIT_EVENTS';
const CONTRACT=Object.freeze({id:'AWARDS_TRUST_LIVING_OBJECT_TRUE_3D_V4_RECOGNITION_FIRST',cycle:'D_TRUST',claim:'Software should have to earn the right to say it worked.',recognizableObject:'NINE_SUMMITS',renderer:'WEBGL_3D_PROCEDURAL_MASSIF',signatureEvent:'EARNED_ASCENT_REVEAL',eventCount:1,summitCount:9,compositionProfile:Object.freeze({reference:'ONE_CONNECTED_RANGE_NINE_SUMMIT_EVENTS',distinctSummitCount:9,cameraMode:'FRONTAL_OBLIQUE_CONNECTED_RANGE',cameraDistance:2.72,targetFrameOccupancy:.90,hostShape:'PANORAMIC_MOUNTAIN_RANGE'}),sourceBinding:Object.freeze({nineSummits:Object.freeze({path:'/nine-summits/index.html',blob:'99ae0cdafe5ad649d04d381ccbb2aa565aff86cd',coreMessage:'the-climb-becomes-a-world'}),literalMountainDonorGeometryPresent:false,boundedConstructionException:'ONE_TRUE_3D_NINE_SUMMIT_MASSIF_IN_AWARDS_MODULE_ONLY'}),lifecycle:Object.freeze(['REAR_INERT','APPROACHING','FOREGROUND_REST','SIGNATURE_PLAY','FOREGROUND_IDLE','SELECT_RESPONSE','READER_OPEN','RETURN_RESTORING'])});
const SUMMITS=Object.freeze([[-1.62,-.18,.70],[-1.22,.18,.87],[-.83,-.28,.76],[-.46,.10,.98],[-.05,-.05,1.22],[.37,.24,.92],[.79,-.23,1.02],[1.19,.14,.84],[1.58,-.13,.70]]);
const VS=`attribute vec3 a_position;attribute vec3 a_normal;attribute vec4 a_color;uniform float u_yaw,u_pitch,u_scale,u_aspect,u_cam,u_lift;varying vec3 v_n;varying vec4 v_c;void main(){float cy=cos(u_yaw),sy=sin(u_yaw),cx=cos(u_pitch),sx=sin(u_pitch);vec3 p=a_position;p.y+=u_lift*(.05+.07*smoothstep(.15,1.15,p.y));vec3 n=a_normal;p=vec3(cy*p.x+sy*p.z,p.y,-sy*p.x+cy*p.z);n=vec3(cy*n.x+sy*n.z,n.y,-sy*n.x+cy*n.z);p=vec3(p.x,cx*p.y-sx*p.z,sx*p.y+cx*p.z);n=vec3(n.x,cx*n.y-sx*n.z,sx*n.y+cx*n.z);p*=u_scale;float z=max(.35,p.z+u_cam);gl_Position=vec4(p.x*1.68/u_aspect,p.y*1.68,z-1.0,z);v_n=normalize(n);v_c=a_color;}`;const FS=`precision mediump float;varying vec3 v_n;varying vec4 v_c;void main(){vec3 L=normalize(vec3(-.34,.72,-.60));float d=.30+.70*max(0.0,dot(normalize(v_n),L));gl_FragColor=vec4(v_c.rgb*d,v_c.a);}`;
function sh(gl,t,s){const q=gl.createShader(t);gl.shaderSource(q,s);gl.compileShader(q);if(!gl.getShaderParameter(q,gl.COMPILE_STATUS))throw new Error('SUMMITS_SHADER:'+gl.getShaderInfoLog(q));return q}function prog(gl){const p=gl.createProgram();gl.attachShader(p,sh(gl,gl.VERTEX_SHADER,VS));gl.attachShader(p,sh(gl,gl.FRAGMENT_SHADER,FS));gl.linkProgram(p);if(!gl.getProgramParameter(p,gl.LINK_STATUS))throw new Error(gl.getProgramInfoLog(p));return p}
function ridge(x,z){const spine=.18*Math.exp(-(z*z/.72))*Math.exp(-(x*x/6.5));const front=.075*Math.exp(-((z+.40)*(z+.40)/.30))*Math.exp(-(x*x/5.8));const back=.085*Math.exp(-((z-.36)*(z-.36)/.36))*Math.exp(-(x*x/5.1));return spine+front+back}
function height(x,z){let h=.03+ridge(x,z);for(const[sx,sz,a]of SUMMITS){const dx=x-sx,dz=z-sz;const shoulder=a*.20*Math.exp(-(dx*dx/.18+dz*dz/.22));const crown=a*.52*Math.exp(-(dx*dx/.050+dz*dz/.070));h+=shoulder+crown}for(let i=0;i<SUMMITS.length-1;i++){const[a,b]=[SUMMITS[i],SUMMITS[i+1]],vx=b[0]-a[0],vz=b[1]-a[1],l2=vx*vx+vz*vz,t=Math.max(0,Math.min(1,((x-a[0])*vx+(z-a[1])*vz)/l2)),px=a[0]+vx*t,pz=a[1]+vz*t,dx=x-px,dz=z-pz;h+=.045*Math.exp(-(dx*dx/.13+dz*dz/.13))}h+=.018*Math.sin(x*3.5+z*2.4)*Math.exp(-(z*z/.85));return Math.max(.02,h)}function normal(x,z){const e=.016,hx=height(x+e,z)-height(x-e,z),hz=height(x,z+e)-height(x,z-e);const nx=-hx/(2*e),ny=1,nz=-hz/(2*e),l=Math.hypot(nx,ny,nz)||1;return[nx/l,ny/l,nz/l]}
function build(){const N=76,x0=-2.0,x1=2.0,z0=-.92,z1=.98,verts=[],norms=[],cols=[],idx=[];for(let j=0;j<N;j++){const z=z0+(z1-z0)*j/(N-1);for(let i=0;i<N;i++){const x=x0+(x1-x0)*i/(N-1),y=height(x,z),n=normal(x,z),snow=Math.max(0,Math.min(1,(y-.68)/.50)),rock=Math.max(0,Math.min(1,(y-.28)/.58)),r=.11*(1-rock)+.31*rock+.49*snow,g=.18*(1-rock)+.29*rock+.44*snow,b=.15*(1-rock)+.25*rock+.42*snow;verts.push(x,y-.46,z);norms.push(...n);cols.push(r,g,b,1)}}for(let j=0;j<N-1;j++)for(let i=0;i<N-1;i++){const a=j*N+i,b=a+1,c=a+N,d=c+1;idx.push(a,c,b,b,c,d)}return{verts,norms,cols,idx}}
function upload(gl,g){const o={count:g.idx.length};for(const[k,d]of[['p',g.verts],['n',g.norms],['c',g.cols]]){o[k]=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,o[k]);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(d),gl.STATIC_DRAW)}o.i=gl.createBuffer();gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,o.i);gl.bufferData(gl.ELEMENT_ARRAY_BUFFER,new Uint16Array(g.idx),gl.STATIC_DRAW);return o}function bind(gl,p,o){for(const[n,k,s]of[['a_position','p',3],['a_normal','n',3],['a_color','c',4]]){const l=gl.getAttribLocation(p,n);gl.bindBuffer(gl.ARRAY_BUFFER,o[k]);gl.enableVertexAttribArray(l);gl.vertexAttribPointer(l,s,gl.FLOAT,false,0,0)}gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,o.i)}
export function mountTrustLivingObject(root,options={}){if(!root||typeof root.replaceChildren!=='function')throw new TypeError('TRUST_ROOT_REQUIRED');const doc=root.ownerDocument||document,canvas=doc.createElement('canvas');canvas.className='awards-true3d-canvas awards-nine-summits-true3d';canvas.style.cssText='display:block;width:100%;height:100%;min-height:0;pointer-events:none';canvas.setAttribute('aria-hidden','true');root.replaceChildren(canvas);const gl=canvas.getContext('webgl2',{alpha:true,antialias:true})||canvas.getContext('webgl',{alpha:true,antialias:true});if(!gl)throw new Error('TRUST_WEBGL_REQUIRED');const p=prog(gl),gpu=upload(gl,build());gl.useProgram(p);gl.enable(gl.DEPTH_TEST);gl.enable(gl.CULL_FACE);bind(gl,p,gpu);const U=n=>gl.getUniformLocation(p,n),uy=U('u_yaw'),up=U('u_pitch'),us=U('u_scale'),ua=U('u_aspect'),uc=U('u_cam'),ul=U('u_lift');let state='FOREGROUND_REST',phase='rest',destroyed=false,raf=0,start=performance.now(),sig=-1;const reduced=options.reducedMotion??matchMedia?.('(prefers-reduced-motion: reduce)')?.matches??false;function resize(){const d=Math.min(2,devicePixelRatio||1),w=Math.max(1,Math.round(canvas.clientWidth*d)),h=Math.max(1,Math.round(canvas.clientHeight*d));if(w!==canvas.width||h!==canvas.height){canvas.width=w;canvas.height=h}gl.viewport(0,0,w,h);return w/h}function frame(t){if(destroyed)return;const asp=resize(),st=sig<0?99:(t-sig)/1000,asc=reduced?.8:(sig>=0?Math.max(0,Math.min(1,st/.9))*Math.max(0,Math.min(1,(3.1-st)/.65)):0);if(sig>=0&&st>3.35){sig=-1;phase='rest';state='FOREGROUND_IDLE'}gl.clearColor(.005,.012,.018,0);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);gl.uniform1f(uy,-.30+.025*Math.sin((t-start)/4700)+asc*.035);gl.uniform1f(up,-.25+asc*.018);gl.uniform1f(us,1.22+.028*asc);gl.uniform1f(ua,asp);gl.uniform1f(uc,CONTRACT.compositionProfile.cameraDistance);gl.uniform1f(ul,asc*.16);gl.drawElements(gl.TRIANGLES,gpu.count,gl.UNSIGNED_SHORT,0);raf=requestAnimationFrame(frame)}raf=requestAnimationFrame(frame);function S(x){if(!CONTRACT.lifecycle.includes(x))throw new Error('TRUST_INVALID_STATE');state=x;return x}function playSignature(){S('SIGNATURE_PLAY');phase='ascent';sig=performance.now();return sig}function selectResponse(){S('SELECT_RESPONSE')}function readerOpen(){S('READER_OPEN')}function restore(){S('RETURN_RESTORING');sig=-1;phase='rest';setTimeout(()=>{if(!destroyed)S('FOREGROUND_REST')},reduced?0:260)}function setLifecycle(x){S(x)}function inspect(){return Object.freeze({contract:CONTRACT.id,state,phase,reducedMotion:!!reduced,webglContexts:1,renderer:'WEBGL_3D_PROCEDURAL_MASSIF',recognizableObject:'NINE_SUMMITS',summitCount:9,distinctSummitCount:9,connectedMassif:CONNECTED_MASSIF_SHOULDERS,compositionProfile:CONTRACT.compositionProfile,signatureEvent:CONTRACT.signatureEvent,eventCount:1,sourceBinding:CONTRACT.sourceBinding})}function destroy(){destroyed=true;cancelAnimationFrame(raf);for(const k of['p','n','c','i'])gl.deleteBuffer(gpu[k]);gl.deleteProgram(p);gl.getExtension('WEBGL_lose_context')?.loseContext();root.replaceChildren()}return Object.freeze({contract:CONTRACT,setState:setLifecycle,playSignature,selectResponse,readerOpen,restore,inspect,destroy,element:canvas})}export{CONTRACT as AWARDS_TRUST_CYCLE_D_CONTRACT};
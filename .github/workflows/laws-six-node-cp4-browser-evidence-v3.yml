name: Laws Six-Node CP4 Browser Evidence v3 Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by distinct-position verification."

export const ARCHCOIN_LAW_COMPASS_EXTRACTION = Object.freeze({
  schema: "ARCHCOIN_LAW_COMPASS_EXTRACTION_v1",
  status: "EXTRACTION_PENDING",
  sourceFamily: "LAW_COMPASS_SOURCE_FAMILY",
  sourceRoots: Object.freeze(["/laws/"]),
  targetCapabilities: Object.freeze([
    "FLUID_MOTION",
    "INTERACTION_SENSITIVITY",
    "INTERPOLATION",
    "HIT_CORRIDORS",
    "RESPONSIBILITY_SEPARATION"
  ]),
  evidence: Object.freeze([]),
  extractedCapabilities: Object.freeze([]),
  conflictsAndLimits: Object.freeze([]),
  admissionState: "NOT_EVALUATED",
  productionAuthority: false
});

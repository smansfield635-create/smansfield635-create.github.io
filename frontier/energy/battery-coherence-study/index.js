(()=>{'use strict';const root=document.documentElement;const lenses=['platform','engineering','evidence'];const groups=[...document.querySelectorAll('[data-local-lens-group]')];function setGroup(group,lens,focus=false){if(!lenses.includes(lens))lens='platform';const buttons=[...group.querySelectorAll('[data-local-lens-button]')];const panels=[...group.querySelectorAll('[data-local-lens-panel]')];group.dataset.activeLocalLens=lens;buttons.forEach(button=>{const active=button.dataset.localLensButton===lens;button.setAttribute('aria-selected',String(active));button.tabIndex=active?0:-1;if(active&&focus)button.focus();});panels.forEach(panel=>{panel.dataset.active=panel.dataset.localLensPanel===lens?'true':'false';});}groups.forEach(group=>{const buttons=[...group.querySelectorAll('[data-local-lens-button]')];buttons.forEach((button,index)=>{button.type='button';button.setAttribute('role','tab');button.addEventListener('click',()=>setGroup(group,button.dataset.localLensButton));button.addEventListener('keydown',event=>{if(!['ArrowLeft','ArrowRight','Home','End'].includes(event.key))return;event.preventDefault();let next=index;if(event.key==='ArrowRight')next=(index+1)%buttons.length;if(event.key==='ArrowLeft')next=(index-1+buttons.length)%buttons.length;if(event.key==='Home')next=0;if(event.key==='End')next=buttons.length-1;setGroup(group,buttons[next].dataset.localLensButton,true);});});setGroup(group,'platform');});root.dataset.batteryStudyInteractionContract='BATTERY_STUDY_NARRATIVE_LOCAL_LENSES_JS_TNT_v1';root.dataset.localLensGroups=String(groups.length);root.dataset.pageWideLensRemoved='true';})();
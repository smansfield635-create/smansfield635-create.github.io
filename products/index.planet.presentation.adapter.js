/* /products/index.planet.presentation.adapter.js
   RETIRED_PRODUCTS_CENTER_PLANET_PRESENTATION_ADAPTER_v1

   The speculative projection and halo interception mechanism is retired.
   Products now uses the accepted direct center-world host in index.planet.js
   and the accepted compass presentation contract in index.arena.css.
*/
(() => {
  "use strict";

  globalThis.DGB_PRODUCTS_CENTER_PLANET_PRESENTATION_ADAPTER = Object.freeze({
    initialized: true,
    contract: "RETIRED_PRODUCTS_CENTER_PLANET_PRESENTATION_ADAPTER_v1",
    executableProjectionMutation: false,
    executableHaloInterception: false,
    directPlanetHostPreserved: true,
    visualAcceptanceClaimed: false
  });
})();

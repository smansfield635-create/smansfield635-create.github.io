/* Diamond Gate Bridge commercial cosmos · DGB_BUILD_GOVERNANCE_FIBONACCI_COSMOS_v5 */
(()=>{"use strict";const MODULE="DGB_BUILD_GOVERNANCE_COSMOS",GOLDEN_ANGLE=Math.PI*(3-Math.sqrt(5)),FIELD_SEED=0x44474243,CONFIG=Object.freeze({mobileWidth:820,mobileDprCap:1,desktopDprCap:1.25,minimumStars:105,maximumStars:230,areaDivisor:6200,rogueRatio:.14,minimumSparkles:4,maximumSparkles:8,firstBurstMinMs:2600,firstBurstMaxMs:4600,burstDelayMinMs:1700,burstDelayMaxMs:3400,burstDurationMinMs:620,burstDurationMaxMs:980,sparkleFrameMs:125}),COLORS=Object.freeze(["255,248,224","154,217,225","234,208,131","170,155,224"]),state={initialized:false,destroyed:false,failed:false,documentVisible:!document.hidden,reducedMotion:false,mount:null,baseCanvas:null,overlayCanvas:null,baseContext:null,overlayContext:null,width:0,height:0,dpr:1,stars:[],sparkles:[],activeSparkles:[],burstTimer:0,frameTimer:0,motionQuery:null};if(globalThis[MODULE]?.initialized)return;const clamp=(v,min,max)=>Math.min(max,Math.max(min,v)),between=(r,min,max)=>min+r()*(max-min);function randomFactory(seed){let value=seed>>>0;return()=>{value+=0x6d2b79f5;let result=value;result=Math.imul(result^(result>>>15),result|1);result^=result+Math.imul(result^(result>>>7),result|61);return((result^(result>>>14))>>>0)/4294967296}}function makeCanvas(role){const c=document.createElement("canvas");c.dataset.dgbCosmosCanvas=role;c.setAttribute("aria-hidden","true");return c}function resolveMotion(){state.reducedMotion=Boolean(state.motionQuery?.matches)}function canRun(){return state.initialized&&!state.destroyed&&!state.failed&&state.documentVisible&&!state.reducedMotion}function configureSize(){state.width=Math.max(320,Math.round(innerWidth||document.documentElement.clientWidth));state.height=Math.max(480,Math.round(innerHeight||document.documentElement.clientHeight));const cap=state.width<=CONFIG.mobileWidth?CONFIG.mobileDprCap:CONFIG.desktopDprCap;state.dpr=Math.min(devicePixelRatio||1,cap);for(const c of[state.baseCanvas,state.overlayCanvas]){c.width=Math.max(1,Math.round(state.width*state.dpr));c.height=Math.max(1,Math.round(state.height*state.dpr));c.style.width=`${state.width}px`;c.style.height=`${state.height}px`}state.baseContext.setTransform(state.dpr,0,0,state.dpr,0,0);state.overlayContext.setTransform(state.dpr,0,0,state.dpr,0,0)}function generateStars(){const random=randomFactory(FIELD_SEED^state.width^(state.height<<7)),count=clamp(Math.round(state.width*state.height/CONFIG.areaDivisor),CONFIG.minimumStars,CONFIG.maximumStars),stars=[];for(let i=0;i<count;i++){const radius=Math.sqrt((i+.5)/count),angle=i*GOLDEN_ANGLE+between(random,-.08,.08);stars.push({x:clamp(.5+Math.cos(angle)*radius*.69+between(random,-.018,.018),.012,.988),y:clamp(.5+Math.sin(angle)*radius*.63+between(random,-.018,.018),.012,.988),radius:between(random,.5,1.75),alpha:between(random,.34,.92),color:COLORS[Math.floor(random()*COLORS.length)],rogue:random()<CONFIG.rogueRatio})}state.stars=stars;state.sparkles=stars.filter(s=>s.rogue).slice(0,CONFIG.maximumSparkles*3)}function drawStar(ctx,star,alpha=star.alpha,scale=1){const x=star.x*state.width,y=star.y*state.height,r=star.radius*scale;ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fillStyle=`rgba(${star.color},${alpha})`;ctx.shadowColor=`rgba(${star.color},${alpha*.8})`;ctx.shadowBlur=r*4;ctx.fill();ctx.shadowBlur=0}function drawBase(){state.baseContext.clearRect(0,0,state.width,state.height);for(const s of state.stars)drawStar(state.baseContext,s)}function drawSparkles(){state.overlayContext.clearRect(0,0,state.width,state.height);const now=performance.now();state.activeSparkles=state.activeSparkles.filter(i=>now<i.endsAt);for(const item of state.activeSparkles){const p=clamp(1-(item.endsAt-now)/item.duration,0,1),pulse=Math.sin(p*Math.PI);drawStar(state.overlayContext,item.star,item.star.alpha*pulse,1+pulse*1.9)}if(!state.activeSparkles.length){clearInterval(state.frameTimer);state.frameTimer=0}}function scheduleBurst(first=false){clearTimeout(state.burstTimer);if(!canRun())return;const random=randomFactory(FIELD_SEED^Date.now()),delay=first?between(random,CONFIG.firstBurstMinMs,CONFIG.firstBurstMaxMs):between(random,CONFIG.burstDelayMinMs,CONFIG.burstDelayMaxMs);state.burstTimer=setTimeout(()=>{if(!canRun()||!state.sparkles.length)return;const qty=clamp(Math.floor(between(random,CONFIG.minimumSparkles,CONFIG.maximumSparkles+1)),1,state.sparkles.length),pool=[...state.sparkles].sort(()=>random()-.5).slice(0,qty),now=performance.now();state.activeSparkles=pool.map(star=>{const duration=between(random,CONFIG.burstDurationMinMs,CONFIG.burstDurationMaxMs);return{star,duration,endsAt:now+duration}});if(!state.frameTimer)state.frameTimer=setInterval(drawSparkles,CONFIG.sparkleFrameMs);scheduleBurst(false)},delay)}function stop(){clearTimeout(state.burstTimer);clearInterval(state.frameTimer);state.burstTimer=0;state.frameTimer=0;state.activeSparkles=[];state.overlayContext?.clearRect(0,0,state.width,state.height)}function resize(){if(!state.initialized||state.destroyed)return;configureSize();generateStars();drawBase()}function onVisibility(){state.documentVisible=!document.hidden;if(canRun())scheduleBurst(true);else stop()}function onMotion(){resolveMotion();if(canRun())scheduleBurst(true);else stop()}function installCommercialTerms(){if(location.pathname!=="/build/"&&location.pathname!=="/build/index.html")return;const body=document.querySelector(".pricing-disclosure .section-disclosure-body");if(!body||body.querySelector("[data-dgb-inspection-offer]"))return;const box=document.createElement("div");box.dataset.dgbInspectionOffer="";box.className="launch-note";box.style.borderColor="rgba(185,241,209,.3)";box.style.background="linear-gradient(145deg,rgba(20,65,48,.2),rgba(7,11,17,.55))";box.innerHTML='<span class="status live">Build Safely · Pay 50/50</span><p><strong>Half to begin. Half only after you accept the finished version.</strong> For qualifying Custom projects—especially work on an existing website—Diamond Gate can build the new version in a separate project repository and inspection environment. Your live website stays untouched while you review the work. After you accept the finished version and make the final payment, Diamond Gate moves the approved version live.</p><p class="fine">The exact staging, repository and deployment arrangement depends on the customer\'s existing hosting and technical environment and is confirmed before work begins.</p>';const intro=body.querySelector(".disclosure-lede");if(intro)intro.insertAdjacentElement("afterend",box);else body.prepend(box)}function initialize(){try{state.mount=document.querySelector("[data-dgb-fibonacci-cosmos]");if(!state.mount)throw new Error("DGB_FIBONACCI_COSMOS_MOUNT_NOT_FOUND");state.mount.style.zIndex="0";state.mount.style.opacity=".9";const content=document.querySelector("main");if(content){content.style.position="relative";content.style.zIndex="1"}state.baseCanvas=state.mount.querySelector('[data-dgb-cosmos-canvas="base"]')||makeCanvas("base");state.overlayCanvas=state.mount.querySelector('[data-dgb-cosmos-canvas="sparkle"]')||makeCanvas("sparkle");if(!state.baseCanvas.isConnected)state.mount.append(state.baseCanvas);if(!state.overlayCanvas.isConnected)state.mount.append(state.overlayCanvas);state.baseContext=state.baseCanvas.getContext("2d",{alpha:true,desynchronized:true});state.overlayContext=state.overlayCanvas.getContext("2d",{alpha:true,desynchronized:true});if(!state.baseContext||!state.overlayContext)throw new Error("DGB_FIBONACCI_COSMOS_CONTEXT_UNAVAILABLE");state.motionQuery=matchMedia?.("(prefers-reduced-motion: reduce)")||null;resolveMotion();configureSize();generateStars();drawBase();installCommercialTerms();state.initialized=true;globalThis[MODULE]=Object.freeze({initialized:true,sourceModel:"ARCHCOIN_FIBONACCI_PHYLLOTAXIS_FIELD_v1",geometryModel:"golden-angle-square-root-jitter-with-rogue-stars",resize,stop});addEventListener("resize",resize,{passive:true});document.addEventListener("visibilitychange",onVisibility);state.motionQuery?.addEventListener?.("change",onMotion);if(canRun())scheduleBurst(true)}catch(error){state.failed=true;console.warn("DGB Fibonacci cosmos unavailable",error)}}if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",initialize,{once:true});else initialize()})();
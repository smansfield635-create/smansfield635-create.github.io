/** H_EARTH_RUN_8E_R2_CANONICAL_LIVE_RENDER_PACKAGE_v3_AWARDS_CONVERGENCE */
import {getHEarthRun8ER2ImmutableLiveRenderPackage as getRawPackage,buildHEarthRun8ER2ImmutableLiveRenderPackage as buildRawPackage,H_EARTH_OW01_LIVE_RENDER_PACKAGE_OCCURRENCE_ID,evaluateHEarthRun8ER2ImmutableLiveRenderPackage} from './live-render-package.run8e-r2.js';
const GRID_SCALE=16777216;
const FLOAT_BUFFER_NAMES=Object.freeze(['positions','normals','baseColorsLinear','materialParameters']);
const HASH_BUFFER_ORDER=Object.freeze(['positions','normals','baseColorsLinear','materialParameters','materialModelCodes','surfaceClassCodes','primitiveIndices','roleCodes','indices']);
const freezeArray=v=>Object.freeze(Array.from(v));const freezeRecord=v=>Object.freeze(v);const clamp01=v=>Math.min(1,Math.max(0,Number(v)||0));const mix=(a,b,t)=>Number(a)+(Number(b)-Number(a))*clamp01(t);
function canonicalNumber(v){if(!Number.isFinite(v))throw new TypeError('R2_CANONICAL_NONFINITE_NUMBER');const r=Math.round(v*GRID_SCALE)/GRID_SCALE;return Object.is(r,-0)?0:r}
function canonicalizeBuffers(buffers){const o={};for(const n of HASH_BUFFER_ORDER){const v=buffers[n];o[n]=FLOAT_BUFFER_NAMES.includes(n)?freezeArray(v.map(canonicalNumber)):freezeArray(v)}return freezeRecord(o)}
function createHashWriter(){let h=0x811c9dc5;const b=new ArrayBuffer(8),d=new DataView(b),e=new TextEncoder(),byte=v=>{h^=v&255;h=Math.imul(h,0x01000193)>>>0};return{string(v){for(const x of e.encode(String(v)))byte(x);byte(255)},number(v){d.setFloat64(0,v,true);for(let i=0;i<8;i++)byte(d.getUint8(i))},numbers(v){for(const x of v)this.number(x);byte(254)},digest(){return h.toString(16).padStart(8,'0')}}}
function coherentEnvironmentDefaults(environment={}){const sh=Array.isArray(environment.skyHorizonColor)?environment.skyHorizonColor.slice(0,3).map(Number):[18,42,72],sz=Array.isArray(environment.skyZenithColor)?environment.skyZenithColor.slice(0,3).map(Number):[8,24,48],sun=Array.isArray(environment.sunColor)?environment.sunColor.slice(0,3).map(Number):[255,220,180],scale=sh.concat(sz,sun).some(v=>v>1)?255:1,daylight=clamp01(Number(environment.sunIntensity??0)),sunY=Math.abs(Number(environment?.sunDirection?.y??1)),lowSun=clamp01(1-sunY*1.55)*Math.sqrt(daylight),dayH=[.48,.70,.90],dayZ=[.07,.29,.64],nightH=[.035,.075,.14],nightZ=[.012,.028,.075];const horizon=[0,1,2].map(i=>canonicalNumber(clamp01(mix(mix(nightH[i],dayH[i],daylight),sh[i]/scale,.12)*(1-lowSun*.08)+sun[i]/scale*lowSun*.52)*scale));const zenith=[0,1,2].map(i=>canonicalNumber(clamp01(mix(mix(nightZ[i],dayZ[i],daylight),sz[i]/scale,.12)*(1-lowSun*.03)+sun[i]/scale*lowSun*.10)*scale));const sourceFogStart=Number(environment.fogStartDistance??720),sourceFogFalloff=Number(environment.fogFalloff??0.001),sourceFogMax=Number(environment.maximumFogFactor??0.65),sourceDesaturation=Number(environment.distanceDesaturationStrength??0.4),fogStart=Math.min(sourceFogStart,430),fogFalloff=Math.max(sourceFogFalloff,0.00155),fogMax=Math.max(sourceFogMax,0.80),desaturation=Math.max(sourceDesaturation,0.62);return freezeRecord({...environment,skyHorizonColor:freezeArray(horizon),skyZenithColor:freezeArray(zenith),fogStartDistance:fogStart,fogFalloff,maximumFogFactor:fogMax,distanceDesaturationStrength:desaturation,skyLightingCorrelation:freezeRecord({model:'SAME_SOLAR_STATE_DAYLIGHT_LOW_SUN_AND_AWARDS_AERIAL_PERSPECTIVE',daylight,lowSun,sourceSunIntensity:Number(environment.sunIntensity??0),sourceSunDirectionY:Number(environment?.sunDirection?.y??0),sourceAuthorityMutation:false}),aerialPerspectiveArticulation:freezeRecord({fogStartDistance:fogStart,fogFalloff,maximumFogFactor:fogMax,distanceDesaturationStrength:desaturation,purpose:'SEPARATE_FOREGROUND_MIDGROUND_AND_CONTINENTAL_DISTANCE_WITHOUT_GEOMETRY_COST'})})}
function buildCanonicalPackage(raw=getRawPackage()){if(raw?.eligible!==true)return raw;const buffers=canonicalizeBuffers(raw.buffers),hash=createHashWriter();hash.string(raw.sourceAuthorities.run8ER2ContractId);hash.string(raw.sourceAuthorities.packet002TransferContractId);hash.string(raw.sourceAuthorities.run8CMaterialContractId);hash.string(raw.sourceAuthorities.atmosphereContractId);for(const id of raw.primitiveIds)hash.string(id);for(const n of HASH_BUFFER_ORDER)hash.numbers(buffers[n]);const digest=hash.digest(),packageRecord=freezeRecord({...raw,packageIdentity:`H_EARTH_RUN_8E_R2_LIVE_RENDER_PACKAGE_${digest.toUpperCase()}`,contentDigest:`fnv1a32:${digest}`,revision:2,buffers,environmentDefaults:coherentEnvironmentDefaults(raw.environmentDefaults),sourceAuthorities:freezeRecord({...raw.sourceAuthorities,numericIdentityBoundary:'SHARED_COMPLETE_PACKAGE_BUFFER_BOUNDARY',numericCanonicalizationLaw:'ROUND_TO_BINARY_GRID_2^-24_AND_NORMALIZE_NEGATIVE_ZERO',canonicalizedFloatBuffers:FLOAT_BUFFER_NAMES,atmosphereTimeBinding:raw.packageOccurrenceId===H_EARTH_OW01_LIVE_RENDER_PACKAGE_OCCURRENCE_ID?'BROWSER_LOCAL_CLOCK_AT_PACKAGE_CONSTRUCTION':'PACKAGE_DECLARED_TIME',skyLightingCorrelation:'SAME_SOLAR_STATE_DAYLIGHT_LOW_SUN_AND_AWARDS_AERIAL_PERSPECTIVE'})});const evaluation=evaluateHEarthRun8ER2ImmutableLiveRenderPackage(packageRecord);return evaluation.eligible===true?packageRecord:freezeRecord({eligible:false,status:'RUN_8E_R2_CANONICAL_LIVE_RENDER_PACKAGE_REJECTED',contractId:raw.contractId,issues:evaluation.issues})}
function browserLocalClockHours(){const v=new Date();return v.getHours()+v.getMinutes()/60+v.getSeconds()/3600}
let cachedPackage=null,cachedOW01Package=null;export function getHEarthRun8ER2CanonicalLiveRenderPackage(){if(!cachedPackage)cachedPackage=buildCanonicalPackage();return cachedPackage}export function getHEarthOW01CanonicalLiveRenderPackageOccurrence(){if(!cachedOW01Package)cachedOW01Package=buildCanonicalPackage(buildRawPackage({packageOccurrenceId:H_EARTH_OW01_LIVE_RENDER_PACKAGE_OCCURRENCE_ID,timeOfDayHours:browserLocalClockHours()}));return cachedOW01Package}export default getHEarthRun8ER2CanonicalLiveRenderPackage;

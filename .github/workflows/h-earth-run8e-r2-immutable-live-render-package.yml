name: H-Earth Run 8E R2A Cleanup Boundary

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cleanup-boundary:
    runs-on: ubuntu-latest
    steps:
      - name: Preserve cleanup boundary
        run: |
          echo 'RUN_8E_R2A_CLEANUP_BOUNDARY'
          echo 'TEMPORARY_SCRIPT_REMOVAL_AND_FINAL_READ_ONLY_WORKFLOW_INSTALLATION_PENDING'

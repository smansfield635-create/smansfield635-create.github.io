# M1 — Method Procedure Field

Status: `CONSTRUCTED_NONPUBLIC_PENDING_HARD_GATE_EVALUATION`.

M1 is the first bounded manifestation experiment under
`METHODS_MODELS_BOUNDED_MANIFESTATION_EXPERIMENT_AUTHORITY_v1`.

It manifests only `METHODS`, all fifteen source-bound method stages, and the
dependency edges declared by `method-content-registry.v1.json::sequence[].requires`.
The binding adapter is deterministic and source-bound; it does not inherit legacy
presentation taxonomy or slot geometry.

Only drawn connectors carry dependency meaning. Position, distance, and visual grouping
are nonsemantic. Selecting the bound node itself performs `FOCUS_MOVE` only and leaves
the scientific-state hash unchanged. The persistent orientation panel exposes the
required Q context. Native button semantics provide the accessible route, and every
commit emits the required non-gate action → target → transformation → information-delta
trace.

No public Methods file is changed. No omitted authorized object is denied or
reclassified. No ontology, T*, B*, hard gate, scientific relation, claim ceiling, or
public default is modified by this candidate.

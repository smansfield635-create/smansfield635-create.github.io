# M1 Compass-Integrated Runtime Review v1

Operation: `M1_COMPASS_INTEGRATED_RUNTIME_REVIEW_v1`

Immutable runtime-review subject:

`0c84cc4bb5b75245d225a47fde68d3de879cdaed`

This package is evidence-only. It does not modify the runtime-review subject, the accepted isolated M1, M1R2, the object-projection registry, Research content, Laws content, or any public/live route.

The runtime question is bounded to: `Does the constructed M1_COMPASS_INTEGRATED candidate actually work when executed?`

The review exercises entry, return restoration, field-state persistence, active-object focus, neighbor retention, L0/L1 transitions, pointer drag/swipe, wheel navigation, object and viewport keyboard navigation, Previous/Next controls, responsive projection, continuity state, entry-context restoration, invalid-target rejection, corrupt-registry fail-closed behavior, and registry-fetch failure behavior.

Scientific identity and relation structure are snapshotted at boot and required to remain unchanged throughout navigation tests.

This package does not perform perceptual acceptance, baseline-regression closure, exact-head terminal certification, promotion, or public readiness adjudication.

/** H_EARTH_GRATITUDE_AUDRALIA_LANDSCAPE_PREVIEW_GEN311_v1 */
import { isHEarthNeutralPrimitiveRecord,isHEarthAABB3D,mergeHEarthGeometryBounds } from './geometry-kernel.js';
import { constructHEarthRun8BSuccessorTerrainAndMountain } from './geometry-successor-terrain.run8b.js';
import { constructHEarthFunctionalShorelineGeometry } from './geometry-shoreline.js';
import { constructHEarthDistantContextGeometry } from './geometry-distant-context.js';
import { H_EARTH_FUNCTIONAL_LANDSCAPE_REALIZATION_PLAN } from '../../../../h-earth-3d/integration/h-earth.landscape-realization-planner.js';
import { H_EARTH_WORLD_MANIFOLD_TOPOLOGY_SOURCE_ID } from '../../../../h-earth-3d/terrain/h-earth.world-manifold-domain.js';
import { H_EARTH_GEN311_REGIONAL_ARTICULATION_CONTRACT_ID,deriveHEarthGen311RegionalArticulation } from '../../../../h-earth-3d/terrain/h-earth.successor-terrain-field.run8b.js';
import { buildHEarthWorldManifoldRepresentationPlan } from '../../../../h-earth-3d/integration/h-earth.world-representation-plan.js';
import { admitHEarthWorldManifoldUnion } from '../../../../h-earth-3d/integration/h-earth.world-manifold-union-admission.js';
const freeze=(v,s=new WeakSet())=>{if(v===null||typeof v!=='object'||Object.isFrozen(v)||s.has(v))return v;s.add(v);Object.values(v).forEach(x=>freeze(x,s));return Object.freeze(v)};const canonical=values=>Object.freeze([...new Set((values??[]).filter(v=>typeof v==='string'&&v.length))].sort());
export const H_EARTH_FUNCTIONAL_LANDSCAPE_PREVIEW_CONTRACT_ID='H_EARTH_GRATITUDE_AUDRALIA_LANDSCAPE_PREVIEW_GEN310_v1';
export const H_EARTH_GEN311_LANDSCAPE_PREVIEW_EXTENSION_CONTRACT_ID='H_EARTH_GRATITUDE_AUDRALIA_LANDSCAPE_PREVIEW_GEN311_v1';
function regionalWitnesses(){const points=[];for(let z=-224;z<=-48;z+=32)for(let x=-160;x<=160;x+=40)points.push([x,z]);const samples=points.map(([x,z])=>deriveHEarthGen311RegionalArticulation(x,z)).filter(s=>s.valid===true),classes=canonical(samples.map(s=>s.landformClass));const maxima={ridge:0,pass:0,valley:0,watershed:0,foothill:0};for(const s of samples){maxima.ridge=Math.max(maxima.ridge,s.ridgeSignal);maxima.pass=Math.max(maxima.pass,s.passSignal);maxima.valley=Math.max(maxima.valley,s.valleySignal);maxima.watershed=Math.max(maxima.watershed,s.watershedSignal);maxima.foothill=Math.max(maxima.foothill,s.foothillSignal);}return freeze({contractId:H_EARTH_GEN311_REGIONAL_ARTICULATION_CONTRACT_ID,sampleCount:samples.length,landformClasses:classes,landformClassCount:classes.length,maxima:freeze(maxima),samples:freeze(samples),derivedFromCanonicalWorldManifold:true,topologyMutation:false,elevationMutation:false});}
export function previewHEarthFunctionalLandscape({cameraWorld={x:0,y:8,z:-40}}={}){const representationPlan=buildHEarthWorldManifoldRepresentationPlan({cameraWorld}),terrainResult=constructHEarthRun8BSuccessorTerrainAndMountain(),shoreline=constructHEarthFunctionalShorelineGeometry(),distantContext=constructHEarthDistantContextGeometry({cameraWorld}),regionalDevelopment=regionalWitnesses(),issues=[];if(representationPlan.eligible!==true)issues.push(...representationPlan.issues);if(terrainResult?.ok!==true||!terrainResult.primitive)issues.push('COMPONENT_INVALID:terrain');if(shoreline?.ok!==true)issues.push('COMPONENT_INVALID:shoreline');if(distantContext?.ok!==true)issues.push('COMPONENT_INVALID:distantContext');if(regionalDevelopment.sampleCount<20)issues.push('GEN311_REGIONAL_WITNESS_COVERAGE_INVALID');if(regionalDevelopment.contractId!==H_EARTH_GEN311_REGIONAL_ARTICULATION_CONTRACT_ID)issues.push('GEN311_REGIONAL_ARTICULATION_IDENTITY_INVALID');if(distantContext?.geographicIdentity!=='AUDRALIA'||distantContext?.playableRegionIdentity!=='GRATITUDE'||distantContext?.oceanSectorEmptinessEnforced!==true)issues.push('GRATITUDE_AUDRALIA_CONTEXT_INVALID');if(distantContext?.oceanVisualContinuationMaterialized!==true)issues.push('OCEAN_VISUAL_CONTINUATION_MISSING');const farClasses=new Set((distantContext?.primitives??[]).map(p=>p.metadata?.farSurfaceClass));if(!farClasses.has('LAND')||!farClasses.has('OCEAN')||farClasses.size!==2)issues.push('RECIPROCAL_FAR_SURFACE_CLASSES_INVALID');if((distantContext?.primitives??[]).some(p=>p.metadata?.navigable!==false||p.metadata?.collisionAuthority!==false||p.metadata?.accessibleRegionExpansion!==false))issues.push('FAR_CONTINUATION_AUTHORITY_VIOLATION');const nearPrimitives=terrainResult?.primitive?[terrainResult.primitive]:[],midPrimitives=shoreline?.primitives??[],farPrimitives=distantContext?.primitives??[],union=admitHEarthWorldManifoldUnion({representationPlan,nearPrimitives,midPrimitives,farPrimitives});if(union.valid!==true)issues.push(...union.issues);const primitives=[...nearPrimitives,...midPrimitives,...farPrimitives];if(!primitives.every(isHEarthNeutralPrimitiveRecord))issues.push('COMPONENT_PRIMITIVES_INVALID');const bounds=primitives.length?mergeHEarthGeometryBounds(primitives.map(p=>p.geometry.bounds)):null;if(!isHEarthAABB3D(bounds))issues.push('AGGREGATE_BOUNDS_INVALID');const primitiveIds=primitives.map(p=>p.primitiveId);if(new Set(primitiveIds).size!==primitiveIds.length)issues.push('DUPLICATE_PRIMITIVE_ID');const plan=H_EARTH_FUNCTIONAL_LANDSCAPE_REALIZATION_PLAN,semanticAddressIds=canonical(plan.chunks.flatMap(c=>c.memberAddressIds)),terrainAddressIds=canonical(plan.chunks.flatMap(c=>c.terrainMemberAddressIds)),shorelineWaterAddressIds=canonical(plan.chunks.flatMap(c=>c.shorelineWaterMemberAddressIds)),proxySummarizedAddressIds=canonical(plan.chunks.flatMap(c=>c.proxyMemberAddressIds)),formationIds=canonical(plan.chunks.flatMap(c=>c.formationIds));if(semanticAddressIds.length!==256)issues.push('SEMANTIC_ADDRESS_COUNT_INVALID');if(terrainAddressIds.length!==124)issues.push('TERRAIN_ADDRESS_COUNT_INVALID');if(shorelineWaterAddressIds.length!==96)issues.push('SHORELINE_ADDRESS_COUNT_INVALID');if(proxySummarizedAddressIds.length!==36)issues.push('PROXY_ADDRESS_COUNT_INVALID');return freeze({ok:issues.length===0,status:issues.length?'WORLD_MANIFOLD_NEUTRAL_PREVIEW_FAILED':'WORLD_MANIFOLD_NEUTRAL_PREVIEW_COMPLETE',contractId:H_EARTH_FUNCTIONAL_LANDSCAPE_PREVIEW_CONTRACT_ID,gen311ExtensionContractId:H_EARTH_GEN311_LANDSCAPE_PREVIEW_EXTENSION_CONTRACT_ID,developmentPhase:'REGIONAL_ELABORATION',developmentTarget:'GRATITUDE_GEOGRAPHIC_AND_ENVIRONMENTAL_ARTICULATION',geographicIdentity:freeze({playableRegion:'GRATITUDE',continentalContext:'AUDRALIA',climate:'WARM_SUBTROPICAL_COASTAL'}),realizationPlanContractId:plan.contractId,representationPlan,regionalDevelopment,worldManifoldUnion:union,topologySourceId:H_EARTH_WORLD_MANIFOLD_TOPOLOGY_SOURCE_ID,componentResults:{terrain:terrainResult,shoreline,distantContext},primitiveCount:primitives.length,primitiveIds,primitives,bounds,semanticAddressCount:semanticAddressIds.length,semanticAddressIds,terrainAddressCount:terrainAddressIds.length,terrainAddressIds,shorelineWaterAddressCount:shorelineWaterAddressIds.length,shorelineWaterAddressIds,proxySummarizedAddressCount:proxySummarizedAddressIds.length,proxySummarizedAddressIds,formationIds,semanticIdentityIndependentOfPhysicalGranularity:true,continuousWorldManifold:union.valid,oceanFacingEmptinessPreserved:distantContext?.oceanSectorEmptinessEnforced===true,oceanVisualContinuationMaterialized:distantContext?.oceanVisualContinuationMaterialized===true,reciprocalFarSurfaceClasses:freeze([...farClasses].sort()),oppositeShoreFabricationProhibited:true,independentComponentGeographyAuthority:false,canonicalWorldFieldProtected:true,regionalArticulationDerivedOnly:true,accessibleRegionExpansion:false,admitted:false,WestAdmissionPerformed:false,compositorNodeCreated:false,renderInstanceCreated:false,issues});}
export const H_EARTH_FUNCTIONAL_LANDSCAPE_NEUTRAL_PREVIEW=previewHEarthFunctionalLandscape();

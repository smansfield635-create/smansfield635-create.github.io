# Methods Role 6 operation packet carrier

This directory serializes and independently reproduces one future Role 6 operation packet. It does not assign Role 6, activate permanent authority, execute the formal-record audit, authorize the user assignment, adopt outputs, assign a next role, merge, deploy, or release.

`PACKET_BUILDER` may serialize the user-origin target and emit a packet receipt. `FRESH_PACKET_VERIFIER` must independently recompute the packet hash, dependency closure, corpus identity, exact changed-path map, and stopping boundary from a clean exact-head checkout. Neither function may repair the packet or acquire Role 6 authority.

The verified generic first-assignment infrastructure at exact head `0d601cb71dac863130b934f387425085fdf54e52` is immutable. Assignment state artifacts and substantive Role 6 audit outputs are prohibited in this carrier.

import {getHEarth3DSharedShorelineBoundary} from '/showroom/globe/h-earth/environment.js';
const CONTRACT=Object.freeze({id:'AWARDS_WORLD_LIVING_OBJECT_TRUE_3D_V1',cycle:'B_WORLD',claim:'The browser can hold a world.',recognizableObject:'H_EARTH_AUDRALIA_WORLD',renderer:'WEBGL_3D',signatureEvent:'ENVIRONMENT_DEPTH_RESOLVE',eventCount:1,sourceBinding:Object.freeze({hEarthEnvironment:Object.freeze({path:'/showroom/globe/h-earth/environment.js',blob:'3f3bc750b0e1a87531e0ea425dc0ac343fb18381',provider:'getHEarth3DSharedShorelineBoundary'}),hEarthRenderer:Object.freeze({path:'/showroom/globe/h-earth/renderer.js',blob:'799d37cec5244e6aa19b7d94dffe37e182b85884'}),audralia:Object.freeze({path:'/showroom/globe/audralia/index.html',blob:'a10fff1420681abdc377e5c06d8620a7b4f019ac'})}),lifecycle:Object.freeze(['REAR_INERT','APPROACHING','FOREGROUND_REST','SIGNATURE_PLAY','FOREGROUND_IDLE','SELECT_RESPONSE','READER_OPEN','RETURN_RESTORING'])});
const VS=`attribute vec3 a_position;attribute vec3 a_normal;attribute vec4 a_color;uniform float u_yaw,u_pitch,u_scale,u_aspect,u_cam,u_zshift;varying vec3 v_n;varying vec4 v_c;void main(){float cy=cos(u_yaw),sy=sin(u_yaw),cx=cos(u_pitch),sx=sin(u_pitch);vec3 p=a_position;vec3 n=a_normal;p.z+=u_zshift;p=vec3(cy*p.x+sy*p.z,p.y,-sy*p.x+cy*p.z);n=vec3(cy*n.x+sy*n.z,n.y,-sy*n.x+cy*n.z);p=vec3(p.x,cx*p.y-sx*p.z,sx*p.y+cx*p.z);n=vec3(n.x,cx*n.y-sx*n.z,sx*n.y+cx*n.z);p*=u_scale;float z=max(.3,p.z+u_cam);gl_Position=vec4(p.x*1.7/u_aspect,p.y*1.7,z-1.0,z);v_n=normalize(n);v_c=a_color;}`;const FS=`precision mediump float;varying vec3 v_n;varying vec4 v_c;void main(){vec3 L=normalize(vec3(-.25,.82,-.5));float d=.28+.72*max(0.0,dot(normalize(v_n),L));gl_FragColor=vec4(v_c.rgb*d,v_c.a);}`;
function sh(gl,t,s){const x=gl.createShader(t);gl.shaderSource(x,s);gl.compileShader(x);if(!gl.getShaderParameter(x,gl.COMPILE_STATUS))throw new Error(gl.getShaderInfoLog(x));return x}function prog(gl){const p=gl.createProgram();gl.attachShader(p,sh(gl,gl.VERTEX_SHADER,VS));gl.attachShader(p,sh(gl,gl.FRAGMENT_SHADER,FS));gl.linkProgram(p);if(!gl.getProgramParameter(p,gl.LINK_STATUS))throw new Error(gl.getProgramInfoLog(p));return p}function M(){return{p:[],n:[],c:[]}}function T(m,a,b,c,n,col){for(const q of[a,b,c]){m.p.push(...q);m.n.push(...n);m.c.push(...col)}}function Q(m,a,b,c,d,n,col){T(m,a,b,c,n,col);T(m,a,c,d,n,col)}
function discoverSamples(v,seen=new Set()){if(!v||typeof v!=='object'||seen.has(v))return null;seen.add(v);if(Array.isArray(v)&&v.length>=3){if(v.every(q=>Array.isArray(q)&&q.length>=2&&Number.isFinite(+q[0])&&Number.isFinite(+q[1])))return v.map(q=>[+q[0],+q[1]]);if(v.every(q=>q&&Number.isFinite(+q.x)&&Number.isFinite(+(q.z??q.y))))return v.map(q=>[+q.x,+(q.z??q.y)])}for(const x of Object.values(v)){const f=discoverSamples(x,seen);if(f)return f}return null}function findWater(v,seen=new Set()){if(!v||typeof v!=='object'||seen.has(v))return null;seen.add(v);for(const[k,x]of Object.entries(v)){if(/water.*(elevation|height|level)|elevation.*water/i.test(k)&&Number.isFinite(+x))return +x}for(const x of Object.values(v)){const f=findWater(x,seen);if(f!=null)return f}return null}
function sceneFromBoundary(boundary){const raw=discoverSamples(boundary)||Array.from({length:25},(_,i)=>[-96+i*8,-96+8*Math.sin(i*.65)]);let xmin=Infinity,xmax=-Infinity,zmin=Infinity,zmax=-Infinity;for(const[x,z]of raw){xmin=Math.min(xmin,x);xmax=Math.max(xmax,x);zmin=Math.min(zmin,z);zmax=Math.max(zmax,z)}const sx=Math.max(1,xmax-xmin),sz=Math.max(1,zmax-zmin),pts=raw.map(([x,z])=>[(x-(xmin+xmax)/2)/sx*3.0,(z-(zmin+zmax)/2)/Math.max(sz,sx)*1.35]);const water=findWater(boundary)??.68,m=M();for(let i=0;i<pts.length-1;i++){const [x0,z0]=pts[i],[x1,z1]=pts[i+1],h0=.14+.24*(.5+.5*Math.sin(i*.83)),h1=.14+.24*(.5+.5*Math.sin((i+1)*.83));T(m,[x0,h0,z0],[x1,h1,z1],[x1,.02,z1],[0,.65,-.75],[.18,.37,.24,1]);T(m,[x0,h0,z0],[x1,.02,z1],[x0,.02,z0],[0,.65,-.75],[.16,.31,.21,1]);Q(m,[x0,.015,z0-.08],[x1,.015,z1-.08],[x1,.015,z1+.04],[x0,.015,z0+.04],[0,1,0],[.58,.49,.30,1])}Q(m,[-1.9,-.035,-1.1],[1.9,-.035,-1.1],[1.9,-.035,.15],[-1.9,-.035,.15],[0,1,0],[.035,.30,.42,1]);for(let i=0;i<11;i++){const x=-1.65+i*.33,h=.35+.5*(.5+.5*Math.sin(i*1.31));T(m,[x-.32,.06,.55],[x+.32,.06,.55],[x,.06+h,.78],[0,.45,-.89],i%2?[.18,.32,.24,1]:[.25,.39,.27,1])}return{mesh:m,sampleCount:raw.length,waterElevation:water}}
function up(gl,m){const o={count:m.p.length/3};for(const[k,d]of[['p',m.p],['n',m.n],['c',m.c]]){o[k]=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,o[k]);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(d),gl.STATIC_DRAW)}return o}function bind(gl,p,o){for(const[n,k,s]of[['a_position','p',3],['a_normal','n',3],['a_color','c',4]]){const l=gl.getAttribLocation(p,n);gl.bindBuffer(gl.ARRAY_BUFFER,o[k]);gl.enableVertexAttribArray(l);gl.vertexAttribPointer(l,s,gl.FLOAT,false,0,0)}}
export function mountWorldLivingObject(root,options={}){if(!root||typeof root.replaceChildren!=='function')throw new TypeError('WORLD_ROOT_REQUIRED');const boundary=getHEarth3DSharedShorelineBoundary(),built=sceneFromBoundary(boundary),doc=root.ownerDocument||document,canvas=doc.createElement('canvas');canvas.className='awards-true3d-canvas awards-world-true3d';canvas.style.cssText='display:block;width:100%;height:100%;min-height:220px;pointer-events:none';canvas.setAttribute('aria-hidden','true');root.replaceChildren(canvas);const gl=canvas.getContext('webgl2',{alpha:true,antialias:true})||canvas.getContext('webgl',{alpha:true,antialias:true});if(!gl)throw new Error('WORLD_WEBGL_REQUIRED');const p=prog(gl),gpu=up(gl,built.mesh);gl.useProgram(p);gl.enable(gl.DEPTH_TEST);gl.enable(gl.CULL_FACE);bind(gl,p,gpu);const U=n=>gl.getUniformLocation(p,n),uy=U('u_yaw'),upit=U('u_pitch'),us=U('u_scale'),ua=U('u_aspect'),uc=U('u_cam'),uz=U('u_zshift');let state='FOREGROUND_REST',phase='rest',destroyed=false,raf=0,start=performance.now(),sig=-1;const reduced=options.reducedMotion??matchMedia?.('(prefers-reduced-motion: reduce)')?.matches??false;function resize(){const d=Math.min(2,devicePixelRatio||1),w=Math.max(1,Math.round(canvas.clientWidth*d)),h=Math.max(1,Math.round(canvas.clientHeight*d));if(w!==canvas.width||h!==canvas.height){canvas.width=w;canvas.height=h}gl.viewport(0,0,w,h);return w/h}function draw(t){if(destroyed)return;const aspect=resize(),elapsed=(t-start)/1000,st=sig<0?99:(t-sig)/1000,resolve=reduced?1:(sig>=0?Math.min(1,st/.9)*Math.max(0,Math.min(1,(3.0-st)/.6)):0);if(sig>=0&&st>3.3){sig=-1;phase='rest';state='FOREGROUND_IDLE'}gl.clearColor(.01,.025,.04,0);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);gl.uniform1f(uy,-.18+.045*Math.sin(elapsed*.28));gl.uniform1f(upit,-.32+.07*resolve);gl.uniform1f(us,.92+.07*resolve);gl.uniform1f(ua,aspect);gl.uniform1f(uc,4.1);gl.uniform1f(uz,-.18*resolve);gl.drawArrays(gl.TRIANGLES,0,gpu.count);raf=requestAnimationFrame(draw)}raf=requestAnimationFrame(draw);function S(x){if(!CONTRACT.lifecycle.includes(x))throw new Error('WORLD_INVALID_STATE');state=x;return x}function playSignature(){S('SIGNATURE_PLAY');phase='resolve';sig=performance.now();return sig}function selectResponse(){S('SELECT_RESPONSE')}function readerOpen(){S('READER_OPEN')}function restore(){S('RETURN_RESTORING');sig=-1;phase='rest';setTimeout(()=>{if(!destroyed)S('FOREGROUND_REST')},reduced?0:250)}function setLifecycle(x){S(x)}function inspect(){return Object.freeze({contract:CONTRACT.id,state,phase,reducedMotion:!!reduced,webglContexts:1,renderer:'WEBGL_3D',recognizableObject:CONTRACT.recognizableObject,signatureEvent:CONTRACT.signatureEvent,eventCount:1,shorelineSampleCount:built.sampleCount,waterElevation:built.waterElevation,sourceBinding:CONTRACT.sourceBinding})}function destroy(){destroyed=true;cancelAnimationFrame(raf);for(const k of['p','n','c'])gl.deleteBuffer(gpu[k]);gl.deleteProgram(p);gl.getExtension('WEBGL_lose_context')?.loseContext();root.replaceChildren()}return Object.freeze({contract:CONTRACT,setState:setLifecycle,playSignature,selectResponse,readerOpen,restore,inspect,destroy,element:canvas})}export{CONTRACT as AWARDS_WORLD_CYCLE_B_CONTRACT};
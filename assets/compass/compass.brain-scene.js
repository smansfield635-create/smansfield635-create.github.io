(()=>{
'use strict';
const M=Math,P=M.PI,TAU=P*2,reduce=matchMedia('(prefers-reduced-motion: reduce)'),scenes=new WeakMap();let primary=null;
const norm=v=>{const d=M.hypot(...v)||1;return v.map(x=>x/d)};
const poses=Object.freeze({front:{yaw:0,pitch:0},side:{yaw:P/2,pitch:0},rear:{yaw:P,pitch:0},underside:{yaw:.58,pitch:-.48}});
const rose=[.82,.47,.50],roseLight=[.95,.67,.65],roseDeep=[.43,.20,.24],roseDark=[.14,.075,.095],warm=[.84,.49,.43],palette={frontal:roseLight,temporal:rose,parietal:roseLight,occipital:rose,medial:roseDark,cerebellum:roseDeep,pons:warm,brainstem:roseDeep};
function hemispherePoint(side,phi,theta){const vertical=M.sin(phi),ring=M.cos(phi),depth=M.sin(theta),outward=M.max(0,M.cos(theta)),frontal=M.exp(-M.pow((theta-.76)/.54,2))*M.exp(-M.pow((phi-.04)/.92,2)),parietal=M.exp(-M.pow((theta+.02)/.68,2))*M.exp(-M.pow((phi-.40)/.50,2)),occipital=M.exp(-M.pow((theta+.90)/.46,2))*M.exp(-M.pow((phi-.02)/.78,2)),temporal=M.exp(-M.pow((theta-.02)/.82,2))*M.exp(-M.pow((phi+.56)/.30,2)),fold=.64*M.sin(theta*7.2+phi*4.7)+.34*M.sin(theta*12.5-phi*7.3)+.17*M.sin(theta*18.2+phi*10.4)+.11*M.sin(theta*25.3-phi*15.1),cross=.24*M.sin(theta*5.3-phi*9.1)+.14*M.sin(theta*14.7+phi*3.4),lobe=1+.065*frontal+.04*parietal+.035*occipital+.085*temporal,lr=.73*(.92+.11*temporal+.03*parietal),position=[side*(.045+lr*ring*outward*lobe*(1+.095*fold+.022*cross)),.075+.70*vertical*(1+.024*frontal+.02*parietal)+.018*parietal-.045*temporal+.038*fold*ring+.012*cross,.015+.91*(1+.075*frontal+.04*occipital+.035*temporal)*ring*depth+.042*frontal*ring+.048*fold*ring+.014*cross],base=[side*ring*outward,vertical*.76,ring*depth],normal=norm([base[0]+side*(.13*fold+.045*cross)*ring,base[1]+.08*cross,base[2]+.12*fold-.04*cross]),region=phi<-.27?'temporal':theta>.38?'frontal':theta<-.48?'occipital':'parietal';return{position,normal,region}}
function buildMesh(gl){const pos=[],nor=[],col=[],idx=[],parts=[],bounds={min:[Infinity,Infinity,Infinity],max:[-Infinity,-Infinity,-Infinity]},vertex=(p,n,c)=>{const i=pos.length/3;pos.push(...p);nor.push(...n);col.push(...c);p.forEach((v,a)=>{bounds.min[a]=M.min(bounds.min[a],v);bounds.max[a]=M.max(bounds.max[a],v)});return i},tri=(a,b,c)=>idx.push(a,b,c);
function hemi(side){const start=idx.length,rows=28,cols=44,stride=cols+1,g=[];for(let r=0;r<=rows;r++){const phi=-P/2+r/rows*P;for(let c=0;c<=cols;c++){const q=hemispherePoint(side,phi,-P/2+c/cols*P);g.push(vertex(q.position,q.normal,palette[q.region]))}}for(let r=0;r<rows;r++)for(let c=0;c<cols;c++){const a=g[r*stride+c],b=a+1,d=g[(r+1)*stride+c],e=d+1;side<0?(tri(a,e,d),tri(a,b,e)):(tri(a,d,e),tri(a,e,b))}parts.push({name:side<0?'left-hemisphere':'right-hemisphere',triangles:(idx.length-start)/3})}
function ellipsoid(name,cx,cy,cz,rx,ry,rz,cols=28,rows=16,warp=.025,color=palette.cerebellum,folia=0){const start=idx.length,g=[];for(let r=0;r<=rows;r++){const phi=-P/2+r/rows*P;for(let c=0;c<cols;c++){const th=c/cols*TAU,relief=1+warp*(M.sin(th*6+phi*5)+.4*M.sin(th*11-phi*7))+folia*.018*M.sin(phi*24),cp=M.cos(phi);g.push(vertex([cx+rx*cp*M.cos(th)*relief,cy+ry*M.sin(phi)*relief,cz+rz*cp*M.sin(th)*relief],norm([cp*M.cos(th)/rx,M.sin(phi)/ry,cp*M.sin(th)/rz]),color))}}for(let r=0;r<rows;r++)for(let c=0;c<cols;c++){const n=(c+1)%cols,a=g[r*cols+c],b=g[r*cols+n],e=g[(r+1)*cols+n],d=g[(r+1)*cols+c];tri(a,b,e);tri(a,e,d)}parts.push({name,triangles:(idx.length-start)/3})}
function tube(name,rings,color){const start=idx.length,seg=24,g=[];rings.forEach(q=>{for(let i=0;i<seg;i++){const t=i/seg*TAU,rx=q.rx??q.r,rz=q.rz??q.r*.78;g.push(vertex([q.x||0+rx*M.cos(t),q.y,q.z+rz*M.sin(t)],norm([M.cos(t),q.slope||.12,M.sin(t)]),color))}});for(let r=0;r<rings.length-1;r++)for(let i=0;i<seg;i++){const n=(i+1)%seg,a=g[r*seg+i],b=g[r*seg+n],e=g[(r+1)*seg+n],d=g[(r+1)*seg+i];tri(a,b,e);tri(a,e,d)}parts.push({name,triangles:(idx.length-start)/3})}
hemi(-1);hemi(1);ellipsoid('cerebellum',0,-.54,-.61,.57,.27,.36,34,18,.045,palette.cerebellum,1);ellipsoid('pons',0,-.57,-.08,.235,.18,.22,24,14,.025,palette.pons);tube('brainstem',[{y:-.61,z:-.10,rx:.15,rz:.12},{y:-.78,z:-.11,rx:.12,rz:.095},{y:-.96,z:-.125,rx:.08,rz:.065},{y:-1.10,z:-.135,rx:.055,rz:.047}],palette.brainstem);
const buffer=(target,data)=>{const b=gl.createBuffer();gl.bindBuffer(target,b);gl.bufferData(target,data,gl.STATIC_DRAW);return b};return{positions:buffer(gl.ARRAY_BUFFER,new Float32Array(pos)),normals:buffer(gl.ARRAY_BUFFER,new Float32Array(nor)),colors:buffer(gl.ARRAY_BUFFER,new Float32Array(col)),indices:buffer(gl.ELEMENT_ARRAY_BUFFER,new Uint16Array(idx)),count:idx.length,triangles:idx.length/3,parts,bounds,depthRatio:Number(((bounds.max[2]-bounds.min[2])/(bounds.max[0]-bounds.min[0])).toFixed(3))}}
function shader(gl,type,src){const s=gl.createShader(type);gl.shaderSource(s,src);gl.compileShader(s);if(!gl.getShaderParameter(s,gl.COMPILE_STATUS))throw new Error(gl.getShaderInfoLog(s)||'shader');return s}
function program(gl){const vs=`attribute vec3 p,n,c;uniform mat4 m;uniform float aspect;varying vec3 N,P,C;void main(){P=(m*vec4(p,1.)).xyz;N=normalize(mat3(m)*n);C=c;float sx=.90/max(aspect,1.);gl_Position=vec4(P.x*sx,P.y*.72+.10,P.z*.22,1.);}`,fs=`precision mediump float;varying vec3 N,P,C;void main(){vec3 n=normalize(N),key=normalize(vec3(-.42,.72,.55)),fill=normalize(vec3(.54,.08,.84)),view=normalize(vec3(0.,.10,1.9)-P);float kd=max(dot(n,key),0.),fd=max(dot(n,fill),0.),rim=pow(1.-max(dot(n,view),0.),2.4),spec=pow(max(dot(reflect(-key,n),view),0.),30.);float sulcus=.50+.36*max(n.y*.58+n.z*.42,0.);vec3 tissue=mix(vec3(.11,.045,.065),C,.30+.54*kd+.09*fd)*sulcus;vec3 cool=vec3(.32,.69,.76)*(fd*.055+rim*.13);vec3 warm=vec3(1.,.73,.65)*spec*.12;gl_FragColor=vec4(tissue+cool+warm,.985);}`;const p=gl.createProgram();gl.attachShader(p,shader(gl,gl.VERTEX_SHADER,vs));gl.attachShader(p,shader(gl,gl.FRAGMENT_SHADER,fs));gl.linkProgram(p);if(!gl.getProgramParameter(p,gl.LINK_STATUS))throw new Error(gl.getProgramInfoLog(p)||'link');return p}
const matrix=(yaw,pitch)=>{const cy=M.cos(yaw),sy=M.sin(yaw),cx=M.cos(pitch),sx=M.sin(pitch);return new Float32Array([cy,sy*sx,sy*cx,0,0,cx,-sx,0,-sy,cy*sx,cy*cx,0,0,0,0,1])};
function mount(canvas,{foreground=()=>true}={}){if(!canvas||scenes.has(canvas))return scenes.get(canvas);const gl=canvas.getContext('webgl',{alpha:true,antialias:true,powerPreference:'low-power',preserveDrawingBuffer:true});if(!gl){const f=document.createElement('div');f.className='compass-brain-fallback';f.setAttribute('role','img');f.setAttribute('aria-label','Static rose anatomical brain');f.innerHTML='<span class="fallback-hemisphere fallback-hemisphere--left"></span><span class="fallback-hemisphere fallback-hemisphere--right"></span><span class="fallback-cerebellum"></span><span class="fallback-stem"></span>';canvas.hidden=true;canvas.parentElement?.append(f);const api={canvas,fallback:true,capture:()=>null,restore:()=>false,inspect:()=>({fallback:true,material:'NATIVE_ROSE_FLESH_V2',anatomy:'FOLDED_HEMISPHERES_CEREBELLUM_PONS_BRAINSTEM'})};scenes.set(canvas,api);primary=api;return api}
canvas.hidden=false;canvas.style.display='block';const pr=program(gl),mesh=buildMesh(gl),data=canvas.dataset,L={p:gl.getAttribLocation(pr,'p'),n:gl.getAttribLocation(pr,'n'),c:gl.getAttribLocation(pr,'c'),m:gl.getUniformLocation(pr,'m'),aspect:gl.getUniformLocation(pr,'aspect')};Object.assign(data,{brainContract:'COMPASS_COHERISCOPE_ANATOMICAL_WEBGL_v5',brainRenderer:'anatomical-webgl-v5',brainMaterial:'NATIVE_ROSE_FLESH_V2',brainVolumetricPass:'generation-1562',brainLighting:'directional-key,cool-environment-fill,sulcal-occlusion,soft-rim',brainDepthModel:'GEOMETRY_LIGHT_FALLOFF_FOLD_OCCLUSION_FORM_SHADOW',brainLinework:'none',brainComponents:'left-hemisphere,right-hemisphere,longitudinal-fissure,cerebellum,pons,brainstem',brainMotion:reduce.matches?'static-reduced-motion':'slow-yaw',brainTriangleCount:String(mesh.triangles),brainDepthRatio:String(mesh.depthRatio)});
let yaw=.34,pitch=-.04,raf=0,visible=true,pageOn=!document.hidden,last=0,snap=false,saved=null;const animate=()=>!reduce.matches&&!snap&&visible&&pageOn&&foreground(),resize=()=>{const d=M.min(devicePixelRatio||1,innerWidth<700?1.35:1.8),r=canvas.getBoundingClientRect(),w=M.max(2,M.round(r.width*d)),h=M.max(2,M.round(r.height*d));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h;gl.viewport(0,0,w,h)}},bind=(b,l)=>{gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.enableVertexAttribArray(l);gl.vertexAttribPointer(l,3,gl.FLOAT,false,0,0)};
function draw(time=performance.now()){raf=0;resize();if(animate()){const e=M.min(40,time-last||16);yaw+=e*.00010}last=time;gl.clearColor(0,0,0,0);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);gl.enable(gl.DEPTH_TEST);gl.disable(gl.CULL_FACE);gl.useProgram(pr);gl.uniformMatrix4fv(L.m,false,matrix(yaw,pitch));gl.uniform1f(L.aspect,canvas.width/M.max(1,canvas.height));bind(mesh.positions,L.p);bind(mesh.normals,L.n);bind(mesh.colors,L.c);gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,mesh.indices);gl.drawElements(gl.TRIANGLES,mesh.count,gl.UNSIGNED_SHORT,0);data.brainFrames=String((Number(data.brainFrames)||0)+1);if(animate())raf=requestAnimationFrame(draw)}
const schedule=(force=false)=>{if(!raf&&(force||animate()))raf=requestAnimationFrame(draw)},capture=name=>{const pose=typeof name==='number'?{yaw:name*P/180,pitch:0}:poses[name];if(!pose)return null;if(!saved)saved={yaw,pitch};snap=true;if(raf)cancelAnimationFrame(raf);raf=0;yaw=pose.yaw;pitch=pose.pitch;draw();return{view:typeof name==='string'?name:'custom',yaw,pitch,brainDepthRatio:mesh.depthRatio,parts:mesh.parts}},restore=()=>{if(!saved)return false;({yaw,pitch}=saved);saved=null;snap=false;draw();schedule();return true};
const io=new IntersectionObserver(es=>{visible=es.some(e=>e.isIntersecting);schedule(true)},{rootMargin:'120px'});io.observe(canvas);document.addEventListener('visibilitychange',()=>{pageOn=!document.hidden;schedule(true)});reduce.addEventListener?.('change',()=>schedule(true));const api={canvas,fallback:false,capture,restore,draw:()=>schedule(true),inspect:()=>({fallback:false,renderer:data.brainRenderer,foreground:foreground(),material:data.brainMaterial,depthRatio:mesh.depthRatio,triangles:mesh.triangles,parts:mesh.parts,motion:data.brainMotion})};scenes.set(canvas,api);primary=api;draw();schedule();return api}
const publicApi=Object.freeze({version:'anatomical-webgl-v5-gen1562',brainMaterial:'NATIVE_ROSE_FLESH_V2',mount,capture:view=>primary?.capture(view)||null,restore:()=>primary?.restore()||false,inspect:()=>primary?.inspect()||null});Object.defineProperty(globalThis,'CompassBrainScene',{configurable:false,enumerable:true,get:()=>publicApi,set:()=>{}});
})();
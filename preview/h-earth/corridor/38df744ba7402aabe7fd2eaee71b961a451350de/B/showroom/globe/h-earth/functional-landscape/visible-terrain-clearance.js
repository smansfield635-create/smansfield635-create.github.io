/**
 * H_EARTH_VISIBLE_TERRAIN_CLEARANCE_SURFACE_GEN314_v1
 *
 * Samples the exact terrain mesh presented by the protected Gen311 landscape.
 * Gen321 established dense camera-volume protection. Gen322 adds an observer-
 * view stand-off reference above materially rising relief so a mathematically
 * clear camera cannot still skim a slope with terrain consuming the view.
 *
 * This module creates no geography, topology, renderer, navigation-scale,
 * collision/physics, merge, deployment, or production authority.
 */
import { H_EARTH_FUNCTIONAL_LANDSCAPE_NEUTRAL_PREVIEW } from '../render/landscape-preview.js';

const freeze=(v,s=new WeakSet())=>{if(v===null||typeof v!=='object'||Object.isFrozen(v)||s.has(v))return v;s.add(v);Object.values(v).forEach(x=>freeze(x,s));return Object.freeze(v)};
const finite=v=>typeof v==='number'&&Number.isFinite(v);
const clamp=(v,a,b)=>Math.min(b,Math.max(a,v));

export const H_EARTH_VISIBLE_TERRAIN_CLEARANCE_CONTRACT_ID='H_EARTH_VISIBLE_TERRAIN_CLEARANCE_SURFACE_GEN314_v1';
export const H_EARTH_VISIBLE_TERRAIN_CLEARANCE_PROTECTED_FLOOR='e03363f42441cea7587a49623fd878e8ca51fe28';
export const H_EARTH_CAMERA_VOLUME_CLEARANCE_PROTECTED_PARENT='5f379351d93e1cb01b1eea1b3b72a37d0256ada0';
export const H_EARTH_CAMERA_VOLUME_CLEARANCE_CONTRACT_ID='H_EARTH_CAMERA_VOLUME_CLEARANCE_GEN321_v1';
export const H_EARTH_OBSERVER_VIEW_STANDOFF_CONTRACT_ID='H_EARTH_OBSERVER_VIEW_STANDOFF_GEN322_v1';

function terrainPrimitive(){const preview=H_EARTH_FUNCTIONAL_LANDSCAPE_NEUTRAL_PREVIEW,primitive=preview?.componentResults?.terrain?.primitive??null,relief=preview?.regionalRelief;if(preview?.ok!==true||relief?.ok!==true||relief?.primitive!==primitive)return null;if(primitive?.metadata?.gen311RegionalReliefMaterialized!==true)return null;return primitive}
function barycentricTerrainY(a,b,c,x,z){const den=(b.z-c.z)*(a.x-c.x)+(c.x-b.x)*(a.z-c.z);if(Math.abs(den)<=1e-12)return null;const u=((b.z-c.z)*(x-c.x)+(c.x-b.x)*(z-c.z))/den,v=((c.z-a.z)*(x-c.x)+(a.x-c.x)*(z-c.z))/den,w=1-u-v,e=1e-8;if(u<-e||v<-e||w<-e)return null;return a.y*u+b.y*v+c.y*w}
function samplePresentedTriangleSurface(geometry,x,z){const vertices=geometry?.vertices??[],indices=geometry?.indices??[];for(let o=0;o+2<indices.length;o+=3){const a=vertices[indices[o]],b=vertices[indices[o+1]],c=vertices[indices[o+2]];if(!a||!b||!c)continue;if(x<Math.min(a.x,b.x,c.x)-1e-8||x>Math.max(a.x,b.x,c.x)+1e-8||z<Math.min(a.z,b.z,c.z)-1e-8||z>Math.max(a.z,b.z,c.z)+1e-8)continue;const y=barycentricTerrainY(a,b,c,x,z);if(finite(y))return{y,triangleOffset:o,triangleIndices:[indices[o],indices[o+1],indices[o+2]]};}return null}
export function sampleHEarthVisibleTerrainClearanceSurface(worldX,worldZ){const primitive=terrainPrimitive();if(!primitive)return freeze({valid:false,status:'VISIBLE_TERRAIN_SURFACE_UNAVAILABLE',issues:['PROTECTED_GEN311_VISIBLE_TERRAIN_MESH_INVALID']});const geometry=primitive.geometry,hit=samplePresentedTriangleSurface(geometry,worldX,worldZ);if(!hit)return freeze({valid:false,status:'VISIBLE_TERRAIN_SAMPLE_OUTSIDE_PRESENTED_DOMAIN',worldX,worldZ,issues:['VISIBLE_TERRAIN_FOOTPRINT_OUTSIDE_MESH']});return freeze({valid:true,status:'VISIBLE_TERRAIN_SAMPLE_COMPLETE',contractId:H_EARTH_VISIBLE_TERRAIN_CLEARANCE_CONTRACT_ID,cameraVolumeContractId:H_EARTH_CAMERA_VOLUME_CLEARANCE_CONTRACT_ID,observerViewStandOffContractId:H_EARTH_OBSERVER_VIEW_STANDOFF_CONTRACT_ID,protectedGeographicFloor:H_EARTH_VISIBLE_TERRAIN_CLEARANCE_PROTECTED_FLOOR,protectedParentHead:H_EARTH_CAMERA_VOLUME_CLEARANCE_PROTECTED_PARENT,worldX,worldZ,visibleElevation:hit.y,sourcePrimitiveId:primitive.primitiveId,sourceGeometryId:geometry.geometryId,sourceReliefContractId:primitive.metadata.gen311RegionalReliefContractId,triangle:freeze({offset:hit.triangleOffset,indices:freeze(hit.triangleIndices)}),samplingLaw:'PRESENTED_TRIANGLE_BARYCENTRIC_INTERPOLATION',presentedMeshSampled:true,canonicalWorldFieldMutated:false,geographyAuthorityCreated:false,topologyAuthorityCreated:false,navigationScaleAuthorityCreated:false,issues:[]})}
function cameraVolumePoints(worldX,worldZ,{yawDegrees,lookAheadDistance,lateralRadius}){const yaw=(finite(yawDegrees)?yawDegrees:0)*Math.PI/180,forward={x:Math.sin(yaw),z:-Math.cos(yaw)},right={x:Math.cos(yaw),z:Math.sin(yaw)},reach=Math.max(10,finite(lookAheadDistance)?lookAheadDistance:0),radius=Math.max(4.5,(finite(lateralRadius)?lateralRadius:0)*3.6),distances=[0,.25,.5,.75,1,1.5,2,2.5,3,4,5,6,8,10,reach],lateralFractions=[-1,-.8,-.6,-.4,-.2,0,.2,.4,.6,.8,1],points=[];for(const distance of distances){const localRadius=Math.max(finite(lateralRadius)?lateralRadius:0,radius*(1-.02*distance));for(const fraction of lateralFractions){const lateral=localRadius*fraction;points.push({role:distance===0&&fraction===0?'CENTER':'CAMERA_VOLUME',distance,lateral,critical:distance<=6,x:worldX+forward.x*distance+right.x*lateral,z:worldZ+forward.z*distance+right.z*lateral})}}return freeze({points,reach,radius})}
function observerViewStandOff({centerElevation,highestElevation,highestDistance}){const prominence=Math.max(0,highestElevation-centerElevation);if(prominence<2)return freeze({active:false,prominence,margin:0,proximityBoost:0});const proximityBoost=Math.max(0,6-highestDistance)*.3;const margin=clamp(3+prominence*.32+proximityBoost,3,14);return freeze({active:true,prominence,margin,proximityBoost})}
export function sampleHEarthVisibleTerrainClearanceEnvelope(worldX,worldZ,{yawDegrees=0,lookAheadDistance=6,lateralRadius=1.25}={}){const volume=cameraVolumePoints(worldX,worldZ,{yawDegrees,lookAheadDistance,lateralRadius}),samples=volume.points.map(p=>freeze({...p,sample:sampleHEarthVisibleTerrainClearanceSurface(p.x,p.z)})),center=samples.find(x=>x.role==='CENTER');if(center?.sample?.valid!==true)return freeze({valid:false,status:'VISIBLE_TERRAIN_CAMERA_VOLUME_INVALID',samples,issues:['CENTER_VISIBLE_TERRAIN_SAMPLE_INVALID']});const critical=samples.filter(x=>x.critical);if(critical.some(x=>x.sample.valid!==true))return freeze({valid:false,status:'VISIBLE_TERRAIN_CAMERA_VOLUME_INVALID',samples,issues:['CRITICAL_CAMERA_VOLUME_SAMPLE_OUTSIDE_PRESENTED_DOMAIN']});const valid=samples.filter(x=>x.sample.valid===true);if(valid.length<Math.ceil(samples.length*.8))return freeze({valid:false,status:'VISIBLE_TERRAIN_CAMERA_VOLUME_INVALID',samples,issues:['CAMERA_VOLUME_SAMPLE_COVERAGE_INSUFFICIENT']});const highest=[...valid].sort((a,b)=>b.sample.visibleElevation-a.sample.visibleElevation||a.distance-b.distance)[0],actualVisibleElevation=highest.sample.visibleElevation,standOff=observerViewStandOff({centerElevation:center.sample.visibleElevation,highestElevation:actualVisibleElevation,highestDistance:highest.distance}),clearanceReferenceElevation=actualVisibleElevation+standOff.margin;return freeze({valid:true,status:'VISIBLE_TERRAIN_OBSERVER_VIEW_STANDOFF_COMPLETE',contractId:H_EARTH_VISIBLE_TERRAIN_CLEARANCE_CONTRACT_ID,cameraVolumeContractId:H_EARTH_CAMERA_VOLUME_CLEARANCE_CONTRACT_ID,observerViewStandOffContractId:H_EARTH_OBSERVER_VIEW_STANDOFF_CONTRACT_ID,protectedParentHead:H_EARTH_CAMERA_VOLUME_CLEARANCE_PROTECTED_PARENT,visibleElevation:clearanceReferenceElevation,clearanceReferenceElevation,actualVisibleElevation,centerElevation:center.sample.visibleElevation,observerViewStandOffActive:standOff.active,observerViewStandOffMargin:standOff.margin,terrainProminence:standOff.prominence,highestSample:freeze({x:highest.x,z:highest.z,distance:highest.distance,lateral:highest.lateral,visibleElevation:actualVisibleElevation}),sampleCount:valid.length,requiredSampleCount:samples.length,invalidSampleCount:samples.length-valid.length,cameraVolumeReach:volume.reach,cameraVolumeRadius:volume.radius,samples,conservativeMaximumUsed:true,denseCameraVolumeSampled:true,nearPlaneProtection:true,steepSlopeLateralProtection:true,usableObserverViewStandOffProtected:true,actualTerrainElevationRetainedSeparately:true,geographyAuthorityCreated:false,topologyAuthorityCreated:false,navigationScaleAuthorityCreated:false,issues:[]})}
export default H_EARTH_VISIBLE_TERRAIN_CLEARANCE_CONTRACT_ID;

# Methods native visual architecture remediation prototype v1

Nonproduct Phase 2A implementation only.

This harness loads the unchanged Methods Euclidean showroom and applies `prototype.css` and `prototype.js` only inside the browser session. It establishes native `OVERVIEW` and `BROWSE` camera states, replaces the failed overlapping composition with reserved responsive regions, and captures a representative risk set.

It does not modify public Methods files, execute the full 1,575-state instrument surface, grant product acceptance, create a product pull request, or authorize merge.

The full instrument becomes eligible only after the internal perceptual disposition is `MATERIALLY_COHERENT_AND_WORTH_FULL_VERIFICATION`.

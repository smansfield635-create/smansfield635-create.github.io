name: H-Earth Current-Live Microrelief Exact-Head Validation v4

on:
  push:
    branches:
      - agent/h-earth-current-live-additive-terrain-microrelief-001
    paths:
      - .github/workflows/h-earth-current-live-microrelief-exact-head-validation-v4.yml

permissions:
  contents: read
  pull-requests: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Validate source contract and module syntax
        run: |
          node --input-type=module --check < showroom/globe/h-earth/render/persistent-live-renderer.run8e-r3c.cp2-additive-microrelief-v1.js
          node --input-type=module --check < showroom/globe/h-earth/diagnostic/run8e-r3d/live-gpu-binding.js
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          const binding = fs.readFileSync('showroom/globe/h-earth/diagnostic/run8e-r3d/live-gpu-binding.js', 'utf8');
          const index = fs.readFileSync('showroom/globe/h-earth/index.html', 'utf8');
          const required = [
            "const ADDITIVE_VISUAL_QUERY_KEY = 'visual';",
            "const ADDITIVE_VISUAL_QUERY_VALUE = 'terrain-microrelief-v1';",
            "'../../render/persistent-live-renderer.run8e-r3c.cp2-additive-microrelief-v1.js';",
            "'../../render/persistent-live-renderer.run8e-r3c.cp2-round1-1f520809.js';",
            "'../../render/persistent-live-renderer.run8e-r3c.js';",
            'additiveVisualRequested\n    ? ADDITIVE_VISUAL_RENDERER_PATH\n    : cp2LiveDifferentialRequested\n      ? CP2_LIVE_DIFFERENTIAL_RENDERER_PATH\n      : ACCEPTED_BASELINE_RENDERER_PATH;',
            'const selectedRendererModule = await import(selectedRendererPath);'
          ];
          for (const token of required) if (!binding.includes(token)) throw new Error(`BINDING_CONTRACT_MISSING:${token}`);
          if (!index.includes("parameters.set('cp2', 'round1-1f520809')")) throw new Error('ACCEPTED_DEFAULT_CP2_PROMOTION_MISSING');
          NODE
      - name: Install Chromium
        run: |
          npm install --no-save --package-lock=false playwright@1.54.1
          npx playwright install --with-deps chromium
      - name: Start exact-head server
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 > h-earth-microrelief-server.log 2>&1 &
          for attempt in $(seq 1 60); do
            curl --fail --silent http://127.0.0.1:4173/showroom/globe/h-earth/ > /dev/null && exit 0
            sleep 1
          done
          cat h-earth-microrelief-server.log
          exit 1
      - name: Compile, link, render, and verify live selector behavior
        run: |
          cat > verify-microrelief.cjs <<'NODE'
          const fs = require('fs');
          const { chromium } = require('playwright');
          const CP2 = '../../render/persistent-live-renderer.run8e-r3c.cp2-round1-1f520809.js';
          const ADDITIVE = '../../render/persistent-live-renderer.run8e-r3c.cp2-additive-microrelief-v1.js';
          async function runCase(browser, label, query, path, additive, cp2) {
            const context = await browser.newContext({ viewport: { width: 1280, height: 720 } });
            const page = await context.newPage();
            const consoleErrors = [], pageErrors = [], requestFailures = [];
            page.on('console', m => { if (m.type() === 'error') consoleErrors.push(m.text()); });
            page.on('pageerror', e => pageErrors.push(e.stack || e.message));
            page.on('requestfailed', r => requestFailures.push({ url: r.url(), error: r.failure()?.errorText ?? null }));
            const url = `http://127.0.0.1:4173/showroom/globe/h-earth/${query}`;
            const response = await page.goto(url, { waitUntil: 'domcontentloaded', timeout: 60000 });
            await page.waitForFunction(() => document.getElementById('h-earth-functional-landscape-route')?.dataset?.run8eReady === 'true' && typeof window.H_EARTH_RUN8E_PUBLIC_ROUTE?.getLiveGpuReceipt === 'function', null, { timeout: 120000 });
            const snapshot = await page.evaluate(() => {
              const root = document.getElementById('h-earth-functional-landscape-route');
              const canvas = document.getElementById('h-earth-functional-landscape-canvas');
              const receipt = window.H_EARTH_RUN8E_PUBLIC_ROUTE.getLiveGpuReceipt();
              const gl = canvas?.getContext?.('webgl2') ?? null;
              return { href: location.href, ready: root?.dataset?.run8eReady, error: root?.dataset?.run8eError, canvas: canvas instanceof HTMLCanvasElement, webgl: Boolean(gl), lost: gl?.isContextLost?.() ?? null, receipt };
            });
            const r = snapshot.receipt;
            const failures = [];
            if (response?.status() !== 200) failures.push(`HTTP_${response?.status()}`);
            if (snapshot.ready !== 'true' || snapshot.error === 'true') failures.push('RUNTIME_NOT_READY');
            if (!snapshot.canvas || !snapshot.webgl || snapshot.lost !== false) failures.push('WEBGL2_INVALID');
            if (r?.selectedRendererPath !== path) failures.push(`PATH:${r?.selectedRendererPath}`);
            if (r?.additiveVisualRequested !== additive) failures.push('ADDITIVE_FLAG');
            if (r?.liveDifferential?.requested !== cp2) failures.push('CP2_FLAG');
            if (r?.resources?.counters?.shaderCompileCount !== 4) failures.push(`SHADER_COMPILE:${r?.resources?.counters?.shaderCompileCount}`);
            if (r?.resources?.counters?.programLinkCount !== 2) failures.push(`PROGRAM_LINK:${r?.resources?.counters?.programLinkCount}`);
            if (r?.resources?.counters?.contextCreationCount !== 1) failures.push('CONTEXT_COUNT');
            if (r?.counters?.rendererInitializationCount !== 1) failures.push('RENDERER_COUNT');
            if ((r?.counters?.gpuFramebufferPresentationCount ?? 0) < 1) failures.push('NO_FRAME');
            if (r?.resources?.packageUploadedOnce !== true) failures.push('PACKAGE_UPLOAD');
            if (r?.resources?.resourceIdentityStable !== true) failures.push('RESOURCE_IDENTITY');
            if (r?.resources?.noPostInitializationResourceCreation !== true) failures.push('POST_INIT_RESOURCE');
            if (r?.resources?.noPostInitializationBufferUpload !== true) failures.push('POST_INIT_UPLOAD');
            const blockingConsole = consoleErrors.filter(x => !/favicon|404 \(File not found\)/i.test(x));
            const blockingRequests = requestFailures.filter(x => !/favicon/i.test(x.url));
            if (pageErrors.length) failures.push(`PAGE:${JSON.stringify(pageErrors)}`);
            if (blockingConsole.length) failures.push(`CONSOLE:${JSON.stringify(blockingConsole)}`);
            if (blockingRequests.length) failures.push(`REQUEST:${JSON.stringify(blockingRequests)}`);
            const result = { label, requestedUrl: url, finalUrl: snapshot.href, httpStatus: response?.status() ?? null, selectedRendererPath: r?.selectedRendererPath ?? null, additiveVisualRequested: r?.additiveVisualRequested ?? null, cp2Requested: r?.liveDifferential?.requested ?? null, shaderCompileCount: r?.resources?.counters?.shaderCompileCount ?? null, programLinkCount: r?.resources?.counters?.programLinkCount ?? null, visibleFrames: r?.counters?.gpuFramebufferPresentationCount ?? null, failures };
            await context.close();
            if (failures.length) throw new Error(`${label}:${failures.join('|')}`);
            return result;
          }
          (async () => {
            const browser = await chromium.launch({ headless: true, args: ['--use-angle=swiftshader', '--enable-webgl', '--ignore-gpu-blocklist', '--disable-dev-shm-usage'] });
            const results = [];
            try {
              results.push(await runCase(browser, 'ACCEPTED_DEFAULT', '', CP2, false, true));
              results.push(await runCase(browser, 'EXPLICIT_CP2', '?cp2=round1-1f520809', CP2, false, true));
              results.push(await runCase(browser, 'ADDITIVE_QUERY', '?visual=terrain-microrelief-v1', ADDITIVE, true, true));
              results.push(await runCase(browser, 'ADDITIVE_PRECEDENCE', '?cp2=round1-1f520809&visual=terrain-microrelief-v1', ADDITIVE, true, true));
            } finally { await browser.close(); }
            const receipt = { receiptType: 'H_EARTH_CURRENT_LIVE_ADDITIVE_TERRAIN_MICRORELIEF_EXACT_HEAD_VALIDATION_v1', result: 'PASS', workflowRunId: process.env.GITHUB_RUN_ID, validationHead: process.env.GITHUB_SHA, javascriptSyntax: 'PASS', sourceSelectorContract: 'PASS', shaderCompile: 'PASS', shaderLink: 'PASS', acceptedDefaultSelectionPreserved: 'PASS', additivePrecedence: 'PASS', results, timestamp: new Date().toISOString() };
            fs.writeFileSync('microrelief-validation-receipt.json', JSON.stringify(receipt, null, 2) + '\n');
            console.log(JSON.stringify(receipt, null, 2));
          })().catch(error => {
            const receipt = { receiptType: 'H_EARTH_CURRENT_LIVE_ADDITIVE_TERRAIN_MICRORELIEF_EXACT_HEAD_VALIDATION_v1', result: 'FAIL', workflowRunId: process.env.GITHUB_RUN_ID, validationHead: process.env.GITHUB_SHA, javascriptSyntax: 'PASS', error: error.stack || error.message || String(error), timestamp: new Date().toISOString() };
            fs.writeFileSync('microrelief-validation-receipt.json', JSON.stringify(receipt, null, 2) + '\n');
            console.error(JSON.stringify(receipt, null, 2));
            process.exit(1);
          });
          NODE
          node verify-microrelief.cjs
      - name: Upload receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: h-earth-current-live-microrelief-exact-head-validation-v4
          path: |
            microrelief-validation-receipt.json
            h-earth-microrelief-server.log
          if-no-files-found: warn
          retention-days: 30
      - name: Publish receipt to PR 524
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          {
            echo '## Exact-head terrain microrelief validation v4'
            echo
            echo "Workflow run: \`${GITHUB_RUN_ID}\`"
            echo
            echo "Validation head: \`${GITHUB_SHA}\`"
            echo
            echo '```json'
            cat microrelief-validation-receipt.json 2>/dev/null || printf '%s\n' '{"result":"FAIL","error":"VALIDATION_RECEIPT_MISSING"}'
            echo '```'
          } > microrelief-comment.md
          gh pr comment 524 --body-file microrelief-comment.md

# RTS24 Gen428 Final Adjudication Handoff

**Status:** `CONTROLLING_EXECUTION_HANDOFF`  
**Date:** `2026-09-04`  
**Base:** `90413dd4020072fb7b6fa391868ba19e4287eab4`  
**Branch:** `research/rts24-gen428-final-adjudication-20260904`  
**Carrier issue:** `#2709`

## 1. Purpose

This handoff exists to prevent the RTS24 matched-state experiment from collapsing into repeated repository archaeology, transport debugging, or post-outcome redesign.

The next scientific boundary is already frozen. The task is to execute it, not reinterpret it.

## 2. Final scientific boundary

`GEN428 = RTS24 NON-EQUIVALENT X-ONLY PAIR-SEAL PREFLIGHT`

Required sequence:

1. regenerate the frozen 659 RTS24 states;
2. quotient only known physically equivalent branch/state duplicates;
3. recompute the frozen X8 representation on the quotient population;
4. perform the frozen deterministic nearest-state matching;
5. apply the unchanged gates:
   - `N_pairs >= 30`
   - `N_clusters >= 15`
   - `max_c(N_c / N_pairs) <= 0.20`;
6. emit one terminal pair-seal disposition.

No feature, matching, threshold, population, or equivalence rule may be changed after outcome exposure.

## 3. Terminal dispositions

Exactly three preflight outcomes are lawful:

### `PAIR_SEALED`

The frozen quotient population supplies enough genuinely non-equivalent matched pairs. Proceed exactly once to the frozen matched-state scientific comparison, record the result, and close the RTS24 route.

### `INSUFFICIENT_IDENTIFIABILITY`

The frozen quotient population cannot satisfy the support gates. Close the RTS24 matched-state route. Do not create another generation to relax the design.

### `MATERIALIZATION_FAILURE`

The frozen 659-state population cannot be deterministically reconstructed from the bound source recipe. Repair only the transport/materialization defect. Do not alter scientific definitions, matching, or gates.

## 4. Execution substrate rule

The repository AI entry point prohibits GitHub Actions as agent-execution transport. Execution must use a native/connected execution tool, an authorized disposable builder/Codespace, or another exact-head execution substrate.

A room that lacks such a substrate must stop after producing the durable execution packet. It must not substitute workflow hunting, branch hunting, or repeated GitHub searches for execution.

## 5. Retrieval budget

For every missing artifact or binding:

- attempt at most two equivalent retrieval probes;
- after the first failure, change retrieval strategy;
- after the second evidence-free attempt, emit `NO_NEW_EVIDENCE_DO_NOT_REPEAT`;
- record the exact missing object or failed assertion;
- continue only by changing evidence source or execution substrate.

Repeated searches with synonyms do not count as progress.

## 6. Durable checkpoint law

No material state may exist only in chat context. Before crossing each boundary, commit or attach a receipt containing exact identities and the next lawful action.

Required checkpoints:

1. `SOURCE_BINDING_RECEIPT`
2. `659_STATE_MATERIALIZATION_RECEIPT`
3. `EQUIVALENCE_QUOTIENT_RECEIPT`
4. `X8_RECOMPUTATION_RECEIPT`
5. `PAIR_MATCHING_RECEIPT`
6. `PAIR_SEAL_DISPOSITION`
7. only after `PAIR_SEALED`: `FROZEN_MATCHED_STATE_RESULT`

Every receipt must contain:

- repository head;
- source paths and object hashes;
- environment/package identities where relevant;
- deterministic parameters and seed policy;
- input/output row counts;
- failed assertions, if any;
- result disposition;
- exactly one next lawful action.

## 7. Anti-rescue law

The following are prohibited after Gen428 begins:

- changing X8 to obtain more pairs;
- changing distance or tie-breaking semantics;
- changing the equivalence quotient;
- changing support gates;
- changing the 659-state population because the result is unfavorable;
- creating Gen429/Gen430 solely to rescue RTS24;
- relabeling insufficient identifiability as scientific support or contradiction;
- treating materialization failure as a scientific outcome.

## 8. Room takeover rule

A successor room must begin from the latest durable checkpoint, not from transcript reconstruction.

The takeover sequence is:

1. read this handoff;
2. read issue `#2709`;
3. resolve the exact branch/head;
4. locate the latest completed receipt;
5. verify its hashes/identities;
6. execute only the single next lawful action.

Whole-repository archaeology is prohibited unless the latest receipt explicitly identifies an unresolved source binding that requires it.

## 9. Stop condition

This RTS24 matched-state route ends after Gen428 pair sealing and, only if sealed, one frozen matched-state comparison.

`NO_OPEN_ENDED_GENERATION_CHAIN = TRUE`

## 10. Current next action

`RECOVER_EXACT_FROZEN_SOURCE_BINDINGS_AND_BUILD_SOURCE_BINDING_RECEIPT`

Do not execute the scientific comparison until the source binding and 659-state materialization receipts are durable and verified.

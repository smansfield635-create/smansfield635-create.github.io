# H-Earth Durable Variant-B / B2 Preview Authority Handoff v1

Status: DURABLE RECOVERY AUTHORITY
Mode: READ-ONLY INFRASTRUCTURE INVENTORY NEXT
Production mutation authority: NONE

## Purpose

This artifact exists so a new room does not reconstruct the H-Earth preview problem from conversation memory or invent a new renderer, capture harness, hosting provider, or production deployment path.

## Recovered authority

- Repository: smansfield635-create/smansfield635-create.github.io
- PR #4614: H-Earth: durable Variant B 38df744b inspection snapshot
- PR #4614 head: fb4a3dbb5799175535c21a9aa8a3bcf039b140ec
- PR #4614 contract: materializes the already browser-verified 38df744b Variant-B owner-inspection workspace into the durable preview tree using immutable Git tree objects.
- The snapshot contains the exact candidate H-Earth showroom tree, preserved-live-target-Y compositor, and exact candidate h-earth-3d dependency tree.
- Production H-Earth paths are not mutated by that snapshot.
- Variant B / B0 is the fixed observer/camera/renderer/compositor inspection authority.
- B2 candidate authority: d9b3c159… . B2 is terrain-field mutation only.
- Intended A/B construction: durable Variant-B tree plus exactly one logical substitution: B2 h-earth.terrain-field.js.
- Production remains untouched.

## Critical correction: hosting

VERCEL IS EXPLICITLY OUT OF SCOPE.

Do not reconnect Vercel. Do not add a Vercel dependency. Do not infer that a non-production browser URL requires Vercel.

The required capability is provider-neutral:

> Serve an immutable repository directory at a non-production URL without altering the GitHub Pages production deployment for diamondgatebridge.com.

The repository/tree side is already durable. The unresolved boundary is only the isolated non-production serving layer.

## Prohibited drift

Do not:
- invent another renderer;
- invent another B2 capture harness;
- change Variant-B camera/observer/FOV/movement;
- route preview publication through production GitHub Pages;
- alter diamondgatebridge.com;
- reconnect or recommend Vercel as a default solution;
- add any new external hosting provider before inventorying existing GitHub-native or already-authorized infrastructure;
- reconstruct B2 by guesswork;
- merge experimental Phase-3 residual/LOD work into this B2 cycle.

## Next deterministic task

Perform a READ-ONLY infrastructure inventory of the repository/account.

Goal: identify an EXISTING GitHub-native or already-authorized non-production serving capability that can host preview/h-earth/ independently of the production GitHub Pages deployment.

Inventory at minimum:
1. existing Actions workflows related to preview, Pages, artifacts, environments, deployments, static serving, tunnels, dev servers, or owner inspection;
2. repository environments and deployment records visible through authorized GitHub surfaces;
3. existing preview branches/directories and their publication mechanisms;
4. already-authorized external deployment integrations, if any, WITHOUT adding/reconnecting a provider;
5. whether GitHub-native artifacts or another existing repository mechanism can expose the immutable preview tree as a browser-accessible non-production surface.

If no existing serving authority exists, record exactly that fact. The next decision is then to choose a provider-neutral isolated serving target. Do not silently select Vercel.

## Desired durable chain

candidate SHA
→ immutable preview tree
→ isolated non-production serving authority
→ stable preview URL
→ deployment/inspection receipt

The missing component, if inventory confirms it, is ONLY the isolated serving authority.

## B2 acceptance boundary

Once browser materialization exists:

Variant B baseline
vs.
Variant B + B2 terrain-field substitution

Same world.
Same observer.
Same camera.
Same renderer.
Same compositor.
Same dependencies.
Terrain field only.

No production deployment is authorized by this handoff.

# Methods categorical spatial context and equation embodiment v1

This directory is the nonproduct successor carrier for:

```text
METHODS_CATEGORICAL_SPATIAL_CONTEXT_AND_EQUATION_EMBODIMENT_PASS_v1
```

It starts from exact operational baseline head:

```text
ab636b11e32253ece8e00f05993d980f747fa980
```

It preserves the baseline spatial registry, resolver, native bridge, camera lifecycle, Overview/Browse distinction, inspection entry, immutable return snapshot, twelve exact-return predicates, desktop/mobile operation, state publication, and evidence receipts.

The successor adds only the semantic embodiment layer required to render the actual canonical Methods corpus:

- direct extraction from `laws/research/methods-and-models/showroom.js`;
- one content-preserving semantic descriptor per canonical model;
- explicit equation-form classification for all 25 models;
- complete Overview projection of four families and 25 equation signatures;
- complete Browse projection of active identity, canonical equation, source state, neighbors, and the three native lenses;
- complete scientific inspection workbench;
- environmental distinction between source-confirmed and source-held architecture;
- equation-class-specific instrument geometry;
- static and browser-executed semantic gate reports.

Public Methods files and the preserved baseline directory are not changed. Merge, promotion, product acceptance, empirical validation, and full 1,575-state certification remain prohibited.

## Local validation

From repository root:

```bash
OUT_DIR=methods-categorical-spatial-context-equation-embodiment-v1-evidence \
node verification/methods-categorical-spatial-context-equation-embodiment-v1/validate.mjs
```

Start the repository server:

```bash
python3 -m http.server 4173 --bind 127.0.0.1
```

Open:

```text
http://127.0.0.1:4173/verification/methods-categorical-spatial-context-equation-embodiment-v1/
```

Run the browser observer after installing the pinned driver without modifying repository package files:

```bash
npm install --no-save --no-package-lock puppeteer-core@24.15.0
CHROME_PATH="$(command -v google-chrome || command -v chromium || command -v chromium-browser)" \
METHODS_ORIGIN=http://127.0.0.1:4173 \
OUT_DIR=methods-categorical-spatial-context-equation-embodiment-v1-evidence \
node verification/methods-categorical-spatial-context-equation-embodiment-v1/observer.mjs
```

The observer captures desktop and mobile Overview, Browse, model, lens, family, complete inspection, exact return, and four cross-family representative states. It records console errors, page errors, request failures, semantic completeness, source-state expression, and the complete exact-return receipts.

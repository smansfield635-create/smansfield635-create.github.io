/**
 * Temporary Target 4E execution-branch trigger only.
 * This path is intentionally absent from the installed H-Earth registry.
 * Expected automatic preflight disposition: STOP.
 */
export const TARGET_4E_AUTOMATIC_TRIGGER_ONLY = true;

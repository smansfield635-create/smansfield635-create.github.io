# COMPASS_TAKEOVER_BOUNDARY_v4

## Authority

**GEN1586 = DEPLOYED EVIDENCE BASELINE, NOT ACCEPTED SUCCESSOR.**

Gen1587 source was merged at `78ff88bf69bced653625e458b830d007bb8ce967`. Current public/main authority has since advanced through publication cleanup to `4b169d611624833cefcfc73492daea2c8ebf664b`, and the exact-head Pages deployment path has been independently verified live. Publication and visual/product qualification remain separate facts.

## Current user-observed disposition — 2026-08-22

The newest owner evidence supersedes several older unresolved predicates without erasing their historical record:

- **19/19 foreground-star → information coverage = PASS.** Do not spend another cycle re-proving the already-closed 19-state path unless a regression is observed.
- **Four constellation-star tab ownership = FAIL.** More than one tab shell remains visible at a time. A blank inactive tab is still an exposed competing owner and is therefore a failure. Exactly one tab/container may remain in visual/layout/focus flow at a time. The four constellation transitions must hand off state with the same clean replacement behavior as the already-passing 19 states.
- **Brain/Trophy/House capability carousel = presently acceptable.** Preserve it unless regression evidence identifies a concrete defect; it is not the primary next-cycle target.
- **Mirrorland = primary remaining product defect.** The four-choice chooser must be functionally hardwired to Narrative, Demo, World Map, and Return to Compass. When the page darkens, the four choices must become the luminous visual authority through glow/sparkle/reveal treatment rather than remaining weak dark rectangles. Underlying Mirrorland explanatory content must become visually/focus inert while the chooser owns the state. On Return to Compass, the chooser must disappear before the restoration animation begins so the restoration is visible.
- **TRL/TRA = structurally improved but still below the intended carousel standard.** Outer TRL/TRA selection alone is insufficient. Each active readiness family must retain the established site-wide carousel behavior: one stage, visible inner slide selection, one active card, swipe/drag, keyboard navigation, state-derived title/question/ordinal, clean outgoing→incoming replacement, and no inactive family residue.

## Ownership/layout invariant

One active owner per text region and one active tab/card shell per state. Active content reserves its own layout height. Outgoing state leaves visual/layout/focus flow before incoming state enters. Controls cannot occupy body-copy space. A blank inactive shell is not acceptable merely because its label text is empty.

## Site-wide carousel continuity standard

Compass MUST NOT invent a separate carousel interaction language. Methods & Models is the reference stage/orbit language; Developer, Evidence, and Governance carry the repository-documented `PUBLIC_LEGITIMACY_CONTEXTUAL_CAROUSEL_V3` family.

**ONE STAGE + TABS + ONE ACTIVE CARD + SWIPE/KEYBOARD + STATE-DERIVED HEADING.**

Brain/Trophy/House already approximates this standard sufficiently for the present cycle and should be preserved. Readiness remains an active completion target: outer `TRL | TRA` selects the readiness family, while inner carousel states within the selected family must use the established site grammar.

## Mirrorland acceptance contract

Mirrorland passes only when all of the following are demonstrated in rendered evidence:

1. The chooser darkens/suppresses the surrounding page while the four decisions visibly glow/sparkle/reveal as the sole interaction authority.
2. `Enter the Narrative` reaches the intended narrative destination.
3. `Enter the Demo` reaches the intended demo destination.
4. `See the World Map` reaches the intended world-map destination.
5. `Return to Compass` first removes the chooser, then begins the Compass restoration animation.
6. No underlying Mirrorland copy, stale control, hidden/blank tab shell, or focus target competes with the active chooser.

## Publication boundary

The obsolete push-triggered Pages authority has been retired. `.github/workflows/pages-direct-deploy.yml(target_sha=<exact approved current main>)` is the surviving explicit publication authority. Release proof still requires the matching public `.well-known/dgb-release.json` exact-SHA marker and successful live exact-head verification.

## Acceptance

Acceptance is whole-page, not patch-local. The next cycle should focus on the remaining bounded failures rather than reopening passed work:

- preserve the 19/19 PASS;
- repair the four constellation tabs to exactly-one-visible-owner semantics and 19-state-equivalent transitions;
- complete Mirrorland visual authority, hardwired four-way functionality, and teardown-before-return sequencing;
- complete TRL/TRA as a true carousel rather than a tab-only family switch;
- regression-check Brain/Trophy/House without redesigning it absent evidence.

A successor is not accepted merely because named defects were edited. Acceptance still requires no text collision, clipping, stale state, broken control, predecessor flash, inactive-carousel residue, blank competing tab shell, or newly introduced regression anywhere in the exercised page.

User-observed rendered evidence is the presentation authority. Source presence, CI assertions, merge success, exact-head deployment, and publication are necessary evidence where applicable but are not visual acceptance.

## Evidence spine

Read `docs/COMPASS_VISUAL_EVIDENCE_TIMELINE.md` for timestamped visual chronology, `docs/MASTER_PAGE_AND_COMPASS_CONTINUITY_LEDGER.md` for generation/change continuity, and `docs/PUBLICATION_AUTHORITY_BOUNDARY.md` for release/control-plane disposition.

Active reconstruction lane PR #1578 remains continuity evidence. Any further construction, qualification conclusion, or release claim must resolve against current `main` and these repository-resident boundaries rather than room memory.